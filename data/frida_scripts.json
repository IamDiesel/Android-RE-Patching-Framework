{
    "active_script_id": "556a3ab5",
    "scripts": [
        {
            "id": "f748766c",
            "name": "RASP Hunter",
            "code": "// @ts-nocheck\nconsole.log(\"[*] Native RASP Hunter gestartet.\");\n\n// 1. Dateizugriffe (I/O) \u00fcberwachen\n// Wir hooken die C-Funktionen open() und openat() direkt im Linux-Kernel\nconst openPtr = Module.findExportByName(\"libc.so\", \"open\");\nconst openatPtr = Module.findExportByName(\"libc.so\", \"openat\");\n\nfunction hookFileAccess(ptr, pathIdx) {\n    if (!ptr) return;\n    Interceptor.attach(ptr, {\n        onEnter: function (args) {\n            try {\n                this.path = args[pathIdx].readUtf8String();\n                if (!this.path) return;\n\n                // Filtere das Android-Grundrauschen heraus. \n                // Wir suchen nur nach Pfaden, die typischerweise von RASPs gelesen werden.\n                if (this.path.includes(\"/proc/\") || \n                    this.path.includes(\"base.apk\") || \n                    this.path.includes(\"frida\") ||\n                    this.path.includes(\"magisk\") ||\n                    this.path.includes(\"su\")) {\n                    \n                    console.log(\"[RASP-SCAN] Dateizugriff erkannt: \" + this.path);\n                }\n            } catch (e) {}\n        }\n    });\n}\n\nhookFileAccess(openPtr, 0);   // Signatur: int open(const char *pathname, int flags)\nhookFileAccess(openatPtr, 1); // Signatur: int openat(int dirfd, const char *pathname, int flags)\n\n// 2. Das Laden der RASP-Bibliothek abfangen\nconst dlopenPtr = Module.findExportByName(null, \"android_dlopen_ext\") || Module.findExportByName(null, \"dlopen\");\nif (dlopenPtr) {\n    Interceptor.attach(dlopenPtr, {\n        onEnter: function (args) {\n            try {\n                this.libName = args[0].readUtf8String();\n            } catch (e) {}\n        },\n        onLeave: function (retval) {\n            if (this.libName && this.libName.includes(\"libdb0c.so\")) {\n                console.log(\"[!] RASP PROTEKTOR (libdb0c.so) IN DEN RAM GELADEN!\");\n                console.log(\"[*] Base Address: \" + retval);\n            }\n        }\n    });\n}"
        },
        {
            "id": "ddad46d7",
            "name": "USB Log",
            "code": "// @ts-nocheck\n\n// 1. Nativer Logcat Bypass: Wir umgehen die Python-Konsole f\u00fcr absolute Sicherheit!\nconst androidLog = new NativeFunction(\n    Module.getExportByName(\"liblog.so\", \"__android_log_print\"),\n    \"int\", [\"int\", \"pointer\", \"pointer\"]\n);\nconst tag = Memory.allocUtf8String(\"KIPPY-FRIDA\");\nconst msg = Memory.allocUtf8String(\"!!! NATIVE RASP HUNTER IST ZU 100% AKTIV UND L\u00c4UFT !!!\");\nandroidLog(4, tag, msg); // 4 = INFO\n\n// 2. Dateizugriffe (I/O) \u00fcberwachen\nconst openPtr = Module.findExportByName(\"libc.so\", \"open\");\nconst openatPtr = Module.findExportByName(\"libc.so\", \"openat\");\n\nfunction hookFileAccess(ptr, pathIdx) {\n    if (!ptr) return;\n    Interceptor.attach(ptr, {\n        onEnter: function (args) {\n            try {\n                this.path = args[pathIdx].readUtf8String();\n                if (!this.path) return;\n\n                if (this.path.includes(\"/proc/\") || \n                    this.path.includes(\"base.apk\") || \n                    this.path.includes(\"frida\") ||\n                    this.path.includes(\"magisk\") ||\n                    this.path.includes(\"su\")) {\n                    \n                    // Sende an Python Konsole\n                    console.log(\"[RASP-SCAN] \" + this.path);\n                    \n                    // Schreibe parallel in nativen Logcat!\n                    let cPath = Memory.allocUtf8String(\"SCAN DETECTED: \" + this.path);\n                    androidLog(4, tag, cPath);\n                }\n            } catch (e) {}\n        }\n    });\n}\n\nhookFileAccess(openPtr, 0);\nhookFileAccess(openatPtr, 1);\n\n// 3. Das Laden der RASP-Bibliothek abfangen\nconst dlopenPtr = Module.findExportByName(null, \"android_dlopen_ext\") || Module.findExportByName(null, \"dlopen\");\nif (dlopenPtr) {\n    Interceptor.attach(dlopenPtr, {\n        onEnter: function (args) {\n            try {\n                this.libName = args[0].readUtf8String();\n            } catch (e) {}\n        },\n        onLeave: function (retval) {\n            if (this.libName && this.libName.includes(\"libdb0c.so\")) {\n                let loadMsg = Memory.allocUtf8String(\"!!! RASP PROTEKTOR (libdb0c.so) WIRD GELADEN !!! Base: \" + retval);\n                androidLog(4, tag, loadMsg);\n            }\n        }\n    });\n}"
        },
        {
            "id": "96100c2c",
            "name": "Android log",
            "code": "// @ts-nocheck\n\n// --- 1. Nativer Logcat Bypass ---\n// Greift direkt auf die System-C-API zu. Schreibt sofort, \u00fcberlebt jeden Crash.\nconst androidLog = new NativeFunction(\n    Module.getExportByName(\"liblog.so\", \"__android_log_print\"),\n    \"int\", [\"int\", \"pointer\", \"pointer\"]\n);\nconst tag = Memory.allocUtf8String(\"KIPPY-FRIDA\");\n\nfunction logToLogcat(msgString) {\n    let msg = Memory.allocUtf8String(msgString);\n    androidLog(4, tag, msg); // 4 = INFO\n}\n\nlogToLogcat(\"[*] Native RASP Hunter gestartet.\");\n\n\n// --- 2. Dateizugriffe (I/O) \u00fcberwachen ---\nconst openPtr = Module.findExportByName(\"libc.so\", \"open\");\nconst openatPtr = Module.findExportByName(\"libc.so\", \"openat\");\n\nfunction hookFileAccess(ptr, pathIdx) {\n    if (!ptr) return;\n    Interceptor.attach(ptr, {\n        onEnter: function (args) {\n            try {\n                this.path = args[pathIdx].readUtf8String();\n                if (!this.path) return;\n\n                if (this.path.includes(\"/proc/\") || \n                    this.path.includes(\"base.apk\") || \n                    this.path.includes(\"frida\") ||\n                    this.path.includes(\"magisk\") ||\n                    this.path.includes(\"su\")) {\n                    \n                    logToLogcat(\"[RASP-SCAN] Dateizugriff erkannt: \" + this.path);\n                }\n            } catch (e) {}\n        }\n    });\n}\n\nhookFileAccess(openPtr, 0);   \nhookFileAccess(openatPtr, 1); \n\n\n// --- 3. Das Laden der RASP-Bibliothek abfangen ---\nconst dlopenPtr = Module.findExportByName(null, \"android_dlopen_ext\") || Module.findExportByName(null, \"dlopen\");\nif (dlopenPtr) {\n    Interceptor.attach(dlopenPtr, {\n        onEnter: function (args) {\n            try {\n                this.libName = args[0].readUtf8String();\n            } catch (e) {}\n        },\n        onLeave: function (retval) {\n            if (this.libName && this.libName.includes(\"libdb0c.so\")) {\n                logToLogcat(\"[!] RASP PROTEKTOR (libdb0c.so) IN DEN RAM GELADEN! Base Address: \" + retval);\n            }\n        }\n    });\n}"
        },
        {
            "id": "1f01687b",
            "name": "TryCatch",
            "code": "// @ts-nocheck\n\ntry {\n    // 1. Sichere Initialisierung des nativen Loggers\n    const logPtr = Module.findExportByName(\"liblog.so\", \"__android_log_print\");\n    let androidLog = null;\n    if (logPtr) {\n        androidLog = new NativeFunction(logPtr, \"int\", [\"int\", \"pointer\", \"pointer\"]);\n    }\n    const tag = Memory.allocUtf8String(\"KIPPY-FRIDA\");\n\n    // Dual-Logging: Sendet an GUI und nativen Logcat!\n    function logToAll(msgString) {\n        send(msgString); // send() ist Fridas interne API (viel robuster als console.log)\n        if (androidLog) {\n            try {\n                let msg = Memory.allocUtf8String(msgString);\n                androidLog(4, tag, msg); // 4 = INFO\n            } catch (e) {}\n        }\n    }\n\n    logToAll(\"[*] Native RASP Hunter erfolgreich initialisiert! Keine Abst\u00fcrze.\");\n\n    // 2. I/O \u00dcberwachung\n    const openPtr = Module.findExportByName(\"libc.so\", \"open\");\n    const openatPtr = Module.findExportByName(\"libc.so\", \"openat\");\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) {\n            logToAll(\"[!] Pointer f\u00fcr \" + funcName + \" nicht gefunden!\");\n            return;\n        }\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    if (!this.path) return;\n\n                    if (this.path.includes(\"/proc/\") || \n                        this.path.includes(\"base.apk\") || \n                        this.path.includes(\"frida\") ||\n                        this.path.includes(\"magisk\") ||\n                        this.path.includes(\"su\")) {\n                        \n                        logToAll(\"[RASP-SCAN] \" + this.path);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");   \n    hookFileAccess(openatPtr, 1, \"openat\"); \n\n    // 3. Bibliotheken Lade-\u00dcberwachung\n    const dlopenPtr = Module.findExportByName(null, \"android_dlopen_ext\") || Module.findExportByName(null, \"dlopen\");\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try {\n                    this.libName = args[0].readUtf8String();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.libName && this.libName.includes(\"libdb0c.so\")) {\n                    logToAll(\"[!] RASP PROTEKTOR GELADEN! Base: \" + retval);\n                }\n            }\n        });\n    } else {\n        logToAll(\"[!] dlopen Pointer nicht gefunden!\");\n    }\n\n} catch (globalError) {\n    // Wenn alles kaputt geht, fangen wir den Fehler hier ab!\n    send(\"[FATAL SCRIPT ERROR] \" + globalError.message + \"\\n\" + globalError.stack);\n}"
        },
        {
            "id": "38300808",
            "name": "FunctionCall",
            "code": "// @ts-nocheck\n\nsetImmediate(function() {\n    send(\"[*] Native RASP Hunter gestartet (Bulletproof Mode)...\");\n\n    try {\n        // 1. Dateizugriffe (I/O) \u00fcberwachen\n        var openPtr = Module.findExportByName(\"libc.so\", \"open\");\n        var openatPtr = Module.findExportByName(\"libc.so\", \"openat\");\n\n        function hookFileAccess(ptr, pathIdx, funcName) {\n            if (!ptr) return;\n            Interceptor.attach(ptr, {\n                onEnter: function (args) {\n                    try {\n                        var path = args[pathIdx].readUtf8String();\n                        if (!path) return;\n\n                        // Nutze indexOf statt includes (ES5 kompatibel - verhindert TypeError!)\n                        if (path.indexOf(\"/proc/\") !== -1 || \n                            path.indexOf(\"base.apk\") !== -1 || \n                            path.indexOf(\"frida\") !== -1 ||\n                            path.indexOf(\"magisk\") !== -1 ||\n                            path.indexOf(\"su\") !== -1) {\n                            \n                            // Send feuert die Nachricht direkt in dein Python Framework!\n                            send(\"[RASP-SCAN] \" + funcName + \"() scannt: \" + path);\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n\n        hookFileAccess(openPtr, 0, \"open\");   \n        hookFileAccess(openatPtr, 1, \"openat\"); \n\n        // 2. Das Laden der RASP-Bibliothek abfangen\n        var dlopenPtr = Module.findExportByName(null, \"android_dlopen_ext\") || Module.findExportByName(null, \"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try {\n                        this.libName = args[0].readUtf8String();\n                    } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                        send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Base: \" + retval);\n                    }\n                }\n            });\n        }\n\n        send(\"[+] Alle Hooks erfolgreich gesetzt! Warte auf RASP...\");\n\n    } catch (e) {\n        send(\"[FATAL SCRIPT ERROR] \" + e.message);\n    }\n});"
        },
        {
            "id": "31f768cc",
            "name": "Simple",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] FRIDA Native RASP Hunter gestartet (Bulletproof Mode)...\");\n\n    // 1. Dateizugriffe (I/O) \u00fcberwachen\n    var openPtr = Module.findExportByName(\"libc.so\", \"open\");\n    var openatPtr = Module.findExportByName(\"libc.so\", \"openat\");\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var path = args[pathIdx].readUtf8String();\n                    if (!path) return;\n\n                    // Klassisches indexOf statt modernes includes\n                    if (path.indexOf(\"/proc/\") !== -1 || \n                        path.indexOf(\"base.apk\") !== -1 || \n                        path.indexOf(\"frida\") !== -1 ||\n                        path.indexOf(\"magisk\") !== -1 ||\n                        path.indexOf(\"su\") !== -1) {\n                        \n                        send(\"[RASP-SCAN] \" + funcName + \"() scannt: \" + path);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");   \n    hookFileAccess(openatPtr, 1, \"openat\"); \n\n    // 2. Das Laden der RASP-Bibliothek abfangen\n    var dlopenPtr = Module.findExportByName(null, \"android_dlopen_ext\") || Module.findExportByName(null, \"dlopen\");\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try {\n                    this.libName = args[0].readUtf8String();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                    send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Base: \" + retval);\n                }\n            }\n        });\n    }\n\n    send(\"[+] Alle Hooks erfolgreich gesetzt! Warte auf RASP...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "5a409624",
            "name": "SimpleFix",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Frida 0001 Native RASP Hunter gestartet (Global Mode)...\");\n\n    // DER FIX: Wir zwingen den Compiler, das globale Frida-Objekt zu nehmen, \n    // anstatt sein internes Node.js 'module' zu verwenden!\n    var FridaModule = globalThis.Module || global.Module;\n\n    // 1. Dateizugriffe (I/O) \u00fcberwachen\n    var openPtr = FridaModule.findExportByName(\"libc.so\", \"open\");\n    var openatPtr = FridaModule.findExportByName(\"libc.so\", \"openat\");\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var path = args[pathIdx].readUtf8String();\n                    if (!path) return;\n\n                    if (path.indexOf(\"/proc/\") !== -1 || \n                        path.indexOf(\"base.apk\") !== -1 || \n                        path.indexOf(\"frida\") !== -1 ||\n                        path.indexOf(\"magisk\") !== -1 ||\n                        path.indexOf(\"su\") !== -1) {\n                        \n                        send(\"[RASP-SCAN] \" + funcName + \"() scannt: \" + path);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");   \n    hookFileAccess(openatPtr, 1, \"openat\"); \n\n    // 2. Das Laden der RASP-Bibliothek abfangen\n    var dlopenPtr = FridaModule.findExportByName(null, \"android_dlopen_ext\") || FridaModule.findExportByName(null, \"dlopen\");\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try {\n                    this.libName = args[0].readUtf8String();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                    send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Base: \" + retval);\n                }\n            }\n        });\n    }\n\n    send(\"[+] Alle Hooks erfolgreich gesetzt! Warte auf RASP...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "36d09aee",
            "name": "Debug",
            "code": "// @ts-nocheck\nsend(\"[TRACE] Skript gestartet. Beginne Diagnose...\");\n\ntry {\n    send(\"[TRACE] Schritt 1: Hole echtes Frida-Module-Objekt...\");\n    // Der (1, eval) Trick zwingt die JS-Engine, den globalen Scope zu nutzen.\n    // Das verhindert, dass der Node-Compiler sein eigenes \"module\" reinschmuggelt.\n    var realModule = (1, eval)('Module');\n    send(\"[TRACE] Schritt 1 ERFOLGREICH. realModule Typ: \" + typeof realModule);\n\n    send(\"[TRACE] Schritt 2: Suche openPtr in libc.so...\");\n    var openPtr = realModule.findExportByName(\"libc.so\", \"open\");\n    send(\"[TRACE] Schritt 2 ERFOLGREICH. openPtr: \" + openPtr);\n\n    send(\"[TRACE] Schritt 3: Suche openatPtr in libc.so...\");\n    var openatPtr = realModule.findExportByName(\"libc.so\", \"openat\");\n    send(\"[TRACE] Schritt 3 ERFOLGREICH. openatPtr: \" + openatPtr);\n\n    // Hilfsfunktion deklarieren\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        send(\"[TRACE] Setze Hook f\u00fcr \" + funcName + \"...\");\n        if (!ptr) {\n            send(\"[TRACE] \" + funcName + \" Pointer ist null. \u00dcberspringe.\");\n            return;\n        }\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var path = args[pathIdx].readUtf8String();\n                    if (!path) return;\n                    if (path.indexOf(\"/proc/\") !== -1 || \n                        path.indexOf(\"base.apk\") !== -1 || \n                        path.indexOf(\"frida\") !== -1 ||\n                        path.indexOf(\"libdb0c\") !== -1) {\n                        send(\"[RASP-SCAN] \" + funcName + \"() liest: \" + path);\n                    }\n                } catch (e) {}\n            }\n        });\n        send(\"[TRACE] Hook f\u00fcr \" + funcName + \" erfolgreich gesetzt.\");\n    }\n\n    send(\"[TRACE] Schritt 4: F\u00fchre Hook f\u00fcr open() aus...\");\n    hookFileAccess(openPtr, 0, \"open\");\n\n    send(\"[TRACE] Schritt 5: F\u00fchre Hook f\u00fcr openat() aus...\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n\n    send(\"[TRACE] Schritt 6: Suche dlopen() Pointer...\");\n    var dlopenPtr = realModule.findExportByName(null, \"android_dlopen_ext\") || realModule.findExportByName(null, \"dlopen\");\n    send(\"[TRACE] Schritt 6 ERFOLGREICH. dlopenPtr: \" + dlopenPtr);\n\n    if (dlopenPtr) {\n        send(\"[TRACE] Schritt 7: Setze dlopen() Hook...\");\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libName = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                    send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Base: \" + retval);\n                }\n            }\n        });\n        send(\"[TRACE] Schritt 7 ERFOLGREICH.\");\n    }\n\n    send(\"[+] DIAGNOSE BEENDET: Alle Hooks erfolgreich gesetzt! Warte auf RASP...\");\n\n} catch (e) {\n    // Falls etwas crasht, senden wir den Stacktrace mit!\n    send(\"[FATAL SCRIPT ERROR] \" + e.name + \": \" + e.message + \"\\n\" + e.stack);\n}"
        },
        {
            "id": "7d2e8922",
            "name": "processAPI",
            "code": "// @ts-nocheck\nsend(\"[TRACE] 0002 Skript gestartet. Nutze Process-API anstelle von Module-API...\");\n\ntry {\n    send(\"[TRACE] Schritt 1: Hole libc.so \u00fcber Process.findModuleByName...\");\n    // Nutzt \"Process\" (Frida) anstatt \"Module\" (wird von Node.js blockiert)\n    var libc = Process.findModuleByName(\"libc.so\");\n    send(\"[TRACE] Schritt 1 ERFOLGREICH. libc gefunden: \" + (libc !== null));\n\n    var openPtr = null;\n    var openatPtr = null;\n    if (libc !== null) {\n        send(\"[TRACE] Schritt 2: Suche openPtr in libc.so...\");\n        openPtr = libc.findExportByName(\"open\");\n        send(\"[TRACE] Schritt 2 ERFOLGREICH. openPtr: \" + openPtr);\n\n        send(\"[TRACE] Schritt 3: Suche openatPtr in libc.so...\");\n        openatPtr = libc.findExportByName(\"openat\");\n        send(\"[TRACE] Schritt 3 ERFOLGREICH. openatPtr: \" + openatPtr);\n    } else {\n        send(\"[!] libc.so nicht gefunden!\");\n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        send(\"[TRACE] Setze Hook f\u00fcr \" + funcName + \"...\");\n        if (!ptr) {\n            send(\"[TRACE] \" + funcName + \" Pointer ist null. \u00dcberspringe.\");\n            return;\n        }\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var path = args[pathIdx].readUtf8String();\n                    if (!path) return;\n                    if (path.indexOf(\"/proc/\") !== -1 || \n                        path.indexOf(\"base.apk\") !== -1 || \n                        path.indexOf(\"frida\") !== -1 ||\n                        path.indexOf(\"libdb0c\") !== -1 ||\n                        path.indexOf(\"su\") !== -1 ||\n                        path.indexOf(\"magisk\") !== -1) {\n                        \n                        send(\"[RASP-SCAN] \" + funcName + \"() liest: \" + path);\n                    }\n                } catch (e) {}\n            }\n        });\n        send(\"[TRACE] Hook f\u00fcr \" + funcName + \" erfolgreich gesetzt.\");\n    }\n\n    send(\"[TRACE] Schritt 4: F\u00fchre Hook f\u00fcr open() aus...\");\n    hookFileAccess(openPtr, 0, \"open\");\n\n    send(\"[TRACE] Schritt 5: F\u00fchre Hook f\u00fcr openat() aus...\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n\n    send(\"[TRACE] Schritt 6: Hole libdl.so f\u00fcr dlopen...\");\n    var dlopenPtr = null;\n    var libdl = Process.findModuleByName(\"libdl.so\");\n    \n    if (libdl !== null) {\n        dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        send(\"[TRACE] Schritt 6 ERFOLGREICH. dlopenPtr: \" + dlopenPtr);\n    } else {\n        send(\"[!] libdl.so nicht gefunden!\");\n    }\n\n    if (dlopenPtr) {\n        send(\"[TRACE] Schritt 7: Setze dlopen() Hook...\");\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libName = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                    send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Base: \" + retval);\n                }\n            }\n        });\n        send(\"[TRACE] Schritt 7 ERFOLGREICH.\");\n    }\n\n    send(\"[+] DIAGNOSE BEENDET: Alle Hooks erfolgreich gesetzt! Warte auf RASP...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.name + \": \" + e.message + \"\\n\" + e.stack);\n}"
        },
        {
            "id": "25dfa853",
            "name": "counter",
            "code": "// @ts-nocheck\nsend(\"[*] 0003 Native RASP Hunter & I/O Spoofer gestartet...\");\n\ntry {\n    var FridaModule = globalThis.Module || global.Module;\n    var libc = Process.findModuleByName(\"libc.so\");\n\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    // Hier speichern wir die File-Deskriptoren (FDs), die der RASP \u00f6ffnet\n    var trackedFds = {};\n\n    function hookOpen(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n\n                    if (this.path && this.path.indexOf(\"/proc/self/status\") !== -1) {\n                        this.isStatusFile = true;\n                        send(\"[RASP-SCAN] \" + funcName + \"() \u00f6ffnet: \" + this.path);\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                // Wenn es die Status-Datei war, merken wir uns die Datei-ID (FD)\n                if (this.isStatusFile) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = true;\n                        send(\"[TRACE] Verfolge Lesezugriffe auf FD: \" + fd);\n                    }\n                }\n            }\n        });\n    }\n\n    hookOpen(openPtr, 0, \"open\");\n    hookOpen(openatPtr, 1, \"openat\");\n\n    // 4. Der Magische Read-Hook: Wir f\u00e4lschen die Daten im Flug!\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                // Wenn aus der Status-Datei gelesen wird...\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        // Wir suchen nach TracerPid (egal welche Zahl)\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            // Und ersetzen sie durch TracerPid: 0\n                            var spoofed = content.replace(/TracerPid:\\s*\\d+/g, \"TracerPid:\\t0\");\n                            this.buffer.writeUtf8String(spoofed);\n                            send(\"[\ud83d\udd25 SPOOFER] TracerPid in /proc/self/status erfolgreich auf 0 gef\u00e4lscht!\");\n                        }\n                    } catch (e) {\n                        // Ignoriere Parsing-Fehler bei bin\u00e4ren Reads\n                    }\n                }\n            }\n        });\n        send(\"[+] I/O Spoofer (read) aktiv!\");\n    }\n\n    var dlopenPtr = FridaModule.findExportByName(null, \"android_dlopen_ext\") || FridaModule.findExportByName(null, \"dlopen\");\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libName = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                    send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Let the games begin.\");\n                }\n            }\n        });\n    }\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "77b8ceaa",
            "name": "counterOrig",
            "code": "// @ts-nocheck\nsend(\"[*] Native RASP Hunter & I/O Spoofer gestartet...\");\n\ntry {\n    var FridaModule = globalThis.Module || global.Module;\n    var libc = Process.findModuleByName(\"libc.so\");\n\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    // Hier speichern wir die File-Deskriptoren (FDs), die der RASP \u00f6ffnet\n    var trackedFds = {};\n\n    function hookOpen(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n\n                    if (this.path && this.path.indexOf(\"/proc/self/status\") !== -1) {\n                        this.isStatusFile = true;\n                        send(\"[RASP-SCAN] \" + funcName + \"() \u00f6ffnet: \" + this.path);\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                // Wenn es die Status-Datei war, merken wir uns die Datei-ID (FD)\n                if (this.isStatusFile) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = true;\n                        send(\"[TRACE] Verfolge Lesezugriffe auf FD: \" + fd);\n                    }\n                }\n            }\n        });\n    }\n\n    hookOpen(openPtr, 0, \"open\");\n    hookOpen(openatPtr, 1, \"openat\");\n\n    // 4. Der Magische Read-Hook: Wir f\u00e4lschen die Daten im Flug!\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                // Wenn aus der Status-Datei gelesen wird...\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        // Wir suchen nach TracerPid (egal welche Zahl)\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            // Und ersetzen sie durch TracerPid: 0\n                            var spoofed = content.replace(/TracerPid:\\s*\\d+/g, \"TracerPid:\\t0\");\n                            this.buffer.writeUtf8String(spoofed);\n                            send(\"[\ud83d\udd25 SPOOFER] TracerPid in /proc/self/status erfolgreich auf 0 gef\u00e4lscht!\");\n                        }\n                    } catch (e) {\n                        // Ignoriere Parsing-Fehler bei bin\u00e4ren Reads\n                    }\n                }\n            }\n        });\n        send(\"[+] I/O Spoofer (read) aktiv!\");\n    }\n\n    var dlopenPtr = FridaModule.findExportByName(null, \"android_dlopen_ext\") || FridaModule.findExportByName(null, \"dlopen\");\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libName = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                    send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Let the games begin.\");\n                }\n            }\n        });\n    }\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "72e7e5be",
            "name": "Dual",
            "code": "// @ts-nocheck\nsend(\"[*] 0004 Native RASP Hunter & Dual-Spoofer gestartet...\");\n\ntry {\n    var FridaModule = globalThis.Module || global.Module;\n    var libc = Process.findModuleByName(\"libc.so\");\n\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    var trackedFds = {};\n\n    function hookOpen(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n\n                    if (path) {\n                        // 1. Check auf Debugger-Erkennung\n                        if (path.indexOf(\"/proc/self/status\") !== -1) {\n                            this.isStatusFile = true;\n                        }\n                        // 2. Check auf APK-Integrit\u00e4tspr\u00fcfung!\n                        // Wenn er die base.apk lesen will, lenken wir ihn auf unsere saubere Kopie um.\n                        else if (path.indexOf(\"base.apk\") !== -1 && path.indexOf(\"clean_base.apk\") === -1) {\n                            // Der NEUE Pfad zu unserem versteckten Original auf der SD-Karte\n                            var cleanPath = \"/sdcard/Android/data/org.nativescript.LibreLinkUp/files/clean_base.apk\";\n                            \n                            // Wir \u00fcberschreiben den Speicherbereich des Pfad-Arguments VOR Ausf\u00fchrung!\n                            args[pathIdx] = Memory.allocUtf8String(cleanPath);\n                            send(\"[\ud83d\udd25 SPOOFER] \" + funcName + \"() von base.apk auf Original umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                // FD (File Descriptor) speichern f\u00fcr den Read-Spoofer\n                if (this.isStatusFile) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = true;\n                    }\n                }\n            }\n        });\n    }\n\n    hookOpen(openPtr, 0, \"open\");\n    hookOpen(openatPtr, 1, \"openat\");\n\n    // Der Read-Hook f\u00e4lscht die TracerPid im RAM\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            var spoofed = content.replace(/TracerPid:\\s*\\d+/g, \"TracerPid:\\t0\");\n                            this.buffer.writeUtf8String(spoofed);\n                            send(\"[\ud83d\udd25 SPOOFER] TracerPid in /proc/self/status erfolgreich auf 0 gef\u00e4lscht!\");\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n\n    var dlopenPtr = FridaModule.findExportByName(null, \"android_dlopen_ext\") || FridaModule.findExportByName(null, \"dlopen\");\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libName = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                    send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Let the games begin.\");\n                }\n            }\n        });\n    }\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "f6b6ef47",
            "name": "DualSniper",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] 0006 Native RASP Hunter & Targeted Dual-Spoofer gestartet...\");\n\n    // FIX f\u00fcr \"not a function\": Wir nutzen Process statt Module!\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    var trackedFds = {};\n    var raspLoaded = false; // Wird true, sobald libdb0c.so ins Spiel kommt\n\n    function hookOpen(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n                    this.isRaspCaller = false;\n\n                    if (!this.path) return;\n\n                    // 1. Pr\u00fcfen, ob der Aufruf aus dem RASP kommt\n                    var callerMod = Process.findModuleByAddress(this.returnAddress);\n                    if (callerMod && callerMod.name.indexOf(\"libdb0c\") !== -1) {\n                        this.isRaspCaller = true;\n                    }\n\n                    // 2. Debugger-Check abfangen\n                    if (this.path.indexOf(\"/proc/self/status\") !== -1) {\n                        this.isStatusFile = true;\n                    }\n\n                    // 3. APK Redirection - ABER NUR F\u00dcR DEN RASP!\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        \n                        // Wir leiten NUR um, wenn der RASP fragt (oder geladen ist).\n                        // Das l\u00e4sst den Android ClassLoader in Ruhe seine Arbeit machen!\n                        if (this.isRaspCaller || raspLoaded) {\n                            var cleanPath = \"/sdcard/Android/data/org.nativescript.LibreLinkUp/files/clean_base.apk\";\n                            args[pathIdx] = Memory.allocUtf8String(cleanPath);\n                            send(\"[\ud83d\udd25 SPOOFER] \" + funcName + \"() gezielt f\u00fcr RASP auf Original-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isStatusFile) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = true;\n                    }\n                }\n            }\n        });\n    }\n\n    hookOpen(openPtr, 0, \"open\");\n    hookOpen(openatPtr, 1, \"openat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            var spoofed = content.replace(/TracerPid:\\s*\\d+/g, \"TracerPid:\\t0\");\n                            this.buffer.writeUtf8String(spoofed);\n                            send(\"[\ud83d\udd25 SPOOFER] TracerPid gef\u00e4lscht!\");\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n\n    // Abfangen des Ladevorgangs (um raspLoaded auf true zu setzen)\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { this.libName = args[0].readUtf8String(); } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                        raspLoaded = true; // Ab jetzt greift die APK Umleitung!\n                        send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Tarnkappe aktiviert.\");\n                    }\n                }\n            });\n        }\n    }\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "11e229ee",
            "name": "DualV2",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & Sniper-Spoofer gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    var trackedFds = {};\n    \n    // FIX: Wir reservieren den Speicher f\u00fcr den String dauerhaft, \n    // damit der Kernel den Pfad in Ruhe lesen kann!\n    var fakePathMem = Memory.allocUtf8String(\"/storage/emulated/0/Android/data/org.nativescript.LibreLinkUp/files/clean_base.apk\");\n\n    function hookOpen(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n\n                    if (!this.path) return;\n\n                    // 1. Debugger-Check abfangen\n                    if (this.path.indexOf(\"/proc/self/status\") !== -1) {\n                        this.isStatusFile = true;\n                    }\n                    \n                    // 2. APK Redirection - ABER NUR F\u00dcR DEN RASP!\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        \n                        // SNIPER CHECK: Wir scannen den kompletten Callstack der CPU!\n                        var isRasp = false;\n                        var backtrace = Thread.backtrace(this.context, Backtracer.ACCURATE);\n                        for (var i = 0; i < backtrace.length; i++) {\n                            var mod = Process.findModuleByAddress(backtrace[i]);\n                            if (mod && mod.name.indexOf(\"libdb0c\") !== -1) {\n                                isRasp = true;\n                                break;\n                            }\n                        }\n\n                        // Nur wenn libdb0c.so im Stack steht, f\u00e4lschen wir den Pfad!\n                        // Androids Classloader wird hier einfach ignoriert und darf das Original laden.\n                        if (isRasp) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() gezielt f\u00fcr RASP auf Original-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isStatusFile) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = true;\n                    }\n                }\n            }\n        });\n    }\n\n    hookOpen(openPtr, 0, \"open\");\n    hookOpen(openatPtr, 1, \"openat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            var spoofed = content.replace(/TracerPid:\\s*\\d+/g, \"TracerPid:\\t0\");\n                            this.buffer.writeUtf8String(spoofed);\n                            send(\"[\ud83d\udd25 SPOOFER] TracerPid gef\u00e4lscht!\");\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { this.libName = args[0].readUtf8String(); } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                        send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Sniper-Wache aktiv.\");\n                    }\n                }\n            });\n        }\n    }\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "866079d9",
            "name": "DualV3",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & Stealth-Sniper gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    var trackedFds = {};\n    \n    // Unzerst\u00f6rbarer globaler Speicher f\u00fcr den Pfad zur Welt-lesbaren Datei\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    \n    // Liste legitimer Android-Systembibliotheken, die APKs laden d\u00fcrfen\n    var knownSys = [\"libart.so\", \"libziparchive.so\", \"libandroidfw.so\", \"libbase.so\", \"libnativeloader.so\", \"libjavacore.so\", \"libopenjdk.so\"];\n\n    function hookOpen(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n\n                    if (!this.path) return;\n\n                    // 1. Debugger-Check abfangen\n                    if (this.path.indexOf(\"/proc/self/status\") !== -1) {\n                        this.isStatusFile = true;\n                    }\n                    \n                    // 2. APK Hash-Check umleiten (Nur f\u00fcr RASP!)\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        \n                        var isRasp = true; // Wir nehmen an, es ist der RASP (oder ein Geist)\n                        var backtrace = Thread.backtrace(this.context, Backtracer.ACCURATE);\n                        \n                        // Wir scannen den Callstack nach dem echten Android-System\n                        for (var i = 0; i < backtrace.length; i++) {\n                            var mod = Process.findModuleByAddress(backtrace[i]);\n                            if (mod) {\n                                var n = mod.name.toLowerCase();\n                                for (var j = 0; j < knownSys.length; j++) {\n                                    if (n.indexOf(knownSys[j]) !== -1) {\n                                        isRasp = false; // Entwarnung! Es ist das legitime Android-System.\n                                        break;\n                                    }\n                                }\n                            }\n                            if (!isRasp) break;\n                        }\n\n                        // Ist es nicht das Android-System, feuern wir die Original-APK ab!\n                        if (isRasp) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() von RASP auf Original-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isStatusFile) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = true;\n                    }\n                }\n            }\n        });\n    }\n\n    hookOpen(openPtr, 0, \"open\");\n    hookOpen(openatPtr, 1, \"openat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            var spoofed = content.replace(/TracerPid:\\s*\\d+/g, \"TracerPid:\\t0\");\n                            this.buffer.writeUtf8String(spoofed);\n                            send(\"[\ud83d\udd25 SPOOFER] TracerPid gef\u00e4lscht!\");\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Einschlag...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "03739d93",
            "name": "DualV4",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & Direct-Caller Sniper gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    var trackedFds = {};\n    \n    // Unzerst\u00f6rbarer globaler Speicher f\u00fcr den Pfad zur Welt-lesbaren Datei\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    \n    function checkRaspCaller(retAddr) {\n        var isRasp = false;\n        var callerMod = Process.findModuleByAddress(retAddr);\n        \n        if (!callerMod) {\n            // Anonymous memory (Ghost/JIT) - typisch f\u00fcr entpackten RASP-Code!\n            isRasp = true;\n        } else {\n            var n = callerMod.name.toLowerCase();\n            if (n.indexOf(\"libdb0c\") !== -1) {\n                // Direkter Aufruf aus der RASP-Bibliothek\n                isRasp = true;\n            }\n        }\n        return isRasp;\n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n\n                    if (!this.path) return;\n\n                    // 1. Debugger-Check abfangen\n                    if (this.path.indexOf(\"/proc/self/status\") !== -1) {\n                        this.isStatusFile = true;\n                    }\n                    \n                    // 2. APK Hash/Size-Check umleiten (Nur f\u00fcr RASP!)\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        \n                        // Checkt NUR den direkten Aufrufer (ignoriert den restlichen JNI-Stack)\n                        var isRasp = checkRaspCaller(this.returnAddress);\n\n                        if (isRasp) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() von RASP (Ghost/Modul) auf Original-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                // Nur open/openat geben File-Deskriptoren zur\u00fcck, stat nicht.\n                if (this.isStatusFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = true;\n                    }\n                }\n            }\n        });\n    }\n\n    // Wir blockieren jetzt ALLES, was Dateien \u00f6ffnet oder ausmisst\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    // TracerPid Spoofer\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            var spoofed = content.replace(/TracerPid:\\s*\\d+/g, \"TracerPid:\\t0\");\n                            this.buffer.writeUtf8String(spoofed);\n                            send(\"[\ud83d\udd25 SPOOFER] TracerPid in /proc/self/status gef\u00e4lscht!\");\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { this.libName = args[0].readUtf8String(); } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                        send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Wache aktiv.\");\n                    }\n                }\n            });\n        }\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Einschlag...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "8e264171",
            "name": "StealthSniper",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & Ultimate Stealth-Sniper gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n    var readlinkPtr = libc.findExportByName(\"readlink\");\n    var readlinkatPtr = libc.findExportByName(\"readlinkat\");\n\n    var trackedFds = {};\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    \n    // Die perfekte Callstack-Analyse\n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            \n            // 1. Ghost Code (Userfaultfd) -> 100% RASP\n            if (!mod) return true;\n            \n            var n = mod.name.toLowerCase();\n            \n            // 2. libc / libdl / libm -> Wir springen einfach zum n\u00e4chsten Frame, da es nur Wrapper sind\n            if (n === \"libc.so\" || n === \"libdl.so\" || n === \"libm.so\" || n.indexOf(\"liblog\") !== -1) {\n                continue;\n            }\n            \n            // 3. Direkter RASP Call -> 100% RASP\n            if (n.indexOf(\"libdb0c\") !== -1 || n.indexOf(\"dexguard\") !== -1 || n.indexOf(\"promon\") !== -1) {\n                return true;\n            }\n            \n            // 4. Legitimes Android Framework -> 100% System (Kein RASP)\n            if (n.indexOf(\"libandroidfw\") !== -1 || \n                n.indexOf(\"libziparchive\") !== -1 || \n                n.indexOf(\"libjavacore\") !== -1 || \n                n.indexOf(\"libnativeloader\") !== -1 || \n                n.indexOf(\"libbase\") !== -1 ||\n                n.indexOf(\"libart\") !== -1) {\n                return false;\n            }\n        }\n        return true; // \"Guilty until proven innocent\" Fallback\n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n\n                    if (!this.path) return;\n\n                    // Debugger Check\n                    if (this.path.indexOf(\"/proc/self/status\") !== -1) {\n                        this.isStatusFile = true;\n                    }\n                    \n                    // APK Hash-Spoofer\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() von RASP auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isStatusFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = true;\n                    }\n                }\n            }\n        });\n    }\n\n    // Wir klemmen uns in alle Datei-Funktionen\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    // TracerPid Spoofer\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            var spoofed = content.replace(/TracerPid:\\s*\\d+/g, \"TracerPid:\\t0\");\n                            this.buffer.writeUtf8String(spoofed);\n                            send(\"[\ud83d\udd25 SPOOFER] TracerPid gef\u00e4lscht!\");\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    // Die readlink-Falle: Wenn der RASP den Kernel nach dem echten Pfad fragt, l\u00fcgen wir ihn an.\n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                            send(\"[\ud83d\udd25 SPOOFER] \" + funcName + \"() Tarnung aufrecht erhalten!\");\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n    \n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { this.libName = args[0].readUtf8String(); } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                        send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Wache aktiv.\");\n                    }\n                }\n            });\n        }\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Einschlag...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "59e81247",
            "name": "V5",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & GOD-MODE Spoofer gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n    var readlinkPtr = libc.findExportByName(\"readlink\");\n    var readlinkatPtr = libc.findExportByName(\"readlinkat\");\n\n    var trackedFds = {};\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    \n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true; // Ghost Code (Userfaultfd)\n            \n            var n = mod.name.toLowerCase();\n            if (n === \"libc.so\" || n === \"libdl.so\" || n === \"libm.so\" || n.indexOf(\"liblog\") !== -1) continue;\n            if (n.indexOf(\"libdb0c\") !== -1 || n.indexOf(\"dexguard\") !== -1) return true;\n            if (n.indexOf(\"libandroidfw\") !== -1 || n.indexOf(\"libziparchive\") !== -1 || \n                n.indexOf(\"libjavacore\") !== -1 || n.indexOf(\"libnativeloader\") !== -1 || \n                n.indexOf(\"libbase\") !== -1 || n.indexOf(\"libart\") !== -1) {\n                return false;\n            }\n        }\n        return true;\n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n\n                    if (!this.path) return;\n\n                    // Debugger & Memory-Map Check abfangen\n                    if (this.path.indexOf(\"/proc/\") !== -1 && (this.path.indexOf(\"status\") !== -1 || this.path.indexOf(\"maps\") !== -1)) {\n                        this.isTargetFile = true;\n                        this.fileName = this.path.indexOf(\"maps\") !== -1 ? \"maps\" : \"status\";\n                    }\n                    // APK Redirection\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() von RASP auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isTargetFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = this.fileName; // Wir merken uns, ob status oder maps gelesen wird\n                    }\n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    // Der absolut perfekte Read-Spoofer (Verhindert String-L\u00e4ngen-Crashes)\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                \n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                // PADDING: Unsichtbare Leerzeichen anh\u00e4ngen, damit die RAM-Gr\u00f6\u00dfe identisch bleibt!\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                                send(\"[\ud83d\udd25 SPOOFER] TracerPid l\u00e4ngenneutral gef\u00e4lscht!\");\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            // Ersetze frida durch xriba -> Die String-L\u00e4nge bleibt absolut gleich!\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\").replace(/xposed/g, \"yposed\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida in /proc/self/maps erfolgreich unsichtbar gemacht!\");\n                        }\n\n                        if (modified) {\n                            this.buffer.writeUtf8String(content);\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n    \n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { this.libName = args[0].readUtf8String(); } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                        send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Matrix-Tarnung aktiv.\");\n                    }\n                }\n            });\n        }\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Einschlag...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "e21d2567",
            "name": "V6",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & GOD-MODE Spoofer gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n    var readlinkPtr = libc.findExportByName(\"readlink\");\n    var readlinkatPtr = libc.findExportByName(\"readlinkat\");\n\n    var trackedFds = {};\n    \n    // DER FIX: Der korrekte Pfad zur echten Original-APK!\n    var fakePathMem = Memory.allocUtf8String(\"/sdcard/Android/data/org.nativescript.LibreLinkUp/files/clean_base.apk\");\n    \n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true; // Ghost Code (Userfaultfd)\n            \n            var n = mod.name.toLowerCase();\n            if (n === \"libc.so\" || n === \"libdl.so\" || n === \"libm.so\" || n.indexOf(\"liblog\") !== -1) continue;\n            if (n.indexOf(\"libdb0c\") !== -1 || n.indexOf(\"dexguard\") !== -1) return true;\n            if (n.indexOf(\"libandroidfw\") !== -1 || n.indexOf(\"libziparchive\") !== -1 || \n                n.indexOf(\"libjavacore\") !== -1 || n.indexOf(\"libnativeloader\") !== -1 || \n                n.indexOf(\"libbase\") !== -1 || n.indexOf(\"libart\") !== -1) {\n                return false;\n            }\n        }\n        return true;\n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n\n                    if (!this.path) return;\n\n                    // Debugger & Memory-Map Check abfangen\n                    if (this.path.indexOf(\"/proc/\") !== -1 && (this.path.indexOf(\"status\") !== -1 || this.path.indexOf(\"maps\") !== -1)) {\n                        this.isTargetFile = true;\n                        this.fileName = this.path.indexOf(\"maps\") !== -1 ? \"maps\" : \"status\";\n                    }\n                    // APK Redirection\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() von RASP auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isTargetFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) {\n                        trackedFds[fd] = this.fileName; \n                    }\n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                \n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                                send(\"[\ud83d\udd25 SPOOFER] TracerPid l\u00e4ngenneutral gef\u00e4lscht!\");\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\").replace(/xposed/g, \"yposed\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida in /proc/self/maps erfolgreich unsichtbar gemacht!\");\n                        }\n\n                        if (modified) {\n                            this.buffer.writeUtf8String(content);\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n    \n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { this.libName = args[0].readUtf8String(); } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    if (this.libName && this.libName.indexOf(\"libdb0c.so\") !== -1) {\n                        send(\"[!] RASP PROTEKTOR (libdb0c.so) GELADEN! Matrix-Tarnung aktiv.\");\n                    }\n                }\n            });\n        }\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Einschlag...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "9f4f02ec",
            "name": "V7",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] 0007 Native RASP Hunter & BULLETPROOF Sniper gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n    var readlinkPtr = libc.findExportByName(\"readlink\");\n    var readlinkatPtr = libc.findExportByName(\"readlinkat\");\n\n    var trackedFds = {};\n    var raspLoaded = false; // DIE WICHTIGSTE SICHERUNG!\n    \n    // Pfad zur sauberen App im offiziellen, lesbaren App-Verzeichnis\n    var fakePathMem = Memory.allocUtf8String(\"/sdcard/Android/data/org.nativescript.LibreLinkUp/files/clean_base.apk\");\n    \n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n\n                    if (!this.path) return;\n\n                    // 1. Debugger & Memory-Map Check (TracerPid & Frida-Erkennung)\n                    if (this.path.indexOf(\"/proc/\") !== -1 && (this.path.indexOf(\"status\") !== -1 || this.path.indexOf(\"maps\") !== -1)) {\n                        this.isTargetFile = true;\n                        this.fileName = this.path.indexOf(\"maps\") !== -1 ? \"maps\" : \"status\";\n                    }\n                    \n                    // 2. APK Redirection\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        \n                        // SICHERUNG 1: Ist der RASP \u00fcberhaupt schon im RAM? \n                        // Wenn nicht, lassen wir den Android Boot-Vorgang UNANGETASTET!\n                        if (!raspLoaded) return; \n\n                        // SICHERUNG 2: Wir scannen den Stack. Wenn Android-Basis-Bibliotheken\n                        // beteiligt sind (die den RASP nicht interessieren), lassen wir es durch.\n                        var isSystem = false;\n                        var bt = Thread.backtrace(this.context, Backtracer.ACCURATE);\n                        for (var i = 0; i < bt.length; i++) {\n                            var mod = Process.findModuleByAddress(bt[i]);\n                            if (mod) {\n                                var n = mod.name.toLowerCase();\n                                if (n.indexOf(\"libart\") !== -1 || n.indexOf(\"libjavacore\") !== -1 || \n                                    n.indexOf(\"libziparchive\") !== -1 || n.indexOf(\"libandroidfw\") !== -1 ||\n                                    n.indexOf(\"libbase\") !== -1 || n.indexOf(\"libopenjdk\") !== -1) {\n                                    isSystem = true;\n                                    break;\n                                }\n                            }\n                        }\n\n                        // Wenn es nicht das legitime Android-System ist, FEUER!\n                        if (!isSystem) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() gezielt f\u00fcr RASP auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isTargetFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) trackedFds[fd] = this.fileName; \n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                \n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                                send(\"[\ud83d\udd25 SPOOFER] TracerPid l\u00e4ngenneutral gef\u00e4lscht!\");\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\").replace(/xposed/g, \"yposed\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida in /proc/self/maps unsichtbar gemacht!\");\n                        }\n\n                        if (modified) {\n                            this.buffer.writeUtf8String(content);\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n    \n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { \n                        var libName = args[0].readUtf8String(); \n                        if (libName && libName.indexOf(\"libdb0c.so\") !== -1) {\n                            // HIER WIRD DIE SICHERUNG ENTSICHERT!\n                            raspLoaded = true;\n                            send(\"[!] RASP PROTEKTOR (libdb0c.so) WIRD GELADEN! Sniper scharfgestellt.\");\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Android System-Start...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "4c9a0433",
            "name": "V8",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & GOD-MODE Spoofer v2 gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n    var readlinkPtr = libc.findExportByName(\"readlink\");\n    var readlinkatPtr = libc.findExportByName(\"readlinkat\");\n\n    var trackedFds = {};\n    var raspLoaded = false; \n    var fakePathMem = Memory.allocUtf8String(\"/sdcard/Android/data/org.nativescript.LibreLinkUp/files/clean_base.apk\");\n    \n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true; \n            \n            var n = mod.name.toLowerCase();\n            if (n === \"libc.so\" || n === \"libdl.so\" || n === \"libm.so\" || n.indexOf(\"liblog\") !== -1) continue;\n            if (n.indexOf(\"libdb0c\") !== -1 || n.indexOf(\"dexguard\") !== -1) return true;\n            if (n.indexOf(\"libandroidfw\") !== -1 || n.indexOf(\"libziparchive\") !== -1 || \n                n.indexOf(\"libjavacore\") !== -1 || n.indexOf(\"libnativeloader\") !== -1 || \n                n.indexOf(\"libbase\") !== -1 || n.indexOf(\"libart\") !== -1 || n.indexOf(\"libopenjdk\") !== -1) {\n                return false;\n            }\n        }\n        return true;\n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n\n                    if (!this.path) return;\n\n                    // NEU: Erweiterter Scanner f\u00fcr Status, Maps, TCP-Ports und Threads\n                    if (this.path.indexOf(\"/proc/\") !== -1) {\n                        if (this.path.indexOf(\"status\") !== -1) { this.isTargetFile = true; this.fileName = \"status\"; }\n                        else if (this.path.indexOf(\"maps\") !== -1) { this.isTargetFile = true; this.fileName = \"maps\"; }\n                        else if (this.path.indexOf(\"net/tcp\") !== -1) { this.isTargetFile = true; this.fileName = \"tcp\"; }\n                        else if (this.path.indexOf(\"task\") !== -1) { this.isTargetFile = true; this.fileName = \"task\"; }\n                    }\n                    \n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (!raspLoaded) return; \n\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() gezielt f\u00fcr RASP auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isTargetFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) trackedFds[fd] = this.fileName; \n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                \n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                                send(\"[\ud83d\udd25 SPOOFER] TracerPid l\u00e4ngenneutral gef\u00e4lscht!\");\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\").replace(/xposed/g, \"yposed\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida in /proc/self/maps unsichtbar gemacht!\");\n                        }\n                        // NEU: TCP-Port 27042 (Hexadezimal: 69A2) aus der Netzwerktabelle l\u00f6schen\n                        else if (fType === \"tcp\" && content.indexOf(\"69A2\") !== -1) {\n                            content = content.replace(/.*69A2.*/g, \"\"); \n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida TCP-Port versteckt!\");\n                        }\n                        // NEU: Verr\u00e4terische Frida-Threads maskieren\n                        else if (fType === \"task\" && (content.indexOf(\"gum-js-loop\") !== -1 || content.indexOf(\"gmain\") !== -1)) {\n                            content = content.replace(/gum-js-loop/g, \"xum-js-loop\").replace(/gmain/g, \"xmain\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida Threads getarnt!\");\n                        }\n\n                        if (modified) {\n                            this.buffer.writeUtf8String(content);\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n    \n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { \n                        var libName = args[0].readUtf8String(); \n                        if (libName && libName.indexOf(\"libdb0c.so\") !== -1) {\n                            raspLoaded = true;\n                            send(\"[!] RASP PROTEKTOR (libdb0c.so) WIRD GELADEN! Sniper scharfgestellt.\");\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Android System-Start...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "efd67f3d",
            "name": "V9",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & ULTIMATE Sniper gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n    var readlinkPtr = libc.findExportByName(\"readlink\");\n    var readlinkatPtr = libc.findExportByName(\"readlinkat\");\n\n    var trackedFds = {};\n    var raspLoaded = false; \n    \n    // DER ENTSCHEIDENDE FIX: Der Pfad muss exakt mit dem Python-Push \u00fcbereinstimmen!\n    var fakePathMem = Memory.allocUtf8String(\"/sdcard/Android/data/org.nativescript.LibreLinkUp/files/clean_base.apk\");\n    \n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true; // Ghost Code (Userfaultfd) -> RASP\n            \n            var n = mod.name.toLowerCase();\n            if (n === \"libc.so\" || n === \"libdl.so\" || n === \"libm.so\" || n.indexOf(\"liblog\") !== -1) continue;\n            if (n.indexOf(\"libdb0c\") !== -1 || n.indexOf(\"dexguard\") !== -1) return true; // RASP\n            \n            // Wenn legitimes Android Framework involviert ist, lassen wir es durch!\n            if (n.indexOf(\"libandroidfw\") !== -1 || n.indexOf(\"libziparchive\") !== -1 || \n                n.indexOf(\"libjavacore\") !== -1 || n.indexOf(\"libnativeloader\") !== -1 || \n                n.indexOf(\"libbase\") !== -1 || n.indexOf(\"libart\") !== -1 || n.indexOf(\"libopenjdk\") !== -1) {\n                return false; \n            }\n        }\n        return true; // Fallback: \"Guilty until proven innocent\"\n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n\n                    if (!this.path) return;\n\n                    if (this.path.indexOf(\"/proc/\") !== -1) {\n                        if (this.path.indexOf(\"status\") !== -1) { this.isTargetFile = true; this.fileName = \"status\"; }\n                        else if (this.path.indexOf(\"maps\") !== -1) { this.isTargetFile = true; this.fileName = \"maps\"; }\n                        else if (this.path.indexOf(\"net/tcp\") !== -1) { this.isTargetFile = true; this.fileName = \"tcp\"; }\n                        else if (this.path.indexOf(\"task\") !== -1) { this.isTargetFile = true; this.fileName = \"task\"; }\n                    }\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (!raspLoaded) return; \n\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() gezielt f\u00fcr RASP auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isTargetFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) trackedFds[fd] = this.fileName; \n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                \n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                                send(\"[\ud83d\udd25 SPOOFER] TracerPid l\u00e4ngenneutral gef\u00e4lscht!\");\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\").replace(/xposed/g, \"yposed\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida in /proc/self/maps unsichtbar gemacht!\");\n                        }\n                        else if (fType === \"tcp\" && content.indexOf(\"69A2\") !== -1) {\n                            content = content.replace(/.*69A2.*/g, \"\"); \n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida TCP-Port versteckt!\");\n                        }\n                        else if (fType === \"task\" && (content.indexOf(\"gum-js-loop\") !== -1 || content.indexOf(\"gmain\") !== -1)) {\n                            content = content.replace(/gum-js-loop/g, \"xum-js-loop\").replace(/gmain/g, \"xmain\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida Threads getarnt!\");\n                        }\n\n                        if (modified) {\n                            this.buffer.writeUtf8String(content);\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n    \n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { \n                        var libName = args[0].readUtf8String(); \n                        if (libName && libName.indexOf(\"libdb0c.so\") !== -1) {\n                            raspLoaded = true;\n                            send(\"[!] RASP PROTEKTOR (libdb0c.so) WIRD GELADEN! Sniper scharfgestellt.\");\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Android System-Start...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "b7f65bfa",
            "name": "V10",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] 0010 Native RASP Hunter & Top-Frame Sniper gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var accessPtr = libc.findExportByName(\"access\");\n    var faccessatPtr = libc.findExportByName(\"faccessat\");\n    var readPtr = libc.findExportByName(\"read\");\n    var readlinkPtr = libc.findExportByName(\"readlink\");\n    var readlinkatPtr = libc.findExportByName(\"readlinkat\");\n\n    var trackedFds = {};\n    var raspLoaded = false; \n    \n    var fakePathMem = Memory.allocUtf8String(\"/sdcard/Android/data/org.nativescript.LibreLinkUp/files/clean_base.apk\");\n    \n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        \n        // DER FIX: Wir checken NUR die direkten Aufrufer (die obersten 4 Frames)\n        // Das ignoriert libart.so (Java), das sich immer ganz unten im Stack befindet.\n        var limit = Math.min(4, bt.length);\n        for (var i = 0; i < limit; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true; // Ghost Code (Userfaultfd) -> RASP\n            \n            var n = mod.name.toLowerCase();\n            if (n.indexOf(\"libdb0c\") !== -1 || n.indexOf(\"dexguard\") !== -1 || n.indexOf(\"promon\") !== -1) {\n                return true; // RASP eindeutig im Top-Stack identifiziert!\n            }\n        }\n        return false; \n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n\n                    if (!this.path) return;\n\n                    if (this.path.indexOf(\"/proc/\") !== -1) {\n                        if (this.path.indexOf(\"status\") !== -1) { this.isTargetFile = true; this.fileName = \"status\"; }\n                        else if (this.path.indexOf(\"maps\") !== -1) { this.isTargetFile = true; this.fileName = \"maps\"; }\n                        else if (this.path.indexOf(\"net/tcp\") !== -1) { this.isTargetFile = true; this.fileName = \"tcp\"; }\n                        else if (this.path.indexOf(\"task\") !== -1) { this.isTargetFile = true; this.fileName = \"task\"; }\n                    }\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (!raspLoaded) return; \n\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() gezielt f\u00fcr RASP auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isTargetFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) trackedFds[fd] = this.fileName; \n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n    hookFileAccess(accessPtr, 0, \"access\");\n    hookFileAccess(faccessatPtr, 1, \"faccessat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n                this.size = args[2].toInt32();\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                \n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                                send(\"[\ud83d\udd25 SPOOFER] TracerPid l\u00e4ngenneutral gef\u00e4lscht!\");\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\").replace(/xposed/g, \"yposed\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida in /proc/self/maps unsichtbar gemacht!\");\n                        }\n                        else if (fType === \"tcp\" && content.indexOf(\"69A2\") !== -1) {\n                            content = content.replace(/.*69A2.*/g, \"\"); \n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida TCP-Port versteckt!\");\n                        }\n                        else if (fType === \"task\" && (content.indexOf(\"gum-js-loop\") !== -1 || content.indexOf(\"gmain\") !== -1)) {\n                            content = content.replace(/gum-js-loop/g, \"xum-js-loop\").replace(/gmain/g, \"xmain\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida Threads getarnt!\");\n                        }\n\n                        if (modified) {\n                            this.buffer.writeUtf8String(content);\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n    \n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { \n                        var libName = args[0].readUtf8String(); \n                        if (libName && libName.indexOf(\"libdb0c.so\") !== -1) {\n                            raspLoaded = true;\n                            send(\"[!] RASP PROTEKTOR (libdb0c.so) WIRD GELADEN! Sniper scharfgestellt.\");\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n    }\n    \n    send(\"[+] Alle Hooks aktiv. Warte auf Android System-Start...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "88b2f4af",
            "name": "V11",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & OOM-Slayer gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n    var readlinkPtr = libc.findExportByName(\"readlink\");\n    var readlinkatPtr = libc.findExportByName(\"readlinkat\");\n\n    var trackedFds = {};\n    var raspLoaded = false; \n    \n    // DER ABSOLUTE FIX: Exakt der Pfad, den das Python-Tool pusht!\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    \n    // Perfekte Freund-Feind-Erkennung ohne Frame-Limit\n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        var isGhost = false;\n\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) {\n                isGhost = true; \n                continue;\n            }\n            var n = mod.name.toLowerCase();\n            // 1. Wenn RASP explizit im Stack steht -> FEUER!\n            if (n.indexOf(\"libdb0c\") !== -1 || n.indexOf(\"dexguard\") !== -1) {\n                return true;\n            }\n        }\n        \n        // 2. Wenn es ein Ghost-Thread ist (userfaultfd)\n        if (isGhost) {\n            // Wenn Android Java (ART) den Ghost aufgerufen hat, ist es legitimer JIT-Code.\n            for (var j = 0; j < bt.length; j++) {\n                var m = Process.findModuleByAddress(bt[j]);\n                if (m && m.name.indexOf(\"libart\") !== -1) return false; \n            }\n            return true; // Ghost Code OHNE Android Java = RASP Stealth Thread! FEUER!\n        }\n        \n        return false; // Alles andere ist das normale Android System\n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n\n                    if (!this.path) return;\n\n                    // Debugger & Memory Map Scanner\n                    if (this.path.indexOf(\"/proc/\") !== -1) {\n                        if (this.path.indexOf(\"status\") !== -1) { this.isTargetFile = true; this.fileName = \"status\"; }\n                        else if (this.path.indexOf(\"maps\") !== -1) { this.isTargetFile = true; this.fileName = \"maps\"; }\n                        else if (this.path.indexOf(\"net/tcp\") !== -1) { this.isTargetFile = true; this.fileName = \"tcp\"; }\n                        else if (this.path.indexOf(\"task\") !== -1) { this.isTargetFile = true; this.fileName = \"task\"; }\n                    }\n                    // Hash-Pr\u00fcfung umleiten\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (!raspLoaded) return; \n\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() gezielt auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isTargetFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) trackedFds[fd] = this.fileName; \n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                \n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                                send(\"[\ud83d\udd25 SPOOFER] TracerPid l\u00e4ngenneutral gef\u00e4lscht!\");\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\").replace(/xposed/g, \"yposed\");\n                            modified = true;\n                            send(\"[\ud83d\udd25 SPOOFER] Frida in /proc/self/maps unsichtbar gemacht!\");\n                        }\n                        else if (fType === \"tcp\" && content.indexOf(\"69A2\") !== -1) {\n                            content = content.replace(/.*69A2.*/g, \"\"); \n                            modified = true;\n                        }\n                        else if (fType === \"task\" && (content.indexOf(\"gum-js-loop\") !== -1 || content.indexOf(\"gmain\") !== -1)) {\n                            content = content.replace(/gum-js-loop/g, \"xum-js-loop\").replace(/gmain/g, \"xmain\");\n                            modified = true;\n                        }\n\n                        if (modified) this.buffer.writeUtf8String(content);\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n    \n    // Die Readlink-L\u00fcge aufrecht erhalten\n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n    \n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n    \n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    if (libdl) {\n        var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n        if (dlopenPtr) {\n            Interceptor.attach(dlopenPtr, {\n                onEnter: function (args) {\n                    try { \n                        var libName = args[0].readUtf8String(); \n                        if (libName && libName.indexOf(\"libdb0c.so\") !== -1) {\n                            raspLoaded = true;\n                            send(\"[!] RASP PROTEKTOR WIRD GELADEN! Sniper entsichert.\");\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n    }\n    \n    send(\"[+] OOM-Slayer aktiv. Warte auf Einschlag...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "56cd390c",
            "name": "V12Light",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & DEEP-TRACER gestartet...\");\n\n    // 1. Wir \u00fcberwachen den rohen \"syscall\" Befehl (wird oft f\u00fcr Stealth-Access genutzt)\n    var libc = Process.findModuleByName(\"libc.so\");\n    var syscallPtr = libc.findExportByName(\"syscall\");\n    \n    if (syscallPtr) {\n        Interceptor.attach(syscallPtr, {\n            onEnter: function (args) {\n                this.sysNum = args[0].toInt32();\n                // Syscall 56 = openat, 78 = readlinkat, 79 = fstatat (ARM64 Arch)\n                if (this.sysNum === 56 || this.sysNum === 78 || this.sysNum === 79) {\n                    try {\n                        var path = args[2].readUtf8String(); // Bei diesen Syscalls ist der Pfad das 3. Argument\n                        if (path && (path.indexOf(\"base.apk\") !== -1 || path.indexOf(\"/proc/\") !== -1)) {\n                            send(\"[\ud83d\udd75\ufe0f TRACER] Stealth Syscall (\" + this.sysNum + \") erkannt! Ziel: \" + path);\n                        }\n                    } catch(e) {}\n                }\n            }\n        });\n    }\n\n    // 2. Wir \u00fcberwachen alle nativen fopen/open Aufrufe, um zu sehen, ob er doch libc nutzt, aber wir den Callstack falsch lesen\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var fopenPtr = libc.findExportByName(\"fopen\");\n\n    function traceFile(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var path = args[pathIdx].readUtf8String();\n                    if (path && (path.indexOf(\"base.apk\") !== -1 || path.indexOf(\"/proc/\") !== -1)) {\n                        \n                        // Lass uns den Callstack im Log ausgeben, um den Aufrufer zu entlarven!\n                        var isGhost = true;\n                        var callerInfo = \"Unknown Ghost\";\n                        var bt = Thread.backtrace(this.context, Backtracer.ACCURATE);\n                        \n                        for (var i = 0; i < bt.length; i++) {\n                            var mod = Process.findModuleByAddress(bt[i]);\n                            if (mod) {\n                                isGhost = false;\n                                callerInfo = mod.name;\n                                // Wir interessieren uns f\u00fcr den ersten echten Aufrufer nach libc\n                                if (mod.name !== \"libc.so\" && mod.name !== \"libdl.so\") break;\n                            }\n                        }\n\n                        send(\"[\ud83d\udd75\ufe0f TRACER] \" + funcName + \"() -> \" + path + \" | Caller: \" + callerInfo);\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    traceFile(openPtr, 0, \"open\");\n    traceFile(openatPtr, 1, \"openat\");\n    traceFile(fopenPtr, 0, \"fopen\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || libc;\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { \n                    var libName = args[0].readUtf8String(); \n                    if (libName && libName.indexOf(\"libdb0c.so\") !== -1) {\n                        send(\"[!] RASP PROTEKTOR WIRD GELADEN! Tracer aktiv.\");\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    send(\"[+] Aufkl\u00e4rungs-Modus aktiv. Starte die Analyse...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "0f040747",
            "name": "V13AntiLoad",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & LOAD-BLOCKER (Fixed) gestartet...\");\n\n    // Wir holen uns den Linker absolut sicher \u00fcber die Process API\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    \n    // Eine harmlose Ersatz-Adresse f\u00fcr unseren Dummy-R\u00fcckgabewert\n    var libcBase = Process.findModuleByName(\"libc.so\").base;\n\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try {\n                    this.libPath = args[0].readUtf8String();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n\n                // Sobald Android versucht, den RASP zu laden...\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1) {\n                    \n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] RASP Lade-Versuch (\" + this.libPath + \") erkannt und blockiert!\");\n\n                    // Wir \u00fcberschreiben den Zeiger, damit die Bibliothek NICHT geladen wird, \n                    // geben der Java-Maschine aber einen g\u00fcltigen Speicher-Handle zur\u00fcck, damit sie nicht crasht.\n                    retval.replace(libcBase);\n                    \n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] RASP erfolgreich durch Dummy-Speicher ausgetauscht!\");\n                }\n            }\n        });\n    }\n\n    send(\"[+] Load-Blocker scharfgestellt. Warte auf den Android Linker...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "88646613",
            "name": "V14AntiLoad",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & LOAD-BLOCKER (JNI-Safe) gestartet...\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    \n    // Wir bauen uns eine Br\u00fccke zur echten dlopen C-Funktion\n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n\n    // Wir holen uns einen 100% G\u00dcLTIGEN Handle einer harmlosen Systembibliothek\n    var dummyPath = Memory.allocUtf8String(\"libc.so\");\n    var validDummyHandle = rawDlopen(dummyPath, 1); // RTLD_LAZY = 1\n\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try {\n                    this.libPath = args[0].readUtf8String();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n\n                // Sobald Android versucht, den RASP zu laden...\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1 || this.libPath.indexOf(\"promon\") !== -1) {\n                    \n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] RASP Lade-Versuch (\" + this.libPath + \") erkannt und neutralisiert!\");\n\n                    // Wir geben Android den g\u00fcltigen Dummy-Handle. \n                    // dlsym() wird nicht mehr crashen!\n                    retval.replace(validDummyHandle);\n                    \n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] Java-Maschine erfolgreich mit g\u00fcltigem Dummy-Handle get\u00e4uscht!\");\n                }\n            }\n        });\n    }\n\n    send(\"[+] Load-Blocker scharfgestellt. Warte auf den Android Linker...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "d22a28bd",
            "name": "ThreadBlocker",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] 0015 Native RASP Hunter & THREAD-BLOCKER gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var pthread_create = libc.findExportByName(\"pthread_create\");\n    \n    // Wir erstellen einen direkten Zeiger auf die originale Thread-Erstellungsfunktion\n    var raw_pthread_create = new NativeFunction(pthread_create, 'int', ['pointer', 'pointer', 'pointer', 'pointer']);\n\n    Interceptor.replace(pthread_create, new NativeCallback(function(thread, attr, start_routine, arg) {\n        var isRasp = false;\n        var callerName = \"Unknown\";\n        \n        // Wir pr\u00fcfen, WER den Thread starten will (R\u00fccksprungadresse)\n        var callerMod = Process.findModuleByAddress(this.returnAddress);\n        \n        if (callerMod) {\n            callerName = callerMod.name;\n            var n = callerName.toLowerCase();\n            if (n.indexOf(\"libdb0c\") !== -1 || n.indexOf(\"dexguard\") !== -1 || n.indexOf(\"promon\") !== -1) {\n                isRasp = true;\n            }\n        } else {\n            // Wenn der Aufrufer aus anonymem \"Ghost-Speicher\" kommt, ist es zu 99% entpackter RASP-Code!\n            callerName = \"Ghost-Memory (Userfaultfd)\";\n            isRasp = true;\n        }\n\n        // Wenn der RASP einen Scanner-Thread starten will, t\u00f6ten wir ihn!\n        if (isRasp) {\n            send(\"[\ud83d\udd25 THREAD-BLOCKER] Scanner-Thread von \" + callerName + \" abgefangen und vernichtet!\");\n            // Wir geben 0 zur\u00fcck (0 bedeutet in C: \"Thread erfolgreich gestartet\").\n            // Der RASP denkt, sein Scanner l\u00e4uft, aber in Wahrheit existiert er nicht!\n            return 0; \n        }\n\n        // F\u00fcr alle legitimen Android-Systemaufrufe starten wir den Thread ganz normal\n        return raw_pthread_create(thread, attr, start_routine, arg);\n    }, 'int', ['pointer', 'pointer', 'pointer', 'pointer']));\n\n    send(\"[+] Thread-Blocker aktiv. Lasse den RASP in die Falle tappen...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "d5d63c82",
            "name": "V16_CDUM",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & DLSYM-SPOOFER gestartet...\");\n\n    // 1. Die System-Bibliotheken und Funktionen abgreifen\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n    \n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    var dummyPath = Memory.allocUtf8String(\"libc.so\");\n    var validDummyHandle = rawDlopen(dummyPath, 1); \n\n    // 2. Der Load-Blocker (Sperrt den RASP komplett aus dem RAM aus)\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                \n                // Wir blocken die echte RASP-Bibliothek ab...\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1) {\n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] RASP Lade-Versuch (\" + this.libPath + \") blockiert!\");\n                    // ... und geben Java den Dummy-Handle der libc.so!\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    // 3. Der DLSYM JNI-Spoofer (T\u00e4uscht die Java-Maschine auf C-Ebene)\n    // Das ist die gef\u00e4lschte Funktion, die wir Java unterjubeln, wenn es nach \"stackFrames\" sucht\n    var fakeStackFrames = new NativeCallback(function(env, clazz, a, b) {\n        // Diese Funktion wird aufgerufen, wenn die App die RASP-Pr\u00fcfung startet!\n        send(\"[\ud83d\udd25 JNI-SPOOFER] App hat RASP-Pr\u00fcfung stackFrames(\" + a + \", \" + b + \") aufgerufen -> Neutralisiert!\");\n    }, 'void', ['pointer', 'pointer', 'int', 'int']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                // Wenn Java nach der stackFrames Funktion fragt...\n                if (this.symName && this.symName.indexOf(\"stackFrames\") !== -1) {\n                    send(\"[\ud83d\udd25 DLSYM] Gebe gef\u00e4lschte Funktion f\u00fcr '\" + this.symName + \"' zur\u00fcck!\");\n                    // ... geben wir unsere leere C-Funktion zur\u00fcck! Kein Crash mehr!\n                    retval.replace(fakeStackFrames);\n                }\n            }\n        });\n    }\n\n    send(\"[+] Load-Blocker & JNI-Spoofer scharfgestellt. Schachmatt vorbereitet...\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "1ef6e29c",
            "name": "V17_JNIMirror",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & UNIVERSAL DLSYM-SPOOFER gestartet...\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n    \n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    var dummyPath = Memory.allocUtf8String(\"libc.so\");\n    var validDummyHandle = rawDlopen(dummyPath, 1); \n\n    // 1. Der Load-Blocker\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                \n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1) {\n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] RASP Lade-Versuch blockiert!\");\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    // 2. Die universelle Dummy-Funktion f\u00fcr JNI\n    // Sie akzeptiert bis zu 4 Parameter und gibt vorsichtshalber einen 64-bit Integer (0) zur\u00fcck,\n    // um Methoden wie \"long read()\" zu befriedigen.\n    var fakeJniMethod = new NativeCallback(function(env, clazz, arg1, arg2) {\n        send(\"[\ud83d\udd25 JNI-SPOOFER] Native RASP-Pr\u00fcfung aus Java neutralisiert!\");\n        return 0; \n    }, 'int64', ['pointer', 'pointer', 'pointer', 'pointer']);\n\n    // 3. Der Universal-DLSYM Spoofer\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                // Wir fangen ALLES ab, was nach den verschleierten RASP-Methoden sucht!\n                if (this.symName && (this.symName.indexOf(\"stackFrames\") !== -1 || this.symName.indexOf(\"Java_o_\") !== -1)) {\n                    send(\"[\ud83d\udd25 DLSYM] Gebe Universal-Dummy f\u00fcr '\" + this.symName + \"' zur\u00fcck!\");\n                    retval.replace(fakeJniMethod);\n                }\n            }\n        });\n    }\n\n    send(\"[+] Universal-Spoofer scharfgestellt. Bereit f\u00fcr alles!\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "a7e755b5",
            "name": "V18_initArray",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & PHANTOM-BLOCKER gestartet...\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n    \n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    \n    // Trick 17: Wir laden 'liblog.so' (die Android-Logging-Bibliothek). \n    // Sie ist 100% harmlos, hat keine RASP-Logik und liefert einen perfekten Handle zur\u00fcck.\n    var dummyPath = Memory.allocUtf8String(\"liblog.so\");\n    var validDummyHandle = rawDlopen(dummyPath, 1); \n\n    // 1. Der Phantom-Load-Blocker\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                \n                // Wenn die App den echten RASP laden will...\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1) {\n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] RASP Lade-Versuch blockiert!\");\n                    // ... jubeln wir ihr den Handle der harmlosen liblog.so unter!\n                    retval.replace(validDummyHandle);\n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] Harmlose Phantom-Bibliothek geladen!\");\n                }\n            }\n        });\n    }\n\n    // 2. Die universelle Dummy-Funktion f\u00fcr Java\n    // Gibt eine neutrale 0 zur\u00fcck.\n    var fakeJniMethod = new NativeCallback(function(env, clazz, arg1, arg2) {\n        send(\"[\ud83d\udd25 JNI-SPOOFER] Native RASP-Pr\u00fcfung aus Java neutralisiert!\");\n        return 0; \n    }, 'int64', ['pointer', 'pointer', 'pointer', 'pointer']);\n\n    // 3. Der Universal-DLSYM Spoofer\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                // Wenn Java in unserer Phantom-Lib nach RASP-Funktionen sucht...\n                if (this.symName && (this.symName.indexOf(\"stackFrames\") !== -1 || this.symName.indexOf(\"Java_o_\") !== -1 || this.symName.indexOf(\"read\") !== -1)) {\n                    send(\"[\ud83d\udd25 DLSYM] Gebe Universal-Dummy f\u00fcr '\" + this.symName + \"' zur\u00fcck!\");\n                    // ... geben wir unsere leere Dummy-Funktion zur\u00fcck! Kein Absturz!\n                    retval.replace(fakeJniMethod);\n                }\n            }\n        });\n    }\n\n    send(\"[+] Phantom-Spoofer scharfgestellt. Bereit f\u00fcr alles!\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "8d144bb6",
            "name": "V19_initArray_Explorer",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & SW-FLUSS TRACER gestartet...\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n    \n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    var dummyPath = Memory.allocUtf8String(\"liblog.so\");\n    var validDummyHandle = rawDlopen(dummyPath, 1); \n\n    var oomHooked = false;\n\n    // 1. Der Java-OOM-Tracker (Zeigt uns den exakten SW-Fluss der Bombe)\n    function hookJavaOOM() {\n        if (oomHooked) return;\n        try {\n            Java.perform(function() {\n                var OOM = Java.use(\"java.lang.OutOfMemoryError\");\n                OOM.$init.overload('java.lang.String').implementation = function(msg) {\n                    send(\"[\ud83d\udca3 OOM BOMBE AUSGEL\u00d6ST] \" + msg);\n                    var Log = Java.use(\"android.util.Log\");\n                    var Exception = Java.use(\"java.lang.Exception\");\n                    send(\"[\ud83d\udd75\ufe0f SW-FLUSS DER BOMBE]\\n\" + Log.getStackTraceString(Exception.$new()));\n                    return this.$init(msg);\n                };\n                oomHooked = true;\n                send(\"[+] Java OOM-Bomben Detektor aktiv!\");\n            });\n        } catch (e) {}\n    }\n\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1) {\n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] RASP blockiert -> Phantom geladen!\");\n                    retval.replace(validDummyHandle);\n                    \n                    // Sobald JNI geladen wird, ist die VM wach. Wir setzen den Tracker!\n                    hookJavaOOM();\n                }\n            }\n        });\n    }\n\n    // 2. Dummy f\u00fcr stackFrames mit SW-Fluss Trace\n    var fakeStackFrames = new NativeCallback(function(env, clazz, intA, intB) {\n        send(\"[\ud83d\udd25 JNI] stackFrames() aufgerufen!\");\n        try {\n            Java.perform(function() {\n                var Log = Java.use(\"android.util.Log\");\n                send(\"[\ud83d\udd75\ufe0f SW-FLUSS stackFrames]\\n\" + Log.getStackTraceString(Java.use(\"java.lang.Exception\").$new()));\n            });\n        } catch(e) {}\n    }, 'void', ['pointer', 'pointer', 'int', 'int']);\n\n    // 3. Dummy f\u00fcr read() mit SW-Fluss Trace & Defuse-Versuch\n    var fakeRead = new NativeCallback(function(env, clazz) {\n        send(\"[\ud83d\udd25 JNI] read() aufgerufen!\");\n        try {\n            Java.perform(function() {\n                var Log = Java.use(\"android.util.Log\");\n                send(\"[\ud83d\udd75\ufe0f SW-FLUSS read]\\n\" + Log.getStackTraceString(Java.use(\"java.lang.Exception\").$new()));\n            });\n        } catch(e) {}\n        \n        // DER ENTSCH\u00c4RFUNGS-VERSUCH: Wir geben 0 statt 1 zur\u00fcck!\n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    var fakeUniversal = new NativeCallback(function(env, clazz) {\n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.symName) {\n                    if (this.symName.indexOf(\"stackFrames\") !== -1) {\n                        retval.replace(fakeStackFrames);\n                    } \n                    else if (this.symName.indexOf(\"Java_o_onResult_read\") !== -1 || this.symName.indexOf(\"Java_o_onResult_read__\") !== -1) {\n                        retval.replace(fakeRead);\n                    }\n                    else if (this.symName.indexOf(\"Java_o_\") !== -1) {\n                        retval.replace(fakeUniversal);\n                    }\n                }\n            }\n        });\n    }\n\n    send(\"[+] SW-Fluss Tracer & Anti-OOM scharfgestellt.\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "419dbbf1",
            "name": "V20_Defuser",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native RASP Hunter & OOM-DEFUSER gestartet...\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n    \n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    var validDummyHandle = rawDlopen(Memory.allocUtf8String(\"liblog.so\"), 1); \n\n    // 1. Phantom-Load-Blocker\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1) {\n                    send(\"[\ud83d\udd25 LOAD-BLOCKER] RASP blockiert!\");\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    // 2. Hilfsfunktion f\u00fcr ECHTE synchrone Java Stacktraces!\n    function printJavaTrace(name) {\n        try {\n            // performNow zwingt Frida, sofort zu arbeiten, bevor die App weiterlaufen darf!\n            Java.performNow(function() {\n                var thr = Java.use(\"java.lang.Thread\").currentThread();\n                var tr = thr.getStackTrace();\n                var s = \"[\ud83d\udd75\ufe0f SW-FLUSS \" + name + \"]\\n\";\n                // Wir \u00fcberspringen die ersten zwei Zeilen (das sind nur die Frida/Thread internen Aufrufe)\n                for (var i = 2; i < tr.length; i++) {\n                    s += \"   -> \" + tr[i].toString() + \"\\n\";\n                }\n                send(s);\n            });\n        } catch (e) {\n            send(\"[!] Konnte Java Trace f\u00fcr \" + name + \" nicht lesen: \" + e.message);\n        }\n    }\n\n    // 3. Dummy-Funktionen\n    var fakeStackFrames = new NativeCallback(function(env, clazz, intA, intB) {\n        send(\"[\ud83d\udd25 JNI] stackFrames(\" + intA + \", \" + intB + \") aufgerufen!\");\n        printJavaTrace(\"stackFrames\");\n    }, 'void', ['pointer', 'pointer', 'int', 'int']);\n\n    var fakeRead = new NativeCallback(function(env, clazz) {\n        send(\"[\ud83d\udd25 JNI] read() aufgerufen!\");\n        printJavaTrace(\"read\");\n        \n        // DER ENTSCH\u00c4RFUNGS-VERSUCH!\n        // Wir geben nicht mehr 0 zur\u00fcck, sondern 1024. \n        // Wenn read() f\u00fcr Puffer-Gr\u00f6\u00dfen genutzt wird, crasht hier jetzt nichts mehr!\n        return 1024; \n    }, 'int64', ['pointer', 'pointer']);\n\n    var fakeUniversal = new NativeCallback(function(env, clazz) {\n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    // 4. JNI-Dispatcher\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.symName) {\n                    if (this.symName.indexOf(\"stackFrames\") !== -1) {\n                        retval.replace(fakeStackFrames);\n                    } \n                    else if (this.symName.indexOf(\"Java_o_onResult_read\") !== -1 || this.symName.indexOf(\"read__\") !== -1) {\n                        retval.replace(fakeRead);\n                    }\n                    else if (this.symName.indexOf(\"Java_o_\") !== -1) {\n                        retval.replace(fakeUniversal);\n                    }\n                }\n            }\n        });\n    }\n\n    send(\"[+] OOM-Defuser und synchroner Java-Tracer scharfgestellt.\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "2e221f25",
            "name": "0001_breadcrumbs",
            "code": "/**\n * V21_Crypto_Extractor\n * Target: org.nativescript.LibreLinkUp (secure-api)\n * Method: JCA (Java Cryptography Architecture) Hooking via Logcat\n */\n\n// 1. RASP-Safe Native Logging Setup (Umgeht Frida-Konsolen-Crashes)\nvar logDef = new NativeFunction(Module.findExportByName('liblog.so', '__android_log_print'), 'int', ['int', 'pointer', 'pointer']);\nvar TAG = Memory.allocUtf8String(\"KippyCrypto\");\n\nfunction androidLog(msg) {\n    var str = Memory.allocUtf8String(msg);\n    logDef(3, TAG, str); // 3 = ANDROID_LOG_DEBUG\n}\n\n// Helper: Byte-Array zu Hex-String\nfunction bytesToHex(bytes) {\n    if (!bytes) return null;\n    var hexArray = [];\n    for (var i = 0; i < bytes.length; i++) {\n        var hex = (bytes[i] & 0xFF).toString(16);\n        hexArray.push(hex.length === 1 ? '0' + hex : hex);\n    }\n    return hexArray.join('');\n}\n\n// Helper: Byte-Array zu Base64 (optional, da der Traffic auch oft Base64 nutzt)\nfunction bytesToBase64(bytes) {\n    if (!bytes) return null;\n    var binary = '';\n    for (var i = 0; i < bytes.length; i++) {\n        binary += String.fromCharCode(bytes[i] & 0xFF);\n    }\n    return btoa(binary);\n}\n\nJava.performNow(function () {\n    androidLog(\"[*] V21_Crypto_Extractor injected. Waiting for AES initialization...\");\n\n    var Cipher = Java.use('javax.crypto.Cipher');\n    var SecretKeySpec = Java.use('javax.crypto.spec.SecretKeySpec');\n    var IvParameterSpec = Java.use('javax.crypto.spec.IvParameterSpec');\n\n    // 2. Hooking Cipher.init (Der Moment, in dem Key & IV an die Cipher \u00fcbergeben werden)\n    var initOverload = Cipher.init.overload('int', 'java.security.Key', 'java.security.spec.AlgorithmParameterSpec');\n    \n    initOverload.implementation = function (opmode, key, params) {\n        var algorithm = this.getAlgorithm();\n        \n        // Wir filtern gezielt nach AES-Verschl\u00fcsselungen (z.B. AES/GCM/NoPadding)\n        if (algorithm.indexOf('AES') !== -1) {\n            var keyHex = null;\n            var ivHex = null;\n            var keyB64 = null;\n            var ivB64 = null;\n\n            // SecretKeySpec extrahieren\n            try {\n                var secretKey = Java.cast(key, SecretKeySpec);\n                var keyBytes = secretKey.getEncoded();\n                keyHex = bytesToHex(keyBytes);\n                keyB64 = bytesToBase64(keyBytes);\n            } catch (e) { }\n\n            // Initialization Vector (IV) extrahieren\n            try {\n                if (params !== null) {\n                    var ivSpec = Java.cast(params, IvParameterSpec);\n                    var ivBytes = ivSpec.getIV();\n                    ivHex = bytesToHex(ivBytes);\n                    ivB64 = bytesToBase64(ivBytes);\n                }\n            } catch (e) { }\n\n            var modeStr = (opmode === 1) ? \"ENCRYPT\" : (opmode === 2) ? \"DECRYPT\" : opmode;\n\n            // Als sauberes JSON formatieren f\u00fcr den Python-Backend-Service\n            var payload = {\n                \"event\": \"KIPPY_KEY_INTERCEPT\",\n                \"algorithm\": algorithm,\n                \"mode\": modeStr,\n                \"key_hex\": keyHex,\n                \"key_base64\": keyB64,\n                \"iv_hex\": ivHex,\n                \"iv_base64\": ivB64\n            };\n\n            // Sicher in den Android Logcat feuern\n            androidLog(JSON.stringify(payload));\n        }\n\n        // Originale Methode aufrufen, damit die App nicht crasht\n        return this.init(opmode, key, params);\n    };\n});"
        },
        {
            "id": "aca01b7b",
            "name": "0002_breadcrumbs",
            "code": "// @ts-nocheck\n/**\n * V21_Crypto_Extractor\n * Target: org.nativescript.LibreLinkUp (secure-api)\n * Method: JCA Hooking via Logcat\n */\n\ntry {\n    console.log(\"[*] Breadcrumbs FRIDA 0002 gestartet\");\n    send(\"[*] Breadcrumbs 0002 gestartet\");\n    // 1. RASP-Safe Native Logging Setup (Umgeht Frida-Konsolen-Crashes)\n    var logDef = new NativeFunction(Module.findExportByName('liblog.so', '__android_log_print'), 'int', ['int', 'pointer', 'pointer']);\n    var TAG = Memory.allocUtf8String(\"Breadcrumb0002\");\n\n    function androidLog(msg) {\n        var str = Memory.allocUtf8String(msg);\n        logDef(3, TAG, str); // 3 = ANDROID_LOG_DEBUG\n    }\n\n    // Helper: Byte-Array zu Hex-String\n    function bytesToHex(bytes) {\n        if (!bytes) return null;\n        var hexArray = [];\n        for (var i = 0; i < bytes.length; i++) {\n            var hex = (bytes[i] & 0xFF).toString(16);\n            hexArray.push(hex.length === 1 ? '0' + hex : hex);\n        }\n        return hexArray.join('');\n    }\n\n    // Helper: Byte-Array zu Base64\n    function bytesToBase64(bytes) {\n        if (!bytes) return null;\n        var binary = '';\n        for (var i = 0; i < bytes.length; i++) {\n            binary += String.fromCharCode(bytes[i] & 0xFF);\n        }\n        return btoa(binary);\n    }\n\n    Java.performNow(function () {\n        androidLog(\"[*] V21_Crypto_Extractor injected. Waiting for AES initialization...\");\n\n        var Cipher = Java.use('javax.crypto.Cipher');\n        var SecretKeySpec = Java.use('javax.crypto.spec.SecretKeySpec');\n        var IvParameterSpec = Java.use('javax.crypto.spec.IvParameterSpec');\n\n        // 2. Hooking Cipher.init\n        var initOverload = Cipher.init.overload('int', 'java.security.Key', 'java.security.spec.AlgorithmParameterSpec');\n        \n        initOverload.implementation = function (opmode, key, params) {\n            var algorithm = this.getAlgorithm();\n            \n            // Gezielt nach AES-Verschl\u00fcsselungen (z.B. AES/GCM/NoPadding) filtern\n            if (algorithm.indexOf('AES') !== -1) {\n                var keyHex = null;\n                var ivHex = null;\n                var keyB64 = null;\n                var ivB64 = null;\n\n                try {\n                    var secretKey = Java.cast(key, SecretKeySpec);\n                    var keyBytes = secretKey.getEncoded();\n                    keyHex = bytesToHex(keyBytes);\n                    keyB64 = bytesToBase64(keyBytes);\n                } catch (e) { }\n\n                try {\n                    if (params !== null) {\n                        var ivSpec = Java.cast(params, IvParameterSpec);\n                        var ivBytes = ivSpec.getIV();\n                        ivHex = bytesToHex(ivBytes);\n                        ivB64 = bytesToBase64(ivBytes);\n                    }\n                } catch (e) { }\n\n                var modeStr = (opmode === 1) ? \"ENCRYPT\" : (opmode === 2) ? \"DECRYPT\" : opmode;\n\n                var payload = {\n                    \"event\": \"KIPPY_KEY_INTERCEPT\",\n                    \"algorithm\": algorithm,\n                    \"mode\": modeStr,\n                    \"key_hex\": keyHex,\n                    \"key_base64\": keyB64,\n                    \"iv_hex\": ivHex,\n                    \"iv_base64\": ivB64\n                };\n\n                // JSON sicher in den Logcat feuern\n                androidLog(JSON.stringify(payload));\n            }\n\n            return this.init(opmode, key, params);\n        };\n    });\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "125eff94",
            "name": "0003_Breadcrumbs",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] CryptoAudit Hook gestartet (Pure Java Mode)...\");\n\n    // Helper: Byte-Array zu Hex-String\n    function bytesToHex(bytes) {\n        if (!bytes) return null;\n        var hexArray = [];\n        for (var i = 0; i < bytes.length; i++) {\n            var hex = (bytes[i] & 0xFF).toString(16);\n            hexArray.push(hex.length === 1 ? '0' + hex : hex);\n        }\n        return hexArray.join('');\n    }\n\n    send(\"[DEBUG] Warte auf Java VM...\");\n\n    if (typeof Java === 'undefined') {\n        send(\"[FATAL] Java API ist in diesem Kontext nicht verf\u00fcgbar!\");\n    } else {\n        Java.perform(function () {\n            try {\n                send(\"[+] Java VM Hook bereit. Initialisiere Crypto-Interceptor...\");\n\n                var Cipher = Java.use('javax.crypto.Cipher');\n                var SecretKeySpec = Java.use('javax.crypto.spec.SecretKeySpec');\n                var IvParameterSpec = Java.use('javax.crypto.spec.IvParameterSpec');\n                var Base64 = Java.use('android.util.Base64'); \n                \n                var initOverload = Cipher.init.overload('int', 'java.security.Key', 'java.security.spec.AlgorithmParameterSpec');\n\n                initOverload.implementation = function (opmode, key, params) {\n                    var algorithm = this.getAlgorithm();\n                    \n                    if (algorithm.indexOf('AES') !== -1) {\n                        var keyHex = null, ivHex = null, keyB64 = null, ivB64 = null;\n                        \n                        try {\n                            var secretKey = Java.cast(key, SecretKeySpec);\n                            var keyBytes = secretKey.getEncoded();\n                            keyHex = bytesToHex(keyBytes);\n                            keyB64 = Base64.encodeToString(keyBytes, 2); \n                        } catch (e) { }\n\n                        try {\n                            if (params !== null) {\n                                var ivSpec = Java.cast(params, IvParameterSpec);\n                                var ivBytes = ivSpec.getIV();\n                                ivHex = bytesToHex(ivBytes);\n                                ivB64 = Base64.encodeToString(ivBytes, 2);\n                            }\n                        } catch (e) { }\n\n                        var modeStr = (opmode === 1) ? \"ENCRYPT\" : (opmode === 2) ? \"DECRYPT\" : opmode;\n                        var payload = {\n                            \"event\": \"KIPPY_KEY_INTERCEPT\",\n                            \"algorithm\": algorithm,\n                            \"mode\": modeStr,\n                            \"key_hex\": keyHex,\n                            \"key_base64\": keyB64,\n                            \"iv_hex\": ivHex,\n                            \"iv_base64\": ivB64\n                        };\n\n                        // Wir schicken die extrahierten Keys direkt an die Python GUI!\n                        send(\"[INTERCEPT] \" + JSON.stringify(payload));\n                    }\n\n                    // Originale Verschl\u00fcsselung durchf\u00fchren, damit die App nicht einfriert\n                    return initOverload.call(this, opmode, key, params);\n                };\n                \n                send(\"[DEBUG] Cipher Hook erfolgreich gesetzt! App l\u00e4uft weiter.\");\n            } catch (innerE) {\n                send(\"[FATAL INNER ERROR] \" + innerE.message + \"\\n\" + innerE.stack);\n            }\n        });\n    }\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message + \"\\n\" + e.stack);\n}"
        },
        {
            "id": "f8b9432a",
            "name": "0004_Breadcrumbs",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native Crypto Hook gestartet (Nativescript Bypass)...BC0005\");\n\n    // Helper: Rohe C-Pointer in Hex-Strings wandeln\n    function bytesToHex(buffer, length) {\n        if (!buffer || buffer.isNull()) return \"null\";\n        try {\n            var bytes = new Uint8Array(buffer.readByteArray(length));\n            var hexStr = \"\";\n            for (var i = 0; i < bytes.length; i++) {\n                var hex = (bytes[i] & 0xFF).toString(16);\n                hexStr += (hex.length === 1 ? \"0\" + hex : hex);\n            }\n            return hexStr;\n        } catch (e) {\n            return \"error\";\n        }\n    }\n\n    // WORKAROUND: Nativescript \u00fcberschreibt das globale 'Module' Objekt!\n    // Wir suchen die C-Funktionen stattdessen sicher \u00fcber 'Process.enumerateModules'.\n    function findExportSafe(funcName) {\n        var modules = Process.enumerateModules();\n        for (var i = 0; i < modules.length; i++) {\n            var ptr = modules[i].findExportByName(funcName);\n            if (ptr && !ptr.isNull()) {\n                return ptr;\n            }\n        }\n        return null;\n    }\n\n    function hookEVP(funcName, forcedMode) {\n        send(\"[DEBUG] Suche \" + funcName + \" im RAM...\");\n        var funcPtr = findExportSafe(funcName);\n        \n        if (!funcPtr) {\n            send(\"[-] \" + funcName + \" nicht im RAM gefunden.\");\n            return;\n        }\n        \n        send(\"[+] Hook gesetzt auf: \" + funcName + \" (\" + funcPtr + \")\");\n        Interceptor.attach(funcPtr, {\n            onEnter: function(args) {\n                // Signatur: int EVP_CipherInit_ex(ctx, type, impl, key, iv, enc)\n                this.keyPtr = args[3];\n                this.ivPtr = args[4];\n                \n                if (forcedMode) {\n                    this.mode = forcedMode;\n                } else {\n                    try {\n                        var enc = args[5].toInt32();\n                        this.mode = (enc === 1) ? \"ENCRYPT\" : (enc === 0) ? \"DECRYPT\" : \"UNKNOWN\";\n                    } catch(e) {\n                        this.mode = \"UNKNOWN\";\n                    }\n                }\n            },\n            onLeave: function(retval) {\n                try {\n                    // C-Pointer absichern (Null-Checks)\n                    if (!this.keyPtr || this.keyPtr.isNull() || !this.ivPtr || this.ivPtr.isNull()) return;\n\n                    var keyHex = bytesToHex(this.keyPtr, 32);\n                    var ivHex = bytesToHex(this.ivPtr, 16);\n\n                    var payload = {\n                        \"event\": \"KIPPY_KEY_INTERCEPT\",\n                        \"layer\": \"NATIVE_C_BORINGSSL\",\n                        \"function\": funcName,\n                        \"mode\": this.mode,\n                        \"key_hex_32b\": keyHex,\n                        \"iv_hex_16b\": ivHex\n                    };\n\n                    send(\"[INTERCEPT] \" + JSON.stringify(payload));\n                } catch (err) {\n                    send(\"[!] Fehler in onLeave von \" + funcName + \": \" + err.message);\n                }\n            }\n        });\n    }\n\n    // Androids native Krypto-Routinen einklinken\n    hookEVP(\"EVP_CipherInit_ex\", null);\n    hookEVP(\"EVP_EncryptInit_ex\", \"ENCRYPT\");\n    hookEVP(\"EVP_DecryptInit_ex\", \"DECRYPT\");\n\n    send(\"[DEBUG] Native Crypto-Wachen aufgestellt! App l\u00e4uft weiter.\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message + \"\\n\" + e.stack);\n}"
        },
        {
            "id": "e316f1b6",
            "name": "0005_Breadcrumbs",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native Crypto Hook & OOM-Slayer gestartet...0005\");\n\n    function bytesToHex(buffer, length) {\n        if (!buffer || buffer.isNull()) return \"null\";\n        try {\n            var bytes = new Uint8Array(buffer.readByteArray(length));\n            var hexStr = \"\";\n            for (var i = 0; i < bytes.length; i++) {\n                var hex = (bytes[i] & 0xFF).toString(16);\n                hexStr += (hex.length === 1 ? \"0\" + hex : hex);\n            }\n            return hexStr;\n        } catch (e) { return \"error\"; }\n    }\n\n    function findExportSafe(funcName) {\n        var modules = Process.enumerateModules();\n        for (var i = 0; i < modules.length; i++) {\n            var ptr = modules[i].findExportByName(funcName);\n            if (ptr && !ptr.isNull()) return ptr;\n        }\n        return null;\n    }\n\n    // --- 1. DIE OOM-VERTEIDIGUNG (Basierend auf deinem V20_Defuser) ---\n    var dlsymPtr = findExportSafe(\"dlsym\");\n    \n    // Die Dummys fangen den RASP ab, bevor er crasht\n    var fakeStackFrames = new NativeCallback(function(env, clazz, intA, intB) {\n        send(\"[\ud83d\udd25 JNI-SPOOFER] RASP-Angriff stackFrames() neutralisiert!\");\n    }, 'void', ['pointer', 'pointer', 'int', 'int']);\n\n    var fakeRead = new NativeCallback(function(env, clazz) {\n        send(\"[\ud83d\udd25 JNI-SPOOFER] RASP-Angriff read() neutralisiert!\");\n        return 1024; // Verhindert Buffer-Underflows\n    }, 'int64', ['pointer', 'pointer']);\n\n    var fakeUniversal = new NativeCallback(function(env, clazz) {\n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        send(\"[DEBUG] dlsym JNI-Wache aufgestellt.\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.symName) {\n                    if (this.symName.indexOf(\"stackFrames\") !== -1) {\n                        retval.replace(fakeStackFrames);\n                    } else if (this.symName.indexOf(\"Java_o_onResult_read\") !== -1 || this.symName.indexOf(\"read__\") !== -1) {\n                        retval.replace(fakeRead);\n                    } else if (this.symName.indexOf(\"Java_o_\") !== -1) {\n                        retval.replace(fakeUniversal);\n                    }\n                }\n            }\n        });\n    }\n\n    // --- 2. DER CRYPTO EXTRACTOR ---\n    function hookEVP(funcName, forcedMode) {\n        var funcPtr = findExportSafe(funcName);\n        if (!funcPtr) return;\n        \n        send(\"[+] Hook gesetzt auf: \" + funcName);\n        Interceptor.attach(funcPtr, {\n            onEnter: function(args) {\n                this.keyPtr = args[3];\n                this.ivPtr = args[4];\n                \n                if (forcedMode) {\n                    this.mode = forcedMode;\n                } else {\n                    try {\n                        var enc = args[5].toInt32();\n                        this.mode = (enc === 1) ? \"ENCRYPT\" : (enc === 0) ? \"DECRYPT\" : \"UNKNOWN\";\n                    } catch(e) { this.mode = \"UNKNOWN\"; }\n                }\n            },\n            onLeave: function(retval) {\n                try {\n                    if (!this.keyPtr || this.keyPtr.isNull() || !this.ivPtr || this.ivPtr.isNull()) return;\n\n                    var payload = {\n                        \"event\": \"KIPPY_KEY_INTERCEPT\",\n                        \"layer\": \"NATIVE_C_BORINGSSL\",\n                        \"function\": funcName,\n                        \"mode\": this.mode,\n                        \"key_hex_32b\": bytesToHex(this.keyPtr, 32),\n                        \"iv_hex_16b\": bytesToHex(this.ivPtr, 16)\n                    };\n\n                    send(\"[INTERCEPT] \" + JSON.stringify(payload));\n                } catch (err) {}\n            }\n        });\n    }\n\n    hookEVP(\"EVP_CipherInit_ex\", null);\n    hookEVP(\"EVP_EncryptInit_ex\", \"ENCRYPT\");\n    hookEVP(\"EVP_DecryptInit_ex\", \"DECRYPT\");\n\n    send(\"[DEBUG] Alle Wachen aufgestellt! App l\u00e4uft weiter.\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "6df3384f",
            "name": "0006_Breadcrumbs",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] Native Crypto Hook & OOM-Slayer (V2) gestartet...\");\n\n    function bytesToHex(buffer, length) {\n        if (!buffer || buffer.isNull()) return \"null\";\n        try {\n            var bytes = new Uint8Array(buffer.readByteArray(length));\n            var hexStr = \"\";\n            for (var i = 0; i < bytes.length; i++) {\n                var hex = (bytes[i] & 0xFF).toString(16);\n                hexStr += (hex.length === 1 ? \"0\" + hex : hex);\n            }\n            return hexStr;\n        } catch (e) { return \"error\"; }\n    }\n\n    // --- 1. DIE OOM-VERTEIDIGUNG (Exakt wie in V20_Defuser) ---\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlsymPtr = libdl ? libdl.findExportByName(\"dlsym\") : null;\n    \n    var fakeStackFrames = new NativeCallback(function(env, clazz, intA, intB) {\n        send(\"[\ud83d\udd25 JNI-SPOOFER] RASP-Angriff stackFrames() neutralisiert!\");\n    }, 'void', ['pointer', 'pointer', 'int', 'int']);\n\n    var fakeRead = new NativeCallback(function(env, clazz) {\n        send(\"[\ud83d\udd25 JNI-SPOOFER] RASP-Angriff read() neutralisiert!\");\n        return 1024;\n    }, 'int64', ['pointer', 'pointer']);\n\n    var fakeUniversal = new NativeCallback(function(env, clazz) {\n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        send(\"[DEBUG] dlsym JNI-Wache aufgestellt auf: \" + dlsymPtr);\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.symName) {\n                    if (this.symName.indexOf(\"stackFrames\") !== -1) {\n                        send(\"[DEBUG] Abgefangen: \" + this.symName);\n                        retval.replace(fakeStackFrames);\n                    } else if (this.symName.indexOf(\"Java_o_onResult_read\") !== -1 || this.symName.indexOf(\"read__\") !== -1) {\n                        send(\"[DEBUG] Abgefangen: \" + this.symName);\n                        retval.replace(fakeRead);\n                    } else if (this.symName.indexOf(\"Java_o_\") !== -1) {\n                        retval.replace(fakeUniversal);\n                    }\n                }\n            }\n        });\n    } else {\n        send(\"[-] WARNUNG: dlsym konnte nicht gefunden werden!\");\n    }\n\n    // --- 2. DER CRYPTO EXTRACTOR (Nativescript-Bypass Modus) ---\n    function findExportSafe(funcName) {\n        var modules = Process.enumerateModules();\n        for (var i = 0; i < modules.length; i++) {\n            var ptr = modules[i].findExportByName(funcName);\n            if (ptr && !ptr.isNull()) return ptr;\n        }\n        return null;\n    }\n\n    function hookEVP(funcName, forcedMode) {\n        var funcPtr = findExportSafe(funcName);\n        if (!funcPtr) return;\n        \n        send(\"[+] Hook gesetzt auf: \" + funcName);\n        Interceptor.attach(funcPtr, {\n            onEnter: function(args) {\n                this.keyPtr = args[3];\n                this.ivPtr = args[4];\n                \n                if (forcedMode) {\n                    this.mode = forcedMode;\n                } else {\n                    try {\n                        var enc = args[5].toInt32();\n                        this.mode = (enc === 1) ? \"ENCRYPT\" : (enc === 0) ? \"DECRYPT\" : \"UNKNOWN\";\n                    } catch(e) { this.mode = \"UNKNOWN\"; }\n                }\n            },\n            onLeave: function(retval) {\n                try {\n                    if (!this.keyPtr || this.keyPtr.isNull() || !this.ivPtr || this.ivPtr.isNull()) return;\n\n                    var payload = {\n                        \"event\": \"KIPPY_KEY_INTERCEPT\",\n                        \"layer\": \"NATIVE_C_BORINGSSL\",\n                        \"function\": funcName,\n                        \"mode\": this.mode,\n                        \"key_hex_32b\": bytesToHex(this.keyPtr, 32),\n                        \"iv_hex_16b\": bytesToHex(this.ivPtr, 16)\n                    };\n\n                    send(\"[INTERCEPT] \" + JSON.stringify(payload));\n                } catch (err) {}\n            }\n        });\n    }\n\n    hookEVP(\"EVP_CipherInit_ex\", null);\n    hookEVP(\"EVP_EncryptInit_ex\", \"ENCRYPT\");\n    hookEVP(\"EVP_DecryptInit_ex\", \"DECRYPT\");\n\n    send(\"[DEBUG] Alle Wachen aufgestellt! App l\u00e4uft weiter.\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "6ef944e0",
            "name": "0007_Breadcrumbs",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] ULTIMATIVE AUFKL\u00c4RUNG: Spoofer, Crypto & OOM-Tracer gestartet...\");\n\n    // --- 1. RASP STEALTH SPOOFER ---\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var statPtr = libc.findExportByName(\"stat\");\n    var lstatPtr = libc.findExportByName(\"lstat\");\n    var fstatatPtr = libc.findExportByName(\"fstatat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    var trackedFds = {};\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true; // Ghost Code\n            if (mod.name.toLowerCase().indexOf(\"libdb0c\") !== -1) return true;\n        }\n        for (var j = 0; j < bt.length; j++) {\n            var m = Process.findModuleByAddress(bt[j]);\n            if (m && (m.name.indexOf(\"libart\") !== -1 || m.name.indexOf(\"libziparchive\") !== -1 || m.name.indexOf(\"libandroidfw\") !== -1)) return false; \n        }\n        return true; \n    }\n\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n                    if (!this.path) return;\n\n                    if (this.path.indexOf(\"/proc/\") !== -1) {\n                        if (this.path.indexOf(\"status\") !== -1) { this.isTargetFile = true; this.fileName = \"status\"; }\n                        else if (this.path.indexOf(\"maps\") !== -1) { this.isTargetFile = true; this.fileName = \"maps\"; }\n                    }\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + funcName + \"() auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isTargetFile && (funcName === \"open\" || funcName === \"openat\")) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) trackedFds[fd] = this.fileName; \n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n    hookFileAccess(statPtr, 0, \"stat\");\n    hookFileAccess(lstatPtr, 0, \"lstat\");\n    hookFileAccess(fstatatPtr, 1, \"fstatat\");\n\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\");\n                            modified = true;\n                        }\n                        if (modified) this.buffer.writeUtf8String(content);\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n\n    // --- 2. OOM-TRACER (WER WIRFT DIE BOMBE?) ---\n    if (typeof Java !== 'undefined') {\n        Java.perform(function() {\n            send(\"[DEBUG] Java VM bereit. Stelle OOM-Falle auf...\");\n            var OOM = Java.use(\"java.lang.OutOfMemoryError\");\n            \n            OOM.$init.overload('java.lang.String').implementation = function(msg) {\n                send(\"==================================================\");\n                send(\"[\\ud83d\\udca3 OOM BOMBE ABGEFANGEN!] \" + msg);\n                \n                // Wir generieren einen Stacktrace, um den T\u00e4ter zu entlarven!\n                var Exception = Java.use(\"java.lang.Exception\");\n                var Log = Java.use(\"android.util.Log\");\n                var stackTrace = Log.getStackTraceString(Exception.$new());\n                \n                send(\"[\ud83d\udd75\ufe0f T\u00c4TER-CALLSTACK]\\n\" + stackTrace);\n                send(\"==================================================\");\n                \n                return this.$init(msg);\n            };\n            send(\"[+] OOM-Tracker scharfgestellt!\");\n        });\n    }\n\n    // --- 3. CRYPTO EXTRACTOR ---\n    function bytesToHex(buffer, length) {\n        if (!buffer || buffer.isNull()) return \"null\";\n        try {\n            var bytes = new Uint8Array(buffer.readByteArray(length));\n            var hexStr = \"\";\n            for (var i = 0; i < bytes.length; i++) {\n                var hex = (bytes[i] & 0xFF).toString(16);\n                hexStr += (hex.length === 1 ? \"0\" + hex : hex);\n            }\n            return hexStr;\n        } catch (e) { return \"error\"; }\n    }\n\n    function hookEVP(funcName, forcedMode) {\n        var modules = Process.enumerateModules();\n        var funcPtr = null;\n        for (var i = 0; i < modules.length; i++) {\n            funcPtr = modules[i].findExportByName(funcName);\n            if (funcPtr && !funcPtr.isNull()) break;\n        }\n        if (!funcPtr) return;\n        \n        send(\"[+] Crypto-Hook gesetzt: \" + funcName);\n        Interceptor.attach(funcPtr, {\n            onEnter: function(args) {\n                this.keyPtr = args[3];\n                this.ivPtr = args[4];\n                this.mode = forcedMode ? forcedMode : \"UNKNOWN\";\n            },\n            onLeave: function(retval) {\n                try {\n                    if (!this.keyPtr || this.keyPtr.isNull()) return;\n                    var payload = {\n                        \"event\": \"KIPPY_KEY_INTERCEPT\",\n                        \"function\": funcName,\n                        \"mode\": this.mode,\n                        \"key_hex_32b\": bytesToHex(this.keyPtr, 32),\n                        \"iv_hex_16b\": bytesToHex(this.ivPtr, 16)\n                    };\n                    send(\"[INTERCEPT] \" + JSON.stringify(payload));\n                } catch (err) {}\n            }\n        });\n    }\n\n    hookEVP(\"EVP_CipherInit_ex\", null);\n    hookEVP(\"EVP_EncryptInit_ex\", \"ENCRYPT\");\n    hookEVP(\"EVP_DecryptInit_ex\", \"DECRYPT\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "e3941334",
            "name": "0008_Breadcrumbs",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] ULTIMATE NATIVE RECON: Spoofer, Crypto & C-Level OOM-Tracer gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    \n    // --- 1. DER NATIVE LOGCAT-SNIFFER ---\n    // F\u00e4ngt den Moment ab, in dem Android den OOM in den Log schreiben will\n    var logPtr = Module.findExportByName('liblog.so', '__android_log_print');\n    if (logPtr) {\n        Interceptor.attach(logPtr, {\n            onEnter: function(args) {\n                try {\n                    this.msg = args[2].readUtf8String();\n                    if (this.msg && this.msg.indexOf(\"OutOfMemoryError\") !== -1) {\n                        send(\"==================================================\");\n                        send(\"[\ud83d\udca3 OOM LOG ABGEFANGEN] \" + this.msg);\n                        var bt = Thread.backtrace(this.context, Backtracer.ACCURATE);\n                        var trace = \"[\ud83d\udd75\ufe0f NATIVE T\u00c4TER-CALLSTACK (Logger)]\\n\";\n                        for (var i = 0; i < bt.length; i++) {\n                            var mod = Process.findModuleByAddress(bt[i]);\n                            trace += \"   -> \" + bt[i] + \" | \" + (mod ? mod.name : \"GHOST / JIT\") + \"\\n\";\n                        }\n                        send(trace);\n                        send(\"==================================================\");\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // --- 2. DIE OOM-W\u00dcRGEZANGE (mmap Intercept) ---\n    // 707 MB sind 741,343,232 Bytes. Wir blockieren alles \u00fcber 500 MB!\n    var mmapPtr = libc.findExportByName(\"mmap\");\n    if (mmapPtr) {\n        send(\"[DEBUG] mmap Kernel-Wache aufgestellt.\");\n        Interceptor.attach(mmapPtr, {\n            onEnter: function(args) {\n                this.size = args[1].toInt32();\n                if (this.size > 524288000) { // 500 MB in Bytes\n                    send(\"==================================================\");\n                    send(\"[\ud83d\udd25 OOM-SLAYER] Massive Allokation von \" + this.size + \" Bytes detektiert!\");\n                    \n                    var bt = Thread.backtrace(this.context, Backtracer.ACCURATE);\n                    var trace = \"[\ud83d\udd75\ufe0f ALLOCATION T\u00c4TER-CALLSTACK]\\n\";\n                    for (var i = 0; i < bt.length; i++) {\n                        var mod = Process.findModuleByAddress(bt[i]);\n                        trace += \"   -> \" + bt[i] + \" | \" + (mod ? mod.name : \"GHOST / JIT\") + \"\\n\";\n                    }\n                    send(trace);\n                    send(\"==================================================\");\n                    \n                    // DER LEBENSRETTER: Wir manipulieren das Gr\u00f6\u00dfenargument on-the-fly!\n                    // Statt 707 MB verlangen wir nur 1 KB vom Kernel. Die App \u00fcberlebt!\n                    args[1] = ptr(1024); \n                }\n            }\n        });\n    }\n\n    // --- 3. STEALTH SPOOFER (L\u00e4uft perfekt) ---\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    \n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true;\n            if (mod.name.toLowerCase().indexOf(\"libdb0c\") !== -1) return true;\n        }\n        return false;\n    }\n\n    function hookFileAccess(p, pathIdx, name) {\n        if (!p) return;\n        Interceptor.attach(p, {\n            onEnter: function(args) {\n                try {\n                    var path = args[pathIdx].readUtf8String();\n                    if (path && path.indexOf(\"base.apk\") !== -1 && path.indexOf(\"clean_base.apk\") === -1) {\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udd25 SNIPER] \" + name + \"() auf Clean-APK umgeleitet!\");\n                        }\n                    }\n                } catch(e){}\n            }\n        });\n    }\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n\n    // --- 4. CRYPTO EXTRACTOR ---\n    function bytesToHex(buffer, length) {\n        if (!buffer || buffer.isNull()) return \"null\";\n        try {\n            var bytes = new Uint8Array(buffer.readByteArray(length));\n            var hexStr = \"\";\n            for (var i = 0; i < bytes.length; i++) {\n                var hex = (bytes[i] & 0xFF).toString(16);\n                hexStr += (hex.length === 1 ? \"0\" + hex : hex);\n            }\n            return hexStr;\n        } catch (e) { return \"error\"; }\n    }\n\n    function hookEVP(funcName, forcedMode) {\n        var modules = Process.enumerateModules();\n        var funcPtr = null;\n        for (var i = 0; i < modules.length; i++) {\n            funcPtr = modules[i].findExportByName(funcName);\n            if (funcPtr && !funcPtr.isNull()) break;\n        }\n        if (!funcPtr) return;\n        \n        send(\"[+] Crypto-Hook gesetzt: \" + funcName);\n        Interceptor.attach(funcPtr, {\n            onEnter: function(args) {\n                this.keyPtr = args[3];\n                this.ivPtr = args[4];\n                this.mode = forcedMode ? forcedMode : \"UNKNOWN\";\n            },\n            onLeave: function(retval) {\n                try {\n                    if (!this.keyPtr || this.keyPtr.isNull() || !this.ivPtr || this.ivPtr.isNull()) return;\n                    var payload = {\n                        \"event\": \"KIPPY_KEY_INTERCEPT\",\n                        \"function\": funcName,\n                        \"mode\": this.mode,\n                        \"key_hex_32b\": bytesToHex(this.keyPtr, 32),\n                        \"iv_hex_16b\": bytesToHex(this.ivPtr, 16)\n                    };\n                    send(\"[INTERCEPT] \" + JSON.stringify(payload));\n                } catch (err) {}\n            }\n        });\n    }\n\n    hookEVP(\"EVP_CipherInit_ex\", null);\n    hookEVP(\"EVP_EncryptInit_ex\", \"ENCRYPT\");\n    hookEVP(\"EVP_DecryptInit_ex\", \"DECRYPT\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "ff39f436",
            "name": "0009",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] JNI & ART RECON: Starte Deep-Tracing der Android Runtime...\");\n\n    // --- 1. RASP STEALTH SPOOFER ---\n    // (Muss aktiv bleiben, damit wir nicht wegen /proc/self/maps gecrasht werden)\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true; \n            if (mod.name.toLowerCase().indexOf(\"libdb0c\") !== -1) return true;\n        }\n        for (var j = 0; j < bt.length; j++) {\n            var m = Process.findModuleByAddress(bt[j]);\n            if (m && (m.name.indexOf(\"libart\") !== -1 || m.name.indexOf(\"libziparchive\") !== -1 || m.name.indexOf(\"libandroidfw\") !== -1)) return false; \n        }\n        return true; \n    }\n\n    function hookFileAccess(p, pathIdx, name) {\n        if (!p) return;\n        Interceptor.attach(p, {\n            onEnter: function(args) {\n                try {\n                    var path = args[pathIdx].readUtf8String();\n                    if (path && path.indexOf(\"base.apk\") !== -1 && path.indexOf(\"clean_base.apk\") === -1) {\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                        }\n                    }\n                } catch(e){}\n            }\n        });\n    }\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n\n    // --- 2. ART & JNI RECON (Der Aufkl\u00e4rer) ---\n    var libart = Process.findModuleByName(\"libart.so\");\n    if (libart) {\n        send(\"[DEBUG] libart.so gefunden. Scanne Exporte (kann 1-2 Sekunden dauern)...\");\n        var exports = libart.enumerateExports();\n        var hookedCount = 0;\n\n        for (var i = 0; i < exports.length; i++) {\n            var name = exports[i].name;\n\n            // 2A. JNI FindClass belauschen\n            if (name.indexOf(\"FindClass\") !== -1 && name.indexOf(\"JNI\") !== -1) {\n                Interceptor.attach(exports[i].address, {\n                    onEnter: function(args) {\n                        try {\n                            var clsName = args[1].readUtf8String();\n                            // Wir filtern Standard-Android-Klassen heraus, um Spam zu vermeiden\n                            if (clsName && clsName.indexOf(\"java/\") === -1 && clsName.indexOf(\"android/\") === -1) {\n                                send(\"[\ud83d\udd75\ufe0f JNI Class] DexGuard sucht: \" + clsName);\n                            }\n                        } catch(e){}\n                    }\n                });\n                hookedCount++;\n            }\n\n            // 2B. JNI GetMethodID / GetStaticMethodID belauschen\n            if ((name.indexOf(\"GetMethodID\") !== -1 || name.indexOf(\"GetStaticMethodID\") !== -1) && name.indexOf(\"JNI\") !== -1) {\n                Interceptor.attach(exports[i].address, {\n                    onEnter: function(args) {\n                        try {\n                            var methodName = args[2].readUtf8String();\n                            var sig = args[3].readUtf8String();\n                            if (methodName && methodName !== \"<init>\") {\n                                send(\"[\ud83c\udfaf JNI Method] DexGuard ruft auf: \" + methodName + \" | Signatur: \" + sig);\n                            }\n                        } catch(e){}\n                    }\n                });\n                hookedCount++;\n            }\n\n            // 2C. Den absoluten OOM-Thrower der Runtime abfangen\n            if (name.indexOf(\"ThrowOutOfMemoryError\") !== -1) {\n                Interceptor.attach(exports[i].address, {\n                    onEnter: function(args) {\n                        send(\"==================================================\");\n                        send(\"[\ud83d\udca3 ART OOM EXCEPTION AUSGEL\u00d6ST!]\");\n                        var bt = Thread.backtrace(this.context, Backtracer.ACCURATE);\n                        var trace = \"[\ud83d\udc80 NATIVE CALLSTACK]\\n\";\n                        for(var j = 0; j < bt.length; j++) {\n                            var mod = Process.findModuleByAddress(bt[j]);\n                            trace += \"-> \" + bt[j] + \" | \" + (mod ? mod.name : \"GHOST / JIT\") + \"\\n\";\n                        }\n                        send(trace);\n                        send(\"==================================================\");\n                    }\n                });\n                hookedCount++;\n            }\n        }\n        send(\"[DEBUG] \" + hookedCount + \" JNI/ART Wachen erfolgreich aufgestellt.\");\n    } else {\n        send(\"[-] WARNUNG: libart.so nicht gefunden!\");\n    }\n\n    // --- 3. CRYPTO EXTRACTOR ---\n    function bytesToHex(buffer, length) {\n        if (!buffer || buffer.isNull()) return \"null\";\n        try {\n            var bytes = new Uint8Array(buffer.readByteArray(length));\n            var hexStr = \"\";\n            for (var i = 0; i < bytes.length; i++) {\n                var hex = (bytes[i] & 0xFF).toString(16);\n                hexStr += (hex.length === 1 ? \"0\" + hex : hex);\n            }\n            return hexStr;\n        } catch (e) { return \"error\"; }\n    }\n\n    function hookEVP(funcName, forcedMode) {\n        var modules = Process.enumerateModules();\n        var funcPtr = null;\n        for (var i = 0; i < modules.length; i++) {\n            funcPtr = modules[i].findExportByName(funcName);\n            if (funcPtr && !funcPtr.isNull()) break;\n        }\n        if (!funcPtr) return;\n        \n        Interceptor.attach(funcPtr, {\n            onEnter: function(args) {\n                this.keyPtr = args[3];\n                this.ivPtr = args[4];\n                this.mode = forcedMode ? forcedMode : \"UNKNOWN\";\n            },\n            onLeave: function(retval) {\n                try {\n                    if (!this.keyPtr || this.keyPtr.isNull() || !this.ivPtr || this.ivPtr.isNull()) return;\n                    var payload = {\n                        \"event\": \"KIPPY_KEY_INTERCEPT\",\n                        \"function\": funcName,\n                        \"mode\": this.mode,\n                        \"key_hex_32b\": bytesToHex(this.keyPtr, 32),\n                        \"iv_hex_16b\": bytesToHex(this.ivPtr, 16)\n                    };\n                    send(\"[INTERCEPT] \" + JSON.stringify(payload));\n                } catch (err) {}\n            }\n        });\n    }\n\n    hookEVP(\"EVP_CipherInit_ex\", null);\n    hookEVP(\"EVP_EncryptInit_ex\", \"ENCRYPT\");\n    hookEVP(\"EVP_DecryptInit_ex\", \"DECRYPT\");\n\n    send(\"[+] Aufkl\u00e4rung l\u00e4uft. App fortsetzen!\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "b388ef5e",
            "name": "0010",
            "code": "// @ts-nocheck\ntry {\n    send(\"[*] GHOST PROTOCOL: Full-Stealth Recon & Crypto Extractor...\");\n\n    // 100% Nativescript-sichere Export-Suche (Umgeht 'not a function' Crashes)\n    function getSafeExport(libName, funcName) {\n        var mods = Process.enumerateModules();\n        for (var i = 0; i < mods.length; i++) {\n            if (mods[i].name === libName) {\n                return mods[i].findExportByName(funcName);\n            }\n        }\n        return null;\n    }\n\n    // --- 1. RASP STEALTH SPOOFER ---\n    var openPtr = getSafeExport(\"libc.so\", \"open\");\n    var openatPtr = getSafeExport(\"libc.so\", \"openat\");\n    var readlinkPtr = getSafeExport(\"libc.so\", \"readlink\");\n    var readlinkatPtr = getSafeExport(\"libc.so\", \"readlinkat\");\n    var readPtr = getSafeExport(\"libc.so\", \"read\");\n\n    var trackedFds = {};\n    // WICHTIG: Die clean_base.apk MUSS durch das Framework via ADB gepusht worden sein!\n    var fakePathMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    function isRaspAccess(context) {\n        var bt = Thread.backtrace(context, Backtracer.ACCURATE);\n        for (var i = 0; i < bt.length; i++) {\n            var mod = Process.findModuleByAddress(bt[i]);\n            if (!mod) return true; // Ghost Code (JIT/DexGuard)\n            if (mod.name.toLowerCase().indexOf(\"libdb0c\") !== -1) return true;\n        }\n        for (var j = 0; j < bt.length; j++) {\n            var m = Process.findModuleByAddress(bt[j]);\n            if (m && (m.name.indexOf(\"libart\") !== -1 || m.name.indexOf(\"libziparchive\") !== -1 || m.name.indexOf(\"libandroidfw\") !== -1)) return false; \n        }\n        return true; \n    }\n\n    // A: Datei-\u00d6ffnungen auf das saubere Original umleiten\n    function hookOpen(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isTargetFile = false;\n                    if (!this.path) return;\n\n                    if (this.path.indexOf(\"/proc/\") !== -1) {\n                        if (this.path.indexOf(\"status\") !== -1) { this.isTargetFile = true; this.fileName = \"status\"; }\n                        else if (this.path.indexOf(\"maps\") !== -1) { this.isTargetFile = true; this.fileName = \"maps\"; }\n                    }\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        if (isRaspAccess(this.context)) {\n                            args[pathIdx] = fakePathMem;\n                            send(\"[\ud83d\udc7b GHOST] \" + funcName + \"() -> APK-Verzeichnis gef\u00e4lscht!\");\n                        }\n                    }\n                } catch(e){}\n            },\n            onLeave: function(retval) {\n                if (this.isTargetFile) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) trackedFds[fd] = this.fileName; \n                }\n            }\n        });\n    }\n    hookOpen(openPtr, 0, \"open\");\n    hookOpen(openatPtr, 1, \"openat\");\n\n    // B: Linux Kernel-L\u00fcge aufrecht erhalten (readlink f\u00e4lschen)\n    function hookReadlink(ptr, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function(args) {\n                this.buf = (funcName === \"readlink\") ? args[1] : args[2];\n            },\n            onLeave: function(retval) {\n                var len = retval.toInt32();\n                if (len > 0) {\n                    try {\n                        var result = this.buf.readUtf8String(len);\n                        if (result.indexOf(\"clean_base.apk\") !== -1) {\n                            var fixed = result.replace(\"clean_base.apk\", \"base.apk\");\n                            this.buf.writeUtf8String(fixed);\n                            retval.replace(fixed.length);\n                            send(\"[\ud83d\udc7b GHOST] \" + funcName + \"() -> OS-Pfadpr\u00fcfung gef\u00e4lscht!\");\n                        }\n                    } catch(e){}\n                }\n            }\n        });\n    }\n    hookReadlink(readlinkPtr, \"readlink\");\n    hookReadlink(readlinkatPtr, \"readlinkat\");\n\n    // C: Frida-Spuren im Arbeitsspeicher ausradieren\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                var fType = trackedFds[this.fd];\n                if (fType && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        var modified = false;\n\n                        if (fType === \"status\" && content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                modified = true;\n                                send(\"[\ud83d\udc7b GHOST] Debugger-Check (TracerPID) neutralisiert.\");\n                            }\n                        } \n                        else if (fType === \"maps\" && content.indexOf(\"frida\") !== -1) {\n                            content = content.replace(/frida/g, \"xriba\").replace(/magisk/g, \"xagisk\");\n                            modified = true;\n                            send(\"[\ud83d\udc7b GHOST] Memory-Scan: Frida/Magisk aus dem RAM radiert.\");\n                        }\n                        if (modified) this.buffer.writeUtf8String(content);\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n\n    // --- 2. CRYPTO EXTRACTOR (BoringSSL) ---\n    function bytesToHex(buffer, length) {\n        if (!buffer || buffer.isNull()) return \"null\";\n        try {\n            var bytes = new Uint8Array(buffer.readByteArray(length));\n            var hexStr = \"\";\n            for (var i = 0; i < bytes.length; i++) {\n                var hex = (bytes[i] & 0xFF).toString(16);\n                hexStr += (hex.length === 1 ? \"0\" + hex : hex);\n            }\n            return hexStr;\n        } catch (e) { return \"error\"; }\n    }\n\n    function hookEVP(funcName, forcedMode) {\n        var funcPtr = null;\n        var mods = Process.enumerateModules();\n        for (var i = 0; i < mods.length; i++) {\n            funcPtr = mods[i].findExportByName(funcName);\n            if (funcPtr) break;\n        }\n        if (!funcPtr) return;\n        \n        send(\"[+] Crypto-Wache aufgestellt: \" + funcName);\n        Interceptor.attach(funcPtr, {\n            onEnter: function(args) {\n                this.keyPtr = args[3];\n                this.ivPtr = args[4];\n                this.mode = forcedMode ? forcedMode : \"UNKNOWN\";\n            },\n            onLeave: function(retval) {\n                try {\n                    if (!this.keyPtr || this.keyPtr.isNull() || !this.ivPtr || this.ivPtr.isNull()) return;\n                    var payload = {\n                        \"event\": \"KIPPY_KEY_INTERCEPT\",\n                        \"function\": funcName,\n                        \"mode\": this.mode,\n                        \"key_hex_32b\": bytesToHex(this.keyPtr, 32),\n                        \"iv_hex_16b\": bytesToHex(this.ivPtr, 16)\n                    };\n                    send(\"[INTERCEPT] \" + JSON.stringify(payload));\n                } catch (err) {}\n            }\n        });\n    }\n\n    hookEVP(\"EVP_CipherInit_ex\", null);\n    hookEVP(\"EVP_EncryptInit_ex\", \"ENCRYPT\");\n    hookEVP(\"EVP_DecryptInit_ex\", \"DECRYPT\");\n\n} catch (e) {\n    send(\"[FATAL SCRIPT ERROR] \" + e.message);\n}"
        },
        {
            "id": "2106eea9",
            "name": "0011_Breadcrumbs",
            "code": "/**\n * Frida Native Crypto Dumper (OpenSSL/BoringSSL)\n * Target: org.nativescript.LibreLinkUp\n * Safe-Mode: Enabled (Memory checks & Exception handling)\n */\n\nconsole.log(\"[*] GHOST PROTOCOL: Native Crypto Dumper gestartet...\");\n\n// Hilfsfunktion: Sch\u00f6ner Hex-Dump f\u00fcr die Konsole\nfunction hexdump(buffer, length) {\n    if (buffer.isNull() || length === 0) return \"null / empty\";\n    try {\n        let mem = Memory.readByteArray(buffer, length);\n        return \"\\n\" + Hexdump(mem, {\n            offset: 0,\n            length: length,\n            header: true,\n            ansi: true\n        });\n    } catch (e) {\n        return \"[!] Hexdump failed: \" + e.message;\n    }\n}\n\n// Hilfsfunktion: String-Dump f\u00fcr Payload (Klartext)\nfunction stringdump(buffer, length) {\n    if (buffer.isNull() || length === 0) return \"\";\n    try {\n        // Lese als UTF-8 String, ignoriere fehlerhafte Zeichen\n        return buffer.readUtf8String(length);\n    } catch (e) {\n        return \"[Binary Data]\";\n    }\n}\n\n// 1. Hook: EVP_CipherInit_ex (Greift AES Key und IV ab)\n// int EVP_CipherInit_ex(EVP_CIPHER_CTX *ctx, const EVP_CIPHER *type, ENGINE *impl, const unsigned char *key, const unsigned char *iv, int enc);\nlet initPtr = Module.findExportByName(null, \"EVP_CipherInit_ex\") || \n              Module.findExportByName(null, \"EVP_DecryptInit_ex\");\n\nif (initPtr) {\n    console.log(\"[+] Ziel erfasst: EVP_Init @ \" + initPtr);\n    Interceptor.attach(initPtr, {\n        onEnter: function (args) {\n            this.key = args[3];\n            this.iv = args[4];\n            this.enc = args[5].toInt32(); // 1 = Encrypt, 0 = Decrypt\n\n            let mode = this.enc === 1 ? \"ENCRYPT\" : \"DECRYPT\";\n            \n            // Vermeide Spam: Nur loggen, wenn Key oder IV vorhanden sind\n            if (!this.key.isNull() || !this.iv.isNull()) {\n                console.log(\"\\n==========================================\");\n                console.log(\"[\ud83d\udd11 EVP_Init] Modus: \" + mode);\n                \n                // Keys sind bei AES oft 16, 24 oder 32 Bytes lang. Wir dumpen sicherheitshalber 32.\n                if (!this.key.isNull()) {\n                    console.log(\"[+] KEY (max 32b):\" + hexdump(this.key, 32));\n                }\n                if (!this.iv.isNull()) {\n                    console.log(\"[+] IV  (max 16b):\" + hexdump(this.iv, 16));\n                }\n            }\n        }\n    });\n} else {\n    console.log(\"[-] Warnung: EVP_CipherInit_ex nicht gefunden.\");\n}\n\n// 2. Hook: EVP_DecryptUpdate (Greift den Klartext nach der Entschl\u00fcsselung ab)\n// int EVP_DecryptUpdate(EVP_CIPHER_CTX *ctx, unsigned char *out, int *outl, const unsigned char *in, int inl);\nlet decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                       Module.findExportByName(null, \"EVP_CipherUpdate\");\n\nif (decryptUpdatePtr) {\n    console.log(\"[+] Ziel erfasst: EVP_DecryptUpdate @ \" + decryptUpdatePtr);\n    Interceptor.attach(decryptUpdatePtr, {\n        onEnter: function (args) {\n            // Speichere die Pointerstrukturen im 'this' Kontext f\u00fcr onLeave\n            this.ctx = args[0];\n            this.out = args[1];       // Hier landet der Klartext\n            this.outl_ptr = args[2];  // Hier schreibt C die L\u00e4nge des Klartexts rein\n            this.in = args[3];        // Chiffretext (Verschl\u00fcsselt)\n            this.inl = args[4].toInt32(); // L\u00e4nge des Inputs\n        },\n        onLeave: function (retval) {\n            try {\n                // Pr\u00fcfen ob die Entschl\u00fcsselung erfolgreich war (meist retval > 0 oder == 1)\n                if (retval.toInt32() > 0 && !this.out.isNull() && !this.outl_ptr.isNull()) {\n                    \n                    // Lese die tats\u00e4chliche L\u00e4nge des Klartextes aus dem C-Pointer\n                    let out_len = this.outl_ptr.readInt();\n                    \n                    if (out_len > 0) {\n                        let plainString = stringdump(this.out, out_len);\n                        \n                        // Filtern: Wir wollen nur API-Daten sehen (z.B. JSON), keinen Rauschen\n                        if (plainString.includes(\"{\") || plainString.includes(\"data\") || plainString.includes(\"uuid\")) {\n                            console.log(\"\\n==========================================\");\n                            console.log(\"[\ud83d\udd13 DECRYPTED PAYLOAD] L\u00e4nge: \" + out_len + \" Bytes\");\n                            console.log(\"------------------------------------------\");\n                            console.log(plainString);\n                            console.log(\"==========================================\\n\");\n                        }\n                    }\n                }\n            } catch (e) {\n                // F\u00e4ngt Memory-Access-Violations ab, bevor die App crasht\n                console.log(\"[!] DecryptUpdate Hook Error: \" + e.message);\n            }\n        }\n    });\n} else {\n    console.log(\"[-] Warnung: EVP_DecryptUpdate nicht gefunden.\");\n}\n\nconsole.log(\"[*] Warten auf Kryptografie-Operationen...\");"
        },
        {
            "id": "198580dd",
            "name": "0012_Breadcrumbs",
            "code": "/**\n * Frida Native Crypto Dumper (OpenSSL/BoringSSL)\n * Target: org.nativescript.LibreLinkUp\n * Safe-Mode: Enabled (Memory checks & Exception handling)\n */\n\nsetTimeout(function() {\n    console.log(\"[*] GHOST PROTOCOL: Native Crypto Dumper gestartet...\");\n\n    // Umbenannt, um Konflikte mit Fridas nativer hexdump() API zu vermeiden\n    function safeHexDump(buffer, length) {\n        if (buffer.isNull() || length === 0) return \"null / empty\";\n        try {\n            let mem = Memory.readByteArray(buffer, length);\n            // Nutze die native Frida hexdump API\n            return \"\\n\" + hexdump(mem, {\n                offset: 0,\n                length: length,\n                header: true,\n                ansi: false // ANSI deaktiviert f\u00fcr sauberes Logging in der Windows-Konsole\n            });\n        } catch (e) {\n            return \"[!] Hexdump failed: \" + e.message;\n        }\n    }\n\n    function safeStringDump(buffer, length) {\n        if (buffer.isNull() || length === 0) return \"\";\n        try {\n            return buffer.readUtf8String(length);\n        } catch (e) {\n            return \"[Binary Data]\";\n        }\n    }\n\n    // 1. Hook: EVP_CipherInit_ex (Greift AES Key und IV ab)\n    let initPtr = Module.findExportByName(null, \"EVP_CipherInit_ex\") || \n                  Module.findExportByName(null, \"EVP_DecryptInit_ex\");\n\n    if (initPtr) {\n        console.log(\"[+] Ziel erfasst: EVP_Init @ \" + initPtr);\n        Interceptor.attach(initPtr, {\n            onEnter: function (args) {\n                this.key = args[3];\n                this.iv = args[4];\n                this.enc = args[5].toInt32(); \n\n                let mode = this.enc === 1 ? \"ENCRYPT\" : \"DECRYPT\";\n                \n                if (!this.key.isNull() || !this.iv.isNull()) {\n                    console.log(\"\\n==========================================\");\n                    console.log(\"[\ud83d\udd11 EVP_Init] Modus: \" + mode);\n                    \n                    if (!this.key.isNull()) {\n                        console.log(\"[+] KEY (max 32b):\" + safeHexDump(this.key, 32));\n                    }\n                    if (!this.iv.isNull()) {\n                        console.log(\"[+] IV  (max 16b):\" + safeHexDump(this.iv, 16));\n                    }\n                }\n            }\n        });\n    } else {\n        console.log(\"[-] Warnung: EVP_CipherInit_ex nicht gefunden.\");\n    }\n\n    // 2. Hook: EVP_DecryptUpdate (Greift den Klartext nach der Entschl\u00fcsselung ab)\n    let decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                           Module.findExportByName(null, \"EVP_CipherUpdate\");\n\n    if (decryptUpdatePtr) {\n        console.log(\"[+] Ziel erfasst: EVP_DecryptUpdate @ \" + decryptUpdatePtr);\n        Interceptor.attach(decryptUpdatePtr, {\n            onEnter: function (args) {\n                this.out = args[1];       \n                this.outl_ptr = args[2];  \n            },\n            onLeave: function (retval) {\n                try {\n                    if (retval.toInt32() > 0 && !this.out.isNull() && !this.outl_ptr.isNull()) {\n                        let out_len = this.outl_ptr.readInt();\n                        \n                        if (out_len > 0) {\n                            let plainString = safeStringDump(this.out, out_len);\n                            \n                            if (plainString.includes(\"{\") || plainString.includes(\"data\") || plainString.includes(\"uuid\")) {\n                                console.log(\"\\n==========================================\");\n                                console.log(\"[\ud83d\udd13 DECRYPTED PAYLOAD] L\u00e4nge: \" + out_len + \" Bytes\");\n                                console.log(\"------------------------------------------\");\n                                console.log(plainString);\n                                console.log(\"==========================================\\n\");\n                            }\n                        }\n                    }\n                } catch (e) {\n                    console.log(\"[!] DecryptUpdate Hook Error: \" + e.message);\n                }\n            }\n        });\n    } else {\n        console.log(\"[-] Warnung: EVP_DecryptUpdate nicht gefunden.\");\n    }\n\n    console.log(\"[*] Warten auf Kryptografie-Operationen...\");\n}, 500);"
        },
        {
            "id": "ec5079fc",
            "name": "0013_Breadcrumbs",
            "code": "setTimeout(function() {\n    console.log(\"[*] GHOST PROTOCOL: Native Crypto Dumper gestartet...\");\n\n    function safeHexDump(ptr, len) {\n        if (ptr.isNull() || len === 0) return \"null / empty\";\n        try {\n            return \"\\n\" + hexdump(ptr, { offset: 0, length: len, header: true, ansi: false });\n        } catch (e) {\n            return \"[!] Hexdump failed: \" + e.message;\n        }\n    }\n\n    function safeStringDump(ptr, len) {\n        if (ptr.isNull() || len === 0) return \"\";\n        try {\n            return Memory.readUtf8String(ptr, len);\n        } catch (e) {\n            return \"[Binary Data]\";\n        }\n    }\n\n    let initPtr = Module.findExportByName(null, \"EVP_CipherInit_ex\") || \n                  Module.findExportByName(null, \"EVP_DecryptInit_ex\");\n\n    if (initPtr) {\n        Interceptor.attach(initPtr, {\n            onEnter: function (args) {\n                this.key = args[3];\n                this.iv = args[4];\n                this.enc = 0;\n                try {\n                    // Sicherer Cast ohne .toInt32() Abh\u00e4ngigkeit\n                    this.enc = parseInt(args[5].toString(), 16);\n                } catch(e) {}\n\n                if (!this.key.isNull() || !this.iv.isNull()) {\n                    let mode = (this.enc === 1) ? \"ENCRYPT\" : \"DECRYPT\";\n                    console.log(\"\\n==========================================\");\n                    console.log(\"[\ud83d\udd11 EVP_Init] Modus: \" + mode);\n                    if (!this.key.isNull()) console.log(\"[+] KEY: \" + safeHexDump(this.key, 32));\n                    if (!this.iv.isNull()) console.log(\"[+] IV : \" + safeHexDump(this.iv, 16));\n                }\n            }\n        });\n    }\n\n    let decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                           Module.findExportByName(null, \"EVP_CipherUpdate\");\n\n    if (decryptUpdatePtr) {\n        Interceptor.attach(decryptUpdatePtr, {\n            onEnter: function (args) {\n                this.out = args[1];       \n                this.outl_ptr = args[2];  \n            },\n            onLeave: function (retval) {\n                try {\n                    let ret = parseInt(retval.toString(), 16);\n                    if (ret > 0 && !this.out.isNull() && !this.outl_ptr.isNull()) {\n                        // Sicherer Memory-Read statt Methoden-Aufruf auf Pointer\n                        let out_len = Memory.readS32(this.outl_ptr);\n                        \n                        if (out_len > 0) {\n                            let plainString = safeStringDump(this.out, out_len);\n                            if (plainString && (plainString.indexOf(\"{\") !== -1 || plainString.indexOf(\"data\") !== -1 || plainString.indexOf(\"uuid\") !== -1)) {\n                                console.log(\"\\n==========================================\");\n                                console.log(\"[\ud83d\udd13 DECRYPTED PAYLOAD] L\u00e4nge: \" + out_len + \" Bytes\");\n                                console.log(\"------------------------------------------\");\n                                console.log(plainString);\n                                console.log(\"==========================================\\n\");\n                            }\n                        }\n                    }\n                } catch (e) {\n                    // Silent fail to prevent crashes\n                }\n            }\n        });\n    }\n}, 500);"
        },
        {
            "id": "c65d2c9f",
            "name": "0014",
            "code": "setTimeout(function() {\n    console.log(\"[*] GHOST PROTOCOL: Native Crypto Dumper (Bulletproof Edition) gestartet...\");\n\n    let initPtr = Module.findExportByName(null, \"EVP_CipherInit_ex\") || \n                  Module.findExportByName(null, \"EVP_DecryptInit_ex\");\n\n    if (initPtr) {\n        Interceptor.attach(initPtr, {\n            onEnter: function (args) {\n                let keyPtrStr = args[3].toString();\n                let ivPtrStr = args[4].toString();\n\n                if (keyPtrStr !== \"0x0\" || ivPtrStr !== \"0x0\") {\n                    console.log(\"\\n[\ud83d\udd11] Crypto Init gefangen!\");\n                    if (keyPtrStr !== \"0x0\") console.log(\"KEY Pointer: \" + keyPtrStr);\n                    if (ivPtrStr !== \"0x0\") console.log(\"IV  Pointer: \" + ivPtrStr);\n                }\n            }\n        });\n    }\n\n    let decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                           Module.findExportByName(null, \"EVP_CipherUpdate\");\n\n    if (decryptUpdatePtr) {\n        Interceptor.attach(decryptUpdatePtr, {\n            onEnter: function (args) {\n                // WICHTIG: Speichere die Pointer als Strings! Verhindert den TypeError in onLeave\n                this.outBufStr = args[1].toString();\n                this.outLenPtrStr = args[2].toString();\n            },\n            onLeave: function (retval) {\n                try {\n                    // Wenn der Return-Wert positiv ist und wir g\u00fcltige Pointer haben\n                    if (retval.toString() !== \"0x0\" && this.outBufStr !== \"0x0\" && this.outLenPtrStr !== \"0x0\") {\n                        \n                        // Pointer sicher wiederherstellen\n                        let outBuf = ptr(this.outBufStr);\n                        let outLenPtr = ptr(this.outLenPtrStr);\n                        \n                        // L\u00e4nge auslesen (Sichere Methode)\n                        let len = outLenPtr.readS32();\n                        \n                        // Sanity Check: L\u00e4nge muss plausibel sein (kleiner als 1 Megabyte)\n                        if (len > 0 && len < 1048576) { \n                            let plainText = outBuf.readUtf8String(len);\n                            \n                            // Filtern: Wir suchen nach API JSON\n                            if (plainText && (plainText.indexOf(\"{\") !== -1 || plainText.indexOf(\"data\") !== -1)) {\n                                console.log(\"\\n==========================================\");\n                                console.log(\"[\ud83d\udd13 DECRYPTED PAYLOAD] L\u00e4nge: \" + len + \" Bytes\");\n                                console.log(\"------------------------------------------\");\n                                console.log(plainText);\n                                console.log(\"==========================================\\n\");\n                            }\n                        }\n                    }\n                } catch (e) {\n                    // Silent fail, blockiert niemals den App-Start\n                }\n            }\n        });\n    }\n    \n    console.log(\"[*] Warten auf verschl\u00fcsselten API Traffic...\");\n}, 1000);"
        },
        {
            "id": "e096e68d",
            "name": "0015_Bread",
            "code": "console.log(\"[*] GHOST PROTOCOL: Native Crypto Dumper (Bare Metal Edition) gestartet...\");\n\ntry {\n    // Wir zielen nur auf das Update, wo der Payload ausgepackt wird\n    let decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                           Module.findExportByName(null, \"EVP_CipherUpdate\");\n\n    if (decryptUpdatePtr) {\n        console.log(\"[+] Hook scharfgeschaltet auf: \" + decryptUpdatePtr);\n\n        Interceptor.attach(decryptUpdatePtr, {\n            onEnter: function (args) {\n                // Pointer direkt in den Context legen\n                this.out = args[1];\n                this.outl_ptr = args[2];\n            },\n            onLeave: function (retval) {\n                try {\n                    // C-Pointer absichern (nur weiter, wenn R\u00fcckgabewert plausibel)\n                    if (retval.toInt32() > 0 && !this.out.isNull() && !this.outl_ptr.isNull()) {\n                        \n                        // L\u00e4nge aus dem Pointer lesen\n                        let len = this.outl_ptr.readInt();\n                        \n                        // Sanity Check: Daten m\u00fcssen > 0 und kleiner 5 MB sein\n                        if (len > 0 && len < 5000000) { \n                            let plainText = this.out.readUtf8String(len);\n                            \n                            // Filtern: Wir suchen nach dem entsperrten API JSON\n                            if (plainText !== null && (plainText.indexOf(\"{\") !== -1 || plainText.indexOf(\"data\") !== -1 || plainText.indexOf(\"uuid\") !== -1)) {\n                                console.log(\"\\n==========================================\");\n                                console.log(\"[\ud83d\udd13 DECRYPTED PAYLOAD] L\u00e4nge: \" + len + \" Bytes\");\n                                console.log(\"------------------------------------------\");\n                                console.log(plainText);\n                                console.log(\"==========================================\\n\");\n                            }\n                        }\n                    }\n                } catch (e) {\n                    // Silent fail - blockiert niemals den UI-Thread bei Speicherfehlern\n                }\n            }\n        });\n    } else {\n        console.log(\"[-] EVP_DecryptUpdate nicht im Speicher gefunden.\");\n    }\n} catch (err) {\n    console.log(\"[!] Setup Error: \" + err.message);\n}"
        },
        {
            "id": "f493df9e",
            "name": "0016_bread",
            "code": "setTimeout(function() {\n    console.log(\"[*] DIAGNOSTIC MODE: Frida ist aktiv und wartet...\");\n\n    // --- BEST PRACTICE 1: Globale Java-Crashes abfangen ---\n    if (Java.available) {\n        try {\n            Java.perform(function() {\n                const Thread = Java.use(\"java.lang.Thread\");\n                const Log = Java.use(\"android.util.Log\");\n                \n                const currentHandler = Thread.getDefaultUncaughtExceptionHandler();\n                const ExceptionHandler = Java.registerClass({\n                    name: 'com.re.DiagnosticExceptionHandler',\n                    implements: [Java.use('java.lang.Thread$UncaughtExceptionHandler')],\n                    methods: {\n                        uncaughtException: function (thread, throwable) {\n                            console.log(\"\\n[\ud83d\udca5 FATALER APP-CRASH ABGEFANGEN]\");\n                            console.log(Log.getStackTraceString(throwable));\n                            \n                            // App kontrolliert sterben lassen, nachdem wir den Log haben\n                            if (currentHandler !== null) {\n                                currentHandler.uncaughtException(thread, throwable);\n                            }\n                        }\n                    }\n                });\n                Thread.setDefaultUncaughtExceptionHandler(ExceptionHandler.$new());\n                console.log(\"[+] Java Crash-Detektor installiert.\");\n            });\n        } catch (err) {\n            console.log(\"[-] Java Perform Error (normal in manchen RN-Phasen): \" + err.message);\n        }\n    }\n\n    // --- BEST PRACTICE 2: C-Pointer isolieren & sichern ---\n    try {\n        let decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                               Module.findExportByName(null, \"EVP_CipherUpdate\");\n\n        if (decryptUpdatePtr) {\n            console.log(\"[+] Crypto-Hook scharfgeschaltet auf: \" + decryptUpdatePtr);\n\n            Interceptor.attach(decryptUpdatePtr, {\n                onEnter: function (args) {\n                    // Pointer zwingend als String sichern, um V8/QuickJS Garbage-Collection zu \u00fcberleben\n                    this.outStr = args[1].toString();\n                    this.outLenStr = args[2].toString();\n                },\n                onLeave: function (retval) {\n                    try {\n                        let retValStr = retval.toString();\n                        if (retValStr !== \"0x0\" && this.outStr !== \"0x0\" && this.outLenStr !== \"0x0\") {\n                            \n                            let len = ptr(this.outLenStr).readS32();\n                            \n                            // BEST PRACTICE 3: Sanity Check f\u00fcr Puffergr\u00f6\u00dfen vor dem Speicherzugriff\n                            if (len > 0 && len < 2000000) { \n                                let plainText = ptr(this.outStr).readUtf8String(len);\n                                \n                                if (plainText && (plainText.includes(\"{\") || plainText.includes(\"data\"))) {\n                                    console.log(\"\\n==========================================\");\n                                    console.log(\"[\ud83d\udd13 DECRYPTED PAYLOAD] L\u00e4nge: \" + len);\n                                    console.log(plainText);\n                                    console.log(\"==========================================\\n\");\n                                }\n                            }\n                        }\n                    } catch (e) {\n                        // Silent Fail - App darf bei Lesefehlern niemals crashen\n                    }\n                }\n            });\n        } else {\n            console.log(\"[-] EVP_DecryptUpdate nicht gefunden. Nutzt die App Custom-Crypto?\");\n        }\n    } catch (e) {\n        console.log(\"[!] Native Hook Error: \" + e.message);\n    }\n}, 500);"
        },
        {
            "id": "e57b64c0",
            "name": "0017_Bread",
            "code": "setTimeout(function() {\n    console.log(\"[*] GHOST PROTOCOL: Native Crypto Dumper (Bare-Metal) gestartet...\");\n\n    function safeStringDump(p, len) {\n        if (p.isNull() || len <= 0) return \"\";\n        try {\n            return p.readUtf8String(len);\n        } catch (e) {\n            return \"\";\n        }\n    }\n\n    try {\n        let decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                               Module.findExportByName(null, \"EVP_CipherUpdate\");\n\n        if (decryptUpdatePtr) {\n            console.log(\"[+] Crypto-Hook scharfgeschaltet auf: \" + decryptUpdatePtr);\n\n            Interceptor.attach(decryptUpdatePtr, {\n                onEnter: function (args) {\n                    // BEST PRACTICE 2: Pointer als Strings sichern\n                    this.outStr = args[1].toString();\n                    this.outLenStr = args[2].toString();\n                },\n                onLeave: function (retval) {\n                    try {\n                        if (retval.toString() !== \"0x0\" && this.outStr !== \"0x0\" && this.outLenStr !== \"0x0\") {\n                            \n                            // BEST PRACTICE 2: Pointer aus String wiederherstellen\n                            let outLenPtr = ptr(this.outLenStr);\n                            let outBufPtr = ptr(this.outStr);\n                            \n                            // Sicheres Auslesen der L\u00e4nge\n                            let len = outLenPtr.readS32();\n                            \n                            // BEST PRACTICE 3: Puffergr\u00f6\u00dfe plausibilisieren (Max 5MB)\n                            if (len > 0 && len < 5000000) { \n                                let plainText = safeStringDump(outBufPtr, len);\n                                \n                                // Filtern nach API-Daten\n                                if (plainText && (plainText.indexOf(\"{\") !== -1 || plainText.indexOf(\"data\") !== -1 || plainText.indexOf(\"uuid\") !== -1)) {\n                                    console.log(\"\\n==========================================\");\n                                    console.log(\"[\ud83d\udd13 DECRYPTED PAYLOAD] L\u00e4nge: \" + len + \" Bytes\");\n                                    console.log(\"------------------------------------------\");\n                                    console.log(plainText);\n                                    console.log(\"==========================================\\n\");\n                                }\n                            }\n                        }\n                    } catch (e) {\n                        // BEST PRACTICE 4: Silent Fail\n                    }\n                }\n            });\n        } else {\n            console.log(\"[-] EVP_DecryptUpdate nicht gefunden.\");\n        }\n    } catch (e) {\n        console.log(\"[!] Native Hook Error: \" + e.message);\n    }\n}, 1000);"
        },
        {
            "id": "61ac9c81",
            "name": "0018_Bread",
            "code": "setTimeout(function() {\n    send(\"[*] GHOST PROTOCOL: Native Crypto Dumper (Bare-Metal) gestartet...\");\n\n    function safeStringDump(p, len) {\n        if (p.isNull() || len <= 0) return \"\";\n        try {\n            return p.readUtf8String(len);\n        } catch (e) {\n            return \"\";\n        }\n    }\n\n    try {\n        let decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                               Module.findExportByName(null, \"EVP_CipherUpdate\");\n\n        if (decryptUpdatePtr) {\n            send(\"[+] Crypto-Hook scharfgeschaltet auf: \" + decryptUpdatePtr);\n\n            Interceptor.attach(decryptUpdatePtr, {\n                onEnter: function (args) {\n                    // BEST PRACTICE 2: Pointer als Strings sichern\n                    this.outStr = args[1].toString();\n                    this.outLenStr = args[2].toString();\n                },\n                onLeave: function (retval) {\n                    try {\n                        if (retval.toString() !== \"0x0\" && this.outStr !== \"0x0\" && this.outLenStr !== \"0x0\") {\n                            \n                            // BEST PRACTICE 2: Pointer aus String wiederherstellen\n                            let outLenPtr = ptr(this.outLenStr);\n                            let outBufPtr = ptr(this.outStr);\n                            \n                            // Sicheres Auslesen der L\u00e4nge\n                            let len = outLenPtr.readS32();\n                            \n                            // BEST PRACTICE 3: Puffergr\u00f6\u00dfe plausibilisieren (Max 5MB)\n                            if (len > 0 && len < 5000000) { \n                                let plainText = safeStringDump(outBufPtr, len);\n                                \n                                // Filtern nach API-Daten\n                                if (plainText && (plainText.indexOf(\"{\") !== -1 || plainText.indexOf(\"data\") !== -1 || plainText.indexOf(\"uuid\") !== -1)) {\n                                    send(\"\\n==========================================\");\n                                    send(\"[\ud83d\udd13 DECRYPTED PAYLOAD] L\u00e4nge: \" + len + \" Bytes\");\n                                    send(\"------------------------------------------\");\n                                    send(plainText);\n                                    send(\"==========================================\\n\");\n                                }\n                            }\n                        }\n                    } catch (e) {\n                        // BEST PRACTICE 4: Silent Fail\n                    }\n                }\n            });\n        } else {\n            send(\"[-] EVP_DecryptUpdate nicht gefunden.\");\n        }\n    } catch (e) {\n        send(\"[!] Native Hook Error: \" + e.message);\n    }\n}, 1000);"
        },
        {
            "id": "2a26bd8b",
            "name": "0019_Bread",
            "code": "// Wrapper f\u00fcr das Python-Framework\nfunction log(msg) {\n    send(msg);\n}\n\nlog(\"[*] GHOST PROTOCOL: Native Crypto Dumper gestartet...\");\n\ntry {\n    // 1. Suche die Entschl\u00fcsselungsfunktion (Synchron!)\n    let decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\") || \n                           Module.findExportByName(null, \"EVP_CipherUpdate\");\n\n    if (decryptUpdatePtr) {\n        log(\"[+] Crypto-Hook gefunden auf Adresse: \" + decryptUpdatePtr);\n\n        Interceptor.attach(decryptUpdatePtr, {\n            onEnter: function (args) {\n                try {\n                    // Puffer und L\u00e4ngen-Pointer im Context f\u00fcr onLeave speichern\n                    this.outBuf = args[1];\n                    this.outLenPtr = args[2];\n                } catch (e) {\n                    log(\"[-] onEnter Error: \" + e.message + \"\\n\" + e.stack);\n                }\n            },\n            onLeave: function (retval) {\n                try {\n                    // C-Pointer validieren\n                    let retStr = retval.toString();\n                    if (retStr !== \"0x0\" && !this.outBuf.isNull() && !this.outLenPtr.isNull()) {\n                        \n                        // L\u00e4nge auslesen\n                        let len = this.outLenPtr.readInt();\n                        \n                        // Sanity Check: Daten m\u00fcssen > 0 und kleiner 5 MB sein\n                        if (len > 0 && len < 5000000) { \n                            let plainText = this.outBuf.readUtf8String(len);\n                            \n                            // Filtern: Wir suchen nach API JSON\n                            if (plainText && (plainText.indexOf(\"{\") !== -1 || plainText.indexOf(\"data\") !== -1 || plainText.indexOf(\"uuid\") !== -1)) {\n                                log(\"\\n========== [\ud83d\udd13 DECRYPTED] (\" + len + \" Bytes) ==========\");\n                                log(plainText);\n                                log(\"========================================================\\n\");\n                            }\n                        }\n                    }\n                } catch (e) {\n                    // Speicherlesefehler stumm schlucken, um die App nicht einzufrieren\n                    // log(\"[-] Memory Read Error: \" + e.message); \n                }\n            }\n        });\n        log(\"[+] Interceptor erfolgreich angebracht. Warte auf Netzwerk-Daten...\");\n    } else {\n        log(\"[-] EVP_DecryptUpdate wurde im RAM nicht gefunden.\");\n        log(\"[?] Hinweis: Eventuell l\u00e4dt die App ihre Crypto-Bibliothek erst verz\u00f6gert (dlopen).\");\n    }\n} catch (e) {\n    log(\"[!] Fatal Setup Error: \" + e.message + \"\\n\" + e.stack);\n}"
        },
        {
            "id": "e0302702",
            "name": "0020_Bread",
            "code": "// Wir nutzen ausschlie\u00dflich send(), um Abst\u00fcrze durch defekte console.log-Implementierungen zu verhindern.\nfunction log(msg) {\n    try {\n        send(msg);\n    } catch (e) {\n        // Falls send() scheitert, stummer Fallback\n    }\n}\n\ntry {\n    log(\"[1] Skript gestartet. Initialisiere...\");\n\n    log(\"[2] Pr\u00fcfe Frida Module API...\");\n    var modApi = Module;\n    log(\"[3] Module API Typ: \" + typeof modApi);\n\n    log(\"[4] Suche nach EVP_DecryptUpdate...\");\n    var decryptUpdatePtr = null;\n    \n    try {\n        decryptUpdatePtr = Module.findExportByName(null, \"EVP_DecryptUpdate\");\n        log(\"[5] Suche 1 abgeschlossen. Pointer: \" + decryptUpdatePtr);\n    } catch (err1) {\n        log(\"[5.1] Fehler bei Suche 1: \" + err1.message);\n    }\n\n    if (!decryptUpdatePtr) {\n        log(\"[6] EVP_DecryptUpdate nicht gefunden. Suche EVP_CipherUpdate...\");\n        try {\n            decryptUpdatePtr = Module.findExportByName(null, \"EVP_CipherUpdate\");\n            log(\"[7] Suche 2 abgeschlossen. Pointer: \" + decryptUpdatePtr);\n        } catch (err2) {\n            log(\"[7.1] Fehler bei Suche 2: \" + err2.message);\n        }\n    }\n\n    log(\"[8] Werte Pointer aus...\");\n    if (decryptUpdatePtr) {\n        log(\"[9] Pointer ist g\u00fcltig. Lade Interceptor API...\");\n        var interceptorApi = Interceptor;\n        log(\"[10] Interceptor API Typ: \" + typeof interceptorApi);\n\n        log(\"[11] F\u00fchre Interceptor.attach aus...\");\n        Interceptor.attach(decryptUpdatePtr, {\n            onEnter: function (args) {\n                try {\n                    // Keine Logs hier, um Spam und Timing-Probleme zu vermeiden\n                    this.outStr = args[1].toString();\n                    this.lenStr = args[2].toString();\n                } catch (e) {\n                    log(\"[HOOK onEnter] Fehler: \" + e.message);\n                }\n            },\n            onLeave: function (retval) {\n                try {\n                    var retStr = retval.toString();\n                    if (retStr !== \"0x0\" && this.outStr !== \"0x0\" && this.lenStr !== \"0x0\") {\n                        \n                        var lenPtr = ptr(this.lenStr);\n                        var len = lenPtr.readS32();\n                        \n                        if (len > 0 && len < 5000000) { \n                            var plainText = ptr(this.outStr).readUtf8String(len);\n                            \n                            if (plainText && (plainText.indexOf(\"{\") !== -1 || plainText.indexOf(\"data\") !== -1)) {\n                                log(\"\\n========== [\ud83d\udd13 DECRYPTED] (\" + len + \" Bytes) ==========\");\n                                log(plainText);\n                                log(\"========================================================\\n\");\n                            }\n                        }\n                    }\n                } catch (e) {\n                    // Silent fail\n                }\n            }\n        });\n        log(\"[12] Interceptor.attach erfolgreich! Warten auf Daten...\");\n    } else {\n        log(\"[13] Abbruch: Keine Crypto-Funktion im RAM gefunden.\");\n    }\n\n    log(\"[14] Setup-Phase abgeschlossen.\");\n\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack);\n}"
        },
        {
            "id": "4d18e96f",
            "name": "0021_Bread",
            "code": "function log(msg) {\n    try {\n        send(msg);\n    } catch (e) {}\n}\n\ntry {\n    log(\"[1] Skript gestartet. Nutze ApiResolver f\u00fcr sichere RAM-Suche...\");\n\n    var targetPtr = null;\n    // ApiResolver ist immun gegen die Module.findExportByName Bugs in QuickJS\n    var resolver = new ApiResolver(\"module\");\n\n    var matches = resolver.enumerateMatches(\"exports:*!EVP_DecryptUpdate\");\n    if (matches.length > 0) {\n        targetPtr = matches[0].address;\n        log(\"[2] EVP_DecryptUpdate gefunden auf: \" + targetPtr);\n    } else {\n        log(\"[2] EVP_DecryptUpdate nicht gefunden, suche EVP_CipherUpdate...\");\n        matches = resolver.enumerateMatches(\"exports:*!EVP_CipherUpdate\");\n        if (matches.length > 0) {\n            targetPtr = matches[0].address;\n            log(\"[3] EVP_CipherUpdate gefunden auf: \" + targetPtr);\n        }\n    }\n\n    if (targetPtr) {\n        log(\"[4] Bringe Hook an...\");\n        Interceptor.attach(targetPtr, {\n            onEnter: function (args) {\n                try {\n                    // Pointer als Strings sichern\n                    this.outStr = args[1].toString();\n                    this.lenStr = args[2].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (retval.toString() !== \"0x0\" && this.outStr !== \"0x0\" && this.lenStr !== \"0x0\") {\n                        \n                        var len = ptr(this.lenStr).readS32();\n                        \n                        // Sanity Check: Daten m\u00fcssen > 0 und < 5 MB sein\n                        if (len > 0 && len < 5000000) { \n                            var plainText = ptr(this.outStr).readUtf8String(len);\n                            \n                            // Filtern nach JSON und \"data\"\n                            if (plainText && (plainText.indexOf(\"{\") !== -1 || plainText.indexOf(\"data\") !== -1)) {\n                                log(\"\\n========== [\ud83d\udd13 DECRYPTED] (\" + len + \" Bytes) ==========\");\n                                log(plainText);\n                                log(\"========================================================\\n\");\n                            }\n                        }\n                    }\n                } catch (e) {\n                    // Stummer Fallback bei Speicherlesefehlern\n                }\n            }\n        });\n        log(\"[5] Hook aktiv! Warte auf entschl\u00fcsselte API-Daten...\");\n    } else {\n        log(\"[-] Abbruch: Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "f49153a2",
            "name": "0022_Passive",
            "code": "function log(msg) {\n    try {\n        send(msg);\n    } catch (e) {}\n}\n\ntry {\n    log(\"[1] Skript gestartet. Nutze ApiResolver f\u00fcr sichere RAM-Suche...\");\n\n    var targetPtr = null;\n    var resolver = new ApiResolver(\"module\");\n\n    var matches = resolver.enumerateMatches(\"exports:*!EVP_DecryptUpdate\");\n    if (matches.length > 0) {\n        targetPtr = matches[0].address;\n        log(\"[2] EVP_DecryptUpdate gefunden auf: \" + targetPtr);\n    } else {\n        log(\"[2] EVP_DecryptUpdate nicht gefunden, suche EVP_CipherUpdate...\");\n        matches = resolver.enumerateMatches(\"exports:*!EVP_CipherUpdate\");\n        if (matches.length > 0) {\n            targetPtr = matches[0].address;\n            log(\"[3] EVP_CipherUpdate gefunden auf: \" + targetPtr);\n        }\n    }\n\n    if (targetPtr) {\n        log(\"[4] Pointer validiert. STOPP VOR DEM HOOK!\");\n        log(\"[5] Wenn die App jetzt NICHT crasht, erkennt DexGuard den Interceptor.attach.\");\n        \n        // Der Hook ist absichtlich deaktiviert, um die Anti-Hooking-Erkennung zu testen\n        /*\n        Interceptor.attach(targetPtr, {\n            onEnter: function (args) { ... }\n        });\n        */\n    } else {\n        log(\"[-] Abbruch: Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "bd43dfce",
            "name": "0023_Extract",
            "code": "// ============================================================================\n// REGEL 1: Framework-Logging via send() anstatt console.log()\n// ============================================================================\nfunction log(msg) {\n    try {\n        send(msg);\n    } catch (e) {}\n}\n\n// ============================================================================\n// REGEL 4: Diagnostic Try-Catch auf Root-Ebene\n// ============================================================================\ntry {\n    // REGEL 5: Extensive Debugging Prints[cite: 4]\n    log(\"[1] Frida-Skript f\u00fcr MediatorImpl initialisiert. Warte auf Dalvik/ART...\");\n\n    Java.perform(function () {\n        try {\n            log(\"[2] Java.perform gestartet. Suche MediatorImpl...\");\n            \n            var MediatorImpl = Java.use(\"com.adc.nativemediator.MediatorImpl\");\n            var StringClass = Java.use(\"java.lang.String\");\n\n            if (MediatorImpl) {\n                log(\"[3] Klasse MediatorImpl gefunden. Bringe Hook an request-E0qYoyE an...\");\n\n                // ---------------------------------------------------------\n                // HOOK 1: Ausgehende API-Requests\n                // ---------------------------------------------------------\n                MediatorImpl[\"request-E0qYoyE\"].implementation = function (topic, data, timeout, callback) {\n                    try {\n                        log(\"\\n========== [\ud83d\udce4 OUTGOING BRIDGE REQUEST] ==========\");\n                        log(\"[+] Topic: \" + topic);\n                        if (data !== null) {\n                            var dataStr = StringClass.$new(data).toString();\n                            log(\"[+] Payload (Bytes: \" + data.length + \"):\");\n                            log(dataStr);\n                        } else {\n                            log(\"[+] Payload: null\");\n                        }\n                        log(\"==================================================\");\n                    } catch (innerErr) {\n                        log(\"[!] Fehler im request-Hook: \" + innerErr.message);\n                    }\n                    \n                    // Originale Ausf\u00fchrung gew\u00e4hrleisten\n                    return this[\"request-E0qYoyE\"](topic, data, timeout, callback);\n                };\n\n                log(\"[4] Hook f\u00fcr request-E0qYoyE aktiv.\");\n\n                // ---------------------------------------------------------\n                // HOOK 2: Eingehende API-Responses (R\u00fcckkanal der Go-Bridge)\n                // ---------------------------------------------------------\n                log(\"[5] Bringe Hook an publish an (Incoming Data)...\");\n                try {\n                    MediatorImpl.publish.overload('java.lang.String', '[B').implementation = function(topic, data) {\n                        try {\n                            log(\"\\n========== [\ud83d\udce5 INCOMING BRIDGE PUBLISH] ==========\");\n                            log(\"[+] Topic: \" + topic);\n                            if (data !== null) {\n                                var dataStr = StringClass.$new(data).toString();\n                                log(\"[+] Payload (Bytes: \" + data.length + \"):\");\n                                log(dataStr);\n                            } else {\n                                log(\"[+] Payload: null\");\n                            }\n                            log(\"==================================================\");\n                        } catch (innerErr) {\n                            log(\"[!] Fehler im publish-Hook: \" + innerErr.message);\n                        }\n                        \n                        // Originale Ausf\u00fchrung gew\u00e4hrleisten\n                        return this.publish(topic, data);\n                    };\n                    log(\"[6] Hook f\u00fcr publish aktiv.\");\n                } catch (e2) {\n                    log(\"[-] Konnte publish nicht hooken (evtl. andere Signatur): \" + e2.message);\n                }\n                \n                log(\"[7] Setup vollst\u00e4ndig abgeschlossen. App kann nun bedient werden.\");\n\n            } else {\n                log(\"[-] FATAL: MediatorImpl nicht gefunden.\");\n            }\n        } catch (err) {\n            // Diagnostic Try-Catch f\u00fcr den Java-Kontext\n            log(\"[!] Fehler in Java.perform: \" + (err.stack || err.message));\n        }\n    });\n\n} catch (globalErr) {\n    // REGEL 4: Stille Abst\u00fcrze abfangen und Stacktrace senden[cite: 4]\n    log(\"[!] GLOBAL FATAL ERROR: \" + (globalErr.stack || globalErr.message));\n}"
        },
        {
            "id": "77c9170e",
            "name": "0024_Hook",
            "code": "function log(msg) {\n    try {\n        send(msg);\n    } catch (e) {}\n}\n\ntry {\n    log(\"[1] Skript gestartet. Nutze ApiResolver f\u00fcr sichere RAM-Suche...\");\n\n    var targetPtr = null;\n    // ApiResolver ist immun gegen die Module.findExportByName Bugs in QuickJS\n    var resolver = new ApiResolver(\"module\");\n    \n    // Suche nach der Hermes evaluate Methode in C++\n    var matches = resolver.enumerateMatches(\"exports:*!_ZN6hermes2vm7Runtime8evaluate*\");\n    \n    if (matches.length > 0) {\n        targetPtr = matches[0].address;\n        log(\"[2] Hermes evaluate gefunden auf: \" + targetPtr);\n    } else {\n        log(\"[2] Hermes evaluate nicht gefunden. L\u00e4uft die App auf V8?\");\n    }\n\n    if (targetPtr) {\n        log(\"[3] Bringe Hook an...\");\n        Interceptor.attach(targetPtr, {\n            onEnter: function (args) {\n                try {\n                    // Pointer sicher als String auslesen\n                    var sourceCode = ptr(args[1].toString()).readUtf8String();\n                    \n                    if (sourceCode && sourceCode.indexOf(\"secureApiData\") !== -1) {\n                        log(\"[4] BINGO! JS-Bundle mit ALE-Logik im RAM gefunden!\");\n                        \n                        var path = \"/data/data/org.nativescript.LibreLinkUp/files/dumped_bundle.js\";\n                        var file = new File(path, \"w\");\n                        file.write(sourceCode);\n                        file.flush();\n                        file.close();\n                        \n                        log(\"[5] Bundle erfolgreich gespeichert unter: \" + path);\n                    }\n                } catch (e) {\n                    // Stummer Fallback bei Speicherlesefehlern in onEnter\n                }\n            }\n        });\n        log(\"[4] Hook aktiv! Warte auf Ausf\u00fchrung des JS-Bundles...\");\n    } else {\n        log(\"[-] Abbruch: Javascript-Engine-Funktionen im RAM nicht auffindbar.\");\n    }\n} catch (globalErr) {\n    // Diagnostic Try-Catch Rule\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "d3a39db9",
            "name": "0025",
            "code": "function log(msg) {\n    try { send(msg); } catch (e) {}\n}\n\ntry {\n    log(\"[1] Skript gestartet. Warte lautlos auf libhermesvm.so via dlopen...\");\n\n    // ApiResolver umgeht Module-Namespace Bugs in QuickJS\n    var dlopenPtr = null;\n    var resolver = new ApiResolver(\"module\");\n    var dlMatches = resolver.enumerateMatches(\"exports:*!android_dlopen_ext\");\n    if (dlMatches.length > 0) {\n        dlopenPtr = dlMatches[0].address;\n    } else {\n        var dlMatches2 = resolver.enumerateMatches(\"exports:*!dlopen\");\n        if (dlMatches2.length > 0) dlopenPtr = dlMatches2[0].address;\n    }\n\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try {\n                    // Pointer-Safeguarding im this-Kontext\n                    this.path = args[0].readUtf8String();\n                } catch (e) {\n                    this.path = \"\";\n                }\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.path && this.path.indexOf(\"libhermesvm.so\") !== -1) {\n                        log(\"[2] libhermesvm.so geladen! Suche evaluate-Funktion...\");\n                        \n                        var res = new ApiResolver(\"module\");\n                        // Suche nach der C++ Methode hermes::vm::Runtime::evaluate\n                        var evalMatches = res.enumerateMatches(\"exports:*!_ZN6hermes2vm7Runtime8evaluate*\");\n                        \n                        if (evalMatches.length > 0) {\n                            var evalPtr = evalMatches[0].address;\n                            log(\"[3] Hermes evaluate gefunden auf: \" + evalPtr);\n                            \n                            Interceptor.attach(evalPtr, {\n                                onEnter: function (evalArgs) {\n                                    try {\n                                        // Die C++ Struct llvh::StringRef \u00fcbergibt Daten im Register x1 und L\u00e4nge im Register x2\n                                        var len = evalArgs[2].toInt32();\n                                        \n                                        // Sanity Check (>0 und <20MB) um Engine-Crashes zu vermeiden\n                                        if (len > 0 && len < 20000000) { \n                                            var sourceCode = evalArgs[1].readUtf8String(len);\n                                            \n                                            // Pr\u00fcfen ob es das Haupt-Bundle mit unseren Schl\u00fcsseln ist\n                                            if (sourceCode && sourceCode.indexOf(\"secureApiData\") !== -1) {\n                                                log(\"[4] \ud83d\udd13 BINGO! JS-Bundle Klartext gefunden! Speichere...\");\n                                                \n                                                var outPath = \"/data/data/org.nativescript.LibreLinkUp/files/dumped_bundle.js\";\n                                                var f = new File(outPath, \"w\");\n                                                f.write(sourceCode);\n                                                f.flush();\n                                                f.close();\n                                                \n                                                log(\"[5] Erfolgreich gespeichert: \" + outPath);\n                                            }\n                                        }\n                                    } catch (e) {\n                                        // Stummer Fallback bei Lesefehlern\n                                    }\n                                }\n                            });\n                            log(\"[+] Hook auf evaluate scharfgeschaltet. Warte auf JS-Execution...\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    } else {\n        log(\"[-] dlopen nicht gefunden.\");\n    }\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "8521fdd5",
            "name": "0026",
            "code": "function log(msg) {\n    try { send(msg); } catch (e) {}\n}\n\ntry {\n    log(\"[1] Skript gestartet. Warte lautlos auf libhermesvm.so via dlopen...\");\n\n    var dlopenPtr = null;\n    var resolver = new ApiResolver(\"module\");\n    var dlMatches = resolver.enumerateMatches(\"exports:*!android_dlopen_ext\");\n    if (dlMatches.length > 0) {\n        dlopenPtr = dlMatches[0].address;\n    } else {\n        var dlMatches2 = resolver.enumerateMatches(\"exports:*!dlopen\");\n        if (dlMatches2.length > 0) dlopenPtr = dlMatches2[0].address;\n    }\n\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[0].readUtf8String();\n                } catch (e) {\n                    this.path = \"\";\n                }\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.path && this.path.indexOf(\"libhermesvm.so\") !== -1) {\n                        log(\"[2] libhermesvm.so geladen! Suche evaluate-Funktion...\");\n                        \n                        var res = new ApiResolver(\"module\");\n                        var evalMatches = res.enumerateMatches(\"exports:*!_ZN6hermes2vm7Runtime8evaluate*\");\n                        \n                        if (evalMatches.length > 0) {\n                            var evalPtr = evalMatches[0].address;\n                            log(\"[3] Hermes evaluate gefunden auf: \" + evalPtr);\n                            \n                            Interceptor.attach(evalPtr, {\n                                onEnter: function (evalArgs) {\n                                    try {\n                                        var len = evalArgs[2].toInt32();\n                                        if (len > 0 && len < 20000000) { \n                                            var sourceCode = evalArgs[1].readUtf8String(len);\n                                            if (sourceCode && sourceCode.indexOf(\"secureApiData\") !== -1) {\n                                                log(\"[4] \ud83d\udd13 BINGO! JS-Bundle Klartext gefunden! Speichere...\");\n                                                \n                                                var outPath = \"/data/data/org.nativescript.LibreLinkUp/files/dumped_bundle.js\";\n                                                var f = new File(outPath, \"w\");\n                                                f.write(sourceCode);\n                                                f.flush();\n                                                f.close();\n                                                \n                                                log(\"[5] Erfolgreich gespeichert: \" + outPath);\n                                            }\n                                        }\n                                    } catch (e) {}\n                                }\n                            });\n                            log(\"[+] Hook auf evaluate scharfgeschaltet. Warte auf JS-Execution...\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    } else {\n        log(\"[-] dlopen nicht gefunden.\");\n    }\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "22e68c3f",
            "name": "0027_Cipher_extract",
            "code": "function log(msg) {\n    try {\n        send(msg);\n    } catch (e) {}\n}\n\n// Hilfsfunktion: Wandelt ein ArrayBuffer in einen sauberen Hex-String um\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] Framework-konformes Skript gestartet. Initialisiere ApiResolver...\");\n\n    var targetPtr = null;\n    var resolver = new ApiResolver(\"module\");\n\n    // Suche nach der BoringSSL Initialisierungsfunktion f\u00fcr Verschl\u00fcsselung/Entschl\u00fcsselung\n    var matches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (matches.length > 0) {\n        targetPtr = matches[0].address;\n        log(\"[2] EVP_CipherInit_ex gefunden auf: \" + targetPtr);\n    } else {\n        log(\"[2] EVP_CipherInit_ex nicht gefunden, suche EVP_DecryptInit_ex...\");\n        matches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (matches.length > 0) {\n            targetPtr = matches[0].address;\n            log(\"[3] EVP_DecryptInit_ex gefunden auf: \" + targetPtr);\n        }\n    }\n\n    if (targetPtr) {\n        log(\"[4] Bringe Native Hook an...\");\n        Interceptor.attach(targetPtr, {\n            onEnter: function (args) {\n                try {\n                    // Signatur: int EVP_CipherInit_ex(EVP_CIPHER_CTX *ctx, const EVP_CIPHER *type, ENGINE *impl, const unsigned char *key, const unsigned char *iv, int enc)\n                    // args[3] ist der Pointer auf den Klartext-Schl\u00fcssel (Key)\n                    \n                    if (args[3].toString() !== \"0x0\") {\n                        // Pointer-Safeguarding: Als String im Callback-Kontext speichern\n                        this.keyPtrStr = args[3].toString();\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyPtr = ptr(this.keyPtrStr);\n                        // Wir lesen 16 Bytes (128 Bit) - die Standardl\u00e4nge des Abbott Session Keys\n                        var keyBuffer = keyPtr.readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        // Filtere auf nicht-leere Keys, um Rauschen zu minimieren\n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Pointer: \" + this.keyPtrStr);\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {\n                    // Stummer Fallback bei unlesbaren Speicherbereichen\n                }\n            }\n        });\n        log(\"[5] Hook aktiv! Warte auf die Generierung des Session Keys durch die App...\");\n    } else {\n        log(\"[-] Abbruch: BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "6627775b",
            "name": "0028_GHOST",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {}\n}\n\n// Hilfsfunktion: Wandelt ein ArrayBuffer in einen sauberen Hex-String um\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[*] ULTIMATE GHOST PROTOCOL: Phantom-Blocker + JNI-Spoofer + Crypto Extractor gestartet...\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n\n    // =========================================================================\n    // STRATEGIE 1: PHANTOM-LOAD-BLOCKER (Extrahiert aus V18/V20_Defuser)\n    // =========================================================================\n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    // Wir holen uns einen g\u00fctigen Handle der harmlosen liblog.so\n    var validDummyHandle = rawDlopen(Memory.allocUtf8String(\"liblog.so\"), 1); \n\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                \n                // Lobotomie: Wenn der RASP geladen werden soll, laden wir stattdessen liblog.so!\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1) {\n                    log(\"[\ud83d\udd25 PHANTOM-BLOCKER] RASP (libdb0c) blockiert -> liblog.so untergejubelt!\");\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    // =========================================================================\n    // STRATEGIE 2: JNI DLSYM-SPOOFER (Extrahiert aus V17_JNIMirror)\n    // =========================================================================\n    var fakeStackFrames = new NativeCallback(function() { return 0; }, 'void', ['pointer', 'pointer', 'int', 'int']);\n    var fakeRead = new NativeCallback(function() { return 1024; }, 'int64', ['pointer', 'pointer']);\n    var fakeUniversal = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer', 'pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.symName) {\n                    // F\u00e4ngt alle obfuscated JNI Methoden ab (verhindert den o.MediaBrowserCompat Crash)\n                    if (this.symName.indexOf(\"Java_o_\") !== -1) {\n                        log(\"[\ud83d\udd25 JNI-SPOOFER] RASP Kill-Switch neutralisiert: \" + this.symName);\n                        retval.replace(fakeUniversal);\n                    } \n                    else if (this.symName.indexOf(\"stackFrames\") !== -1) {\n                        retval.replace(fakeStackFrames);\n                    } \n                    else if (this.symName.indexOf(\"Java_o_onResult_read\") !== -1 || this.symName.indexOf(\"read__\") !== -1) {\n                        retval.replace(fakeRead);\n                    }\n                }\n            }\n        });\n    }\n\n    // =========================================================================\n    // STRATEGIE 3: CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var targetPtr = null;\n    var resolver = new ApiResolver(\"module\");\n\n    var matches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (matches.length > 0) targetPtr = matches[0].address;\n    else {\n        matches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (matches.length > 0) targetPtr = matches[0].address;\n    }\n\n    if (targetPtr) {\n        Interceptor.attach(targetPtr, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") {\n                        this.keyPtrStr = args[3].toString();\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyPtr = ptr(this.keyPtrStr);\n                        var keyBuffer = keyPtr.readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Pointer: \" + this.keyPtrStr);\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Crypto-Hook aktiv! Setup abgeschlossen.\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "f6dc99fb",
            "name": "0030_GHOST",
            "code": "// @ts-nocheck\n\n// 1. NATIVES LOGCAT-SETUP (Zwingend f\u00fcr den autonomen Script-Modus)\nvar logDef = new NativeFunction(Module.findExportByName('liblog.so', '__android_log_print'), 'int', ['int', 'pointer', 'pointer']);\nvar TAG = Memory.allocUtf8String(\"KIPPY-GHOST\");\n\n// Wir nutzen ausschlie\u00dflich send(), um Abst\u00fcrze durch defekte console.log-Implementierungen zu verhindern.\nfunction log(msg) {\n    try {\n        send(msg);\n    } catch (e) {\n        // Falls send() scheitert, stummer Fallback\n    }\n}\n\ntry {\n    log(\"[1] ULTIMATE GHOST: Starte RASP-Blocker & Hermes-Dumper...\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n\n    // --- RASP PHANTOM BLOCKER ---\n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    var validDummyHandle = rawDlopen(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) { this.libPath = \"\"; }\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                \n                // Lobotomie: RASP blockieren und liblog.so unterjubeln\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1) {\n                    log(\"[\ud83d\udd25 PHANTOM] RASP Ladeversuch blockiert!\");\n                    retval.replace(validDummyHandle);\n                }\n                \n                // Hermes fangen, sobald es geladen wird\n                else if (this.libPath.indexOf(\"libhermesvm.so\") !== -1) {\n                    log(\"[2] libhermesvm.so geladen! Suche evaluate-Funktion...\");\n                    var res = new ApiResolver(\"module\");\n                    var evalMatches = res.enumerateMatches(\"exports:*!_ZN6hermes2vm7Runtime8evaluate*\");\n                    \n                    if (evalMatches.length > 0) {\n                        var evalPtr = evalMatches[0].address;\n                        log(\"[3] Hermes evaluate gefunden auf: \" + evalPtr);\n                        \n                        Interceptor.attach(evalPtr, {\n                            onEnter: function (evalArgs) {\n                                try {\n                                    var len = evalArgs[2].toInt32();\n                                    if (len > 0 && len < 20000000) { \n                                        var sourceCode = evalArgs[1].readUtf8String(len);\n                                        if (sourceCode && sourceCode.indexOf(\"secureApiData\") !== -1) {\n                                            log(\"[4] \ud83d\udd13 BINGO! JS-Bundle gefunden!\");\n                                            var snippet = sourceCode.substring(0, 300);\n                                            log(\"Snippet: \" + snippet + \"...\");\n                                            \n                                            var outPath = \"/data/data/org.nativescript.LibreLinkUp/files/dumped_bundle.js\";\n                                            var f = new File(outPath, \"w\");\n                                            f.write(sourceCode);\n                                            f.flush();\n                                            f.close();\n                                            log(\"[5] Bundle gespeichert in: \" + outPath);\n                                        }\n                                    }\n                                } catch (e) {}\n                            }\n                        });\n                    }\n                }\n            }\n        });\n    }\n\n    // --- JNI SPOOFER (Verhindert die RuntimeException Crash-Schleife) ---\n    var fakeStackFrames = new NativeCallback(function() { return 0; }, 'void', ['pointer', 'pointer', 'int', 'int']);\n    var fakeRead = new NativeCallback(function() { return 1024; }, 'int64', ['pointer', 'pointer']);\n    var fakeUniversal = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer', 'pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.symName) {\n                    if (this.symName.indexOf(\"Java_o_\") !== -1) {\n                        log(\"[\ud83d\udd25 SPOOFER] Neutralisiere JNI Call: \" + this.symName);\n                        retval.replace(fakeUniversal);\n                    } else if (this.symName.indexOf(\"stackFrames\") !== -1) {\n                        retval.replace(fakeStackFrames);\n                    } else if (this.symName.indexOf(\"read__\") !== -1 || this.symName.indexOf(\"Java_o_onResult_read\") !== -1) {\n                        retval.replace(fakeRead);\n                    }\n                }\n            }\n        });\n    }\n\n} catch (err) {\n    log(\"[!] FATAL ERROR: \" + err.message);\n}"
        },
        {
            "id": "e6e14227",
            "name": "0031_GHOST",
            "code": "// @ts-nocheck\n\n// --- NATIVE LOGCAT ---\nvar logDef = new NativeFunction(Module.findExportByName('liblog.so', '__android_log_print'), 'int', ['int', 'pointer', 'pointer']);\nvar TAG = Memory.allocUtf8String(\"KIPPY-GHOST\");\n\nfunction log(msg) {\n    try { logDef(4, TAG, Memory.allocUtf8String(msg)); } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[*] GHOST PROTOCOL: Clean Room Spoofer & Crypto Extractor gestartet...\");\n\n    var libc = Process.findModuleByName(\"libc.so\");\n    var openPtr = libc.findExportByName(\"open\");\n    var openatPtr = libc.findExportByName(\"openat\");\n    var readPtr = libc.findExportByName(\"read\");\n\n    // Unsere Clean-Room Dateien im Dateisystem\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    \n    var trackedFds = {};\n\n    // 1. I/O REDIRECTION (Der Clean-Room)\n    function hookFileAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.path = args[pathIdx].readUtf8String();\n                    this.isStatusFile = false;\n\n                    if (!this.path) return;\n\n                    // A: Wenn der RASP das RAM-Layout scannen will, leiten wir auf die Textdatei um!\n                    if (this.path.indexOf(\"/proc/\") !== -1 && this.path.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                        log(\"[\ud83d\udd25 MATRIX] \" + funcName + \"() auf /proc/self/maps -> Umgeleitet auf clean_maps.txt!\");\n                    }\n                    // B: Debugger-Check abfangen (f\u00fcr den Read-Spoofer)\n                    else if (this.path.indexOf(\"/proc/\") !== -1 && this.path.indexOf(\"status\") !== -1) {\n                        this.isStatusFile = true;\n                    }\n                    // C: APK-Integrit\u00e4tspr\u00fcfung auf Original-APK umleiten\n                    else if (this.path.indexOf(\"base.apk\") !== -1 && this.path.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        log(\"[\ud83d\udd25 MATRIX] \" + funcName + \"() auf base.apk -> Umgeleitet auf clean_base.apk!\");\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.isStatusFile) {\n                    var fd = retval.toInt32();\n                    if (fd > 0) trackedFds[fd] = true;\n                }\n            }\n        });\n    }\n\n    hookFileAccess(openPtr, 0, \"open\");\n    hookFileAccess(openatPtr, 1, \"openat\");\n\n    // 2. READ SPOOFER (TracerPid f\u00e4lschen)\n    if (readPtr) {\n        Interceptor.attach(readPtr, {\n            onEnter: function (args) {\n                this.fd = args[0].toInt32();\n                this.buffer = args[1];\n            },\n            onLeave: function (retval) {\n                var bytesRead = retval.toInt32();\n                if (trackedFds[this.fd] && bytesRead > 0) {\n                    try {\n                        var content = this.buffer.readUtf8String(bytesRead);\n                        if (content.indexOf(\"TracerPid:\") !== -1) {\n                            var match = content.match(/TracerPid:\\s*(\\d+)/);\n                            if (match && match[1] !== \"0\") {\n                                var oldStr = match[0];\n                                var newStr = \"TracerPid:\\t0\";\n                                while(newStr.length < oldStr.length) newStr += \" \"; \n                                content = content.replace(oldStr, newStr);\n                                this.buffer.writeUtf8String(content);\n                                log(\"[\ud83d\udd25 MATRIX] TracerPid erfolgreich auf 0 gef\u00e4lscht!\");\n                            }\n                        }\n                    } catch (e) {}\n                }\n            }\n        });\n    }\n\n    // 3. CRYPTO EXTRACTOR\n    var targetPtr = null;\n    var resolver = new ApiResolver(\"module\");\n\n    var matches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (matches.length > 0) targetPtr = matches[0].address;\n    else {\n        matches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (matches.length > 0) targetPtr = matches[0].address;\n    }\n\n    if (targetPtr) {\n        Interceptor.attach(targetPtr, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") {\n                        this.keyPtrStr = args[3].toString();\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Crypto-Hook aktiv! Setup abgeschlossen.\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL ERROR: \" + err.message);\n}"
        },
        {
            "id": "72aee908",
            "name": "0032_GHOST",
            "code": "// @ts-nocheck\n\n// Regel 1: Framework-Logging via send()[cite: 9]\nfunction log(msg) {\n    try { send(msg); } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\n// Regel 4: Diagnostic Try-Catch auf der obersten Ebene[cite: 9]\ntry {\n    log(\"[*] GHOST PROTOCOL: Synchronisiertes Skript erfolgreich geladen!\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n\n    // --- STRATEGIE 1: PHANTOM-LOAD-BLOCKER ---\n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    var validDummyHandle = rawDlopen(Memory.allocUtf8String(\"liblog.so\"), 1); \n\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) { this.libPath = \"\"; }\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                \n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1 || this.libPath.indexOf(\"promon\") !== -1) {\n                    log(\"[\ud83d\udd25 PHANTOM] RASP Ladeversuch (\" + this.libPath + \") blockiert!\");\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    // --- STRATEGIE 2: JNI DLSYM-SPOOFER ---\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.symName) {\n                    if (this.symName.indexOf(\"Java_o_\") !== -1 || this.symName.indexOf(\"stackFrames\") !== -1 || this.symName.indexOf(\"read__\") !== -1) {\n                        log(\"[\ud83d\udd25 SPOOFER] Neutralisiere RASP JNI Call: \" + this.symName);\n                        retval.replace(fakeJni);\n                    }\n                }\n            }\n        });\n    }\n\n    // --- STRATEGIE 3: CRYPTO EXTRACTOR ---\n    var resolver = new ApiResolver(\"module\");\n    var matches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    var cryptoTarget = matches.length > 0 ? matches[0].address : null;\n\n    if (!cryptoTarget) {\n        matches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (matches.length > 0) cryptoTarget = matches[0].address;\n    }\n\n    if (cryptoTarget) {\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    // Regel 3: Pointer-Safeguarding (Als String im Context sichern)[cite: 9]\n                    if (args[3].toString() !== \"0x0\") {\n                        this.keyPtrStr = args[3].toString();\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        // Pointer wiederherstellen und direkt auslesen[cite: 9]\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Crypto-Hook aktiv! Setup abgeschlossen.\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    // Regel 4: Fehler diagnostisch inklusive Stacktrace an das Framework senden[cite: 9]\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "27b9e9b4",
            "name": "0033_GHOST",
            "code": "// @ts-nocheck\n\nvar logDef = new NativeFunction(Module.findExportByName('liblog.so', '__android_log_print'), 'int', ['int', 'pointer', 'pointer']);\nvar TAG = Memory.allocUtf8String(\"KIPPY-GHOST\");\n\nfunction log(msg) {\n    try { send(msg); logDef(4, TAG, Memory.allocUtf8String(msg)); \n        } catch (e) {}\n}\n\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[*] GHOST PROTOCOL: Starte Java-OOM-Slayer & Crypto Extractor...\");\n\n    var libdl = Process.findModuleByName(\"libdl.so\") || Process.findModuleByName(\"libc.so\");\n    var dlopenPtr = libdl.findExportByName(\"android_dlopen_ext\") || libdl.findExportByName(\"dlopen\");\n    var dlsymPtr = libdl.findExportByName(\"dlsym\");\n\n    var rawDlopen = new NativeFunction(libdl.findExportByName(\"dlopen\"), 'pointer', ['pointer', 'int']);\n    var validDummyHandle = rawDlopen(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    // 1. PHANTOM BLOCKER\n    if (dlopenPtr) {\n        Interceptor.attach(dlopenPtr, {\n            onEnter: function (args) {\n                try { this.libPath = args[0].readUtf8String(); } catch (e) { this.libPath = \"\"; }\n            },\n            onLeave: function (retval) {\n                if (!this.libPath) return;\n                \n                // RASP physisch aussperren\n                if (this.libPath.indexOf(\"libdb0c.so\") !== -1 || this.libPath.indexOf(\"dexguard\") !== -1 || this.libPath.indexOf(\"promon\") !== -1) {\n                    log(\"[\ud83d\udd25 PHANTOM] RASP blockiert!\");\n                    retval.replace(validDummyHandle);\n\n                    // =================================================================\n                    // DER FIX: Java OOM-Bombe entsch\u00e4rfen (Nachdem JNI gestartet ist)\n                    // =================================================================\n                    try {\n                        Java.perform(function() {\n                            var OOM = Java.use(\"java.lang.OutOfMemoryError\");\n                            OOM.$init.overload('java.lang.String').implementation = function(msg) {\n                                log(\"[\ud83d\udd25 OOM-DEFUSER] RASP-Bombe entsch\u00e4rft! Geforderte Allokation: \" + msg);\n                                // Wir unterbrechen den Absturz und geben einfach leere Referenz zur\u00fcck\n                                return; \n                            };\n                            log(\"[+] Java OOM-Slayer scharfgestellt.\");\n                        });\n                    } catch (e) {\n                        log(\"[-] Java.perform noch nicht bereit.\");\n                    }\n                }\n            }\n        });\n    }\n\n    // 2. JNI SPOOFER\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { this.symName = args[1].readUtf8String(); } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.symName) {\n                    if (this.symName.indexOf(\"Java_o_\") !== -1 || this.symName.indexOf(\"stackFrames\") !== -1 || this.symName.indexOf(\"read__\") !== -1) {\n                        retval.replace(fakeJni);\n                    }\n                }\n            }\n        });\n    }\n\n    // 3. CRYPTO EXTRACTOR\n    var resolver = new ApiResolver(\"module\");\n    var matches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    var cryptoTarget = matches.length > 0 ? matches[0].address : null;\n\n    if (!cryptoTarget) {\n        matches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (matches.length > 0) cryptoTarget = matches[0].address;\n    }\n\n    if (cryptoTarget) {\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message);\n}"
        },
        {
            "id": "43d1a8ab",
            "name": "0034_GHOST",
            "code": "// @ts-nocheck\n\n// Regel 1: Framework-Logging via send()[cite: 10]\nfunction log(msg) {\n    try { send(msg); } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\n// Regel 4: Diagnostic Try-Catch auf der obersten Ebene[cite: 10]\ntry {\n    // Regel 5: Nummeriertes Print-Debugging[cite: 10]\n    log(\"[1] BARE-METAL GHOST PROTOCOL gestartet (Strict Framework Mode)...\");\n\n    // Regel 6: ApiResolver statt Module.findExportByName![cite: 10]\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    log(\"[2] Suche libc.so Exports...\");\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var fstatatPtr = findExport(\"fstatat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n\n    log(\"[3] libc.so Exports aufgel\u00f6st. openPtr: \" + openPtr);\n\n    var ghostPathMem = Memory.allocUtf8String(\"/system/lib64/lib_gibts_nicht_1337.so\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // DIE KERNEL-SPOOFER (Die letzte Verteidigungslinie)\n    // =========================================================================\n    function slayRaspAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    // Regel 3: Pointer als Strings absichern[cite: 10]\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    // A: Der ultimative Load-Blocker (Macht die App blind f\u00fcr den RASP)\n                    if (this.pathStr.indexOf(\"libdb0c.so\") !== -1 || this.pathStr.indexOf(\"dexguard\") !== -1 || this.pathStr.indexOf(\"promon\") !== -1) {\n                        args[pathIdx] = ghostPathMem;\n                        log(\"[\ud83d\udd25 BARE-METAL] RASP-Ladeversuch im Kernel (\" + funcName + \") vernichtet!\");\n                    }\n                    // B: Die APK-Hash Redirection\n                    else if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        log(\"[\ud83d\udd25 BARE-METAL] \" + funcName + \"() auf Clean-APK umgeleitet!\");\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[4] Bringe Kernel-Spoofer an...\");\n    slayRaspAccess(openPtr, 0, \"open\");\n    slayRaspAccess(openatPtr, 1, \"openat\");\n    slayRaspAccess(statPtr, 0, \"stat\");\n    slayRaspAccess(fstatatPtr, 1, \"fstatat\");\n    slayRaspAccess(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // JNI-SPOOFER (Sichert die Java-VM ab)\n    // =========================================================================\n    log(\"[5] Setze JNI-Spoofer (dlsym)...\");\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"read__\") !== -1) {\n                            log(\"[\ud83d\udd25 SPOOFER] JNI Call neutralisiert: \" + this.symStr);\n                            retval.replace(fakeJni);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // CRYPTO EXTRACTOR (BoringSSL)\n    // =========================================================================\n    log(\"[6] Suche BoringSSL Crypto-Exports...\");\n    var targetPtr = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) targetPtr = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) targetPtr = evpMatches[0].address;\n    }\n\n    if (targetPtr) {\n        log(\"[7] Crypto-Export gefunden, setze Hook...\");\n        Interceptor.attach(targetPtr, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyPtr = ptr(this.keyPtrStr);\n                        var keyBuffer = keyPtr.readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[8] Setup vollst\u00e4ndig abgeschlossen!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (globalErr) {\n    // Regel 4: F\u00e4ngt Fehler sauber ab und loggt sie in deine Console[cite: 10]\n    log(\"[!] FATAL SCRIPT ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack);\n}"
        },
        {
            "id": "4a4245ad",
            "name": "0035_GHOST",
            "code": "// @ts-nocheck\n\nvar logDef = new NativeFunction(Module.findExportByName('liblog.so', '__android_log_print'), 'int', ['int', 'pointer', 'pointer']);\nvar TAG = Memory.allocUtf8String(\"KIPPY-GHOST\");\n\nfunction log(msg) {\n    try { logDef(4, TAG, Memory.allocUtf8String(msg)); } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V2) gestartet...\");\n\n    // =========================================================================\n    // DER JAVA SYSTEM.LOAD HOOK (Verhindert die NullPointerException)\n    // =========================================================================\n    try {\n        Java.perform(function() {\n            var Runtime = Java.use(\"java.lang.Runtime\");\n            \n            // Fange System.load() ab, das den absoluten Pfad verwendet\n            Runtime.load0.overload('java.lang.Class', 'java.lang.String').implementation = function(clazz, filename) {\n                if (filename && (filename.indexOf(\"libdb0c.so\") !== -1 || filename.indexOf(\"dexguard\") !== -1 || filename.indexOf(\"promon\") !== -1)) {\n                    log(\"[\ud83d\udd25 JAVA-DEFUSER] RASP Ladeversuch (System.load) verschluckt: \" + filename);\n                    // Wir geben einfach zur\u00fcck (void), als w\u00e4re alles okay. Kein Crash!\n                    return; \n                }\n                // F\u00fcr alle anderen (legitimen) Bibliotheken rufen wir das Original auf\n                return this.load0(clazz, filename);\n            };\n\n            // Zur Sicherheit fangen wir auch System.loadLibrary() ab\n            Runtime.loadLibrary0.overload('java.lang.ClassLoader', 'java.lang.Class', 'java.lang.String').implementation = function(classLoader, clazz, libname) {\n                if (libname && (libname.indexOf(\"db0c\") !== -1 || libname.indexOf(\"dexguard\") !== -1 || libname.indexOf(\"promon\") !== -1)) {\n                    log(\"[\ud83d\udd25 JAVA-DEFUSER] RASP Ladeversuch (System.loadLibrary) verschluckt: \" + libname);\n                    return;\n                }\n                return this.loadLibrary0(classLoader, clazz, libname);\n            };\n            log(\"[2] Java-Defuser Wache aufgestellt!\");\n        });\n    } catch (e) {\n        log(\"[-] Konnte Java-Hooks nicht setzen: \" + e.message);\n    }\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var fstatatPtr = findExport(\"fstatat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n\n    var ghostPathMem = Memory.allocUtf8String(\"/system/lib64/lib_gibts_nicht_1337.so\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // DIE KERNEL-SPOOFER\n    // =========================================================================\n    function slayRaspAccess(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"libdb0c.so\") !== -1 || this.pathStr.indexOf(\"dexguard\") !== -1 || this.pathStr.indexOf(\"promon\") !== -1) {\n                        args[pathIdx] = ghostPathMem;\n                        log(\"[\ud83d\udd25 BARE-METAL] RASP Kernel-Scan (\" + funcName + \") vernichtet!\");\n                    }\n                    else if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    slayRaspAccess(openPtr, 0, \"open\");\n    slayRaspAccess(openatPtr, 1, \"openat\");\n    slayRaspAccess(statPtr, 0, \"stat\");\n    slayRaspAccess(fstatatPtr, 1, \"fstatat\");\n    slayRaspAccess(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // JNI-SPOOFER\n    // =========================================================================\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"read__\") !== -1) {\n                            retval.replace(fakeJni);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // CRYPTO EXTRACTOR (BoringSSL)\n    // =========================================================================\n    var targetPtr = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) targetPtr = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) targetPtr = evpMatches[0].address;\n    }\n\n    if (targetPtr) {\n        Interceptor.attach(targetPtr, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[3] Crypto-Hook aktiv! Setup abgeschlossen.\");\n    }\n\n} catch (globalErr) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack);\n}"
        },
        {
            "id": "b0d80d09",
            "name": "0036_GHOST",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[*] INITIALISIERUNG: BARE-METAL GHOST PROTOCOL gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    // Eine kleine Hilfsfunktion, um C-Exporte absolut sicher zu finden\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var fstatatPtr = findExport(\"fstatat\");\n    var accessPtr = findExport(\"access\");\n    var dlopenPtr = findExport(\"dlopen\");\n    var androidDlopenExtPtr = findExport(\"android_dlopen_ext\");\n\n    // Unsere \"Clean Room\" Pfade im RAM\n    var ghostPathMem = Memory.allocUtf8String(\"/system/lib64/lib_gibts_nicht.so\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 1. DER ULTIMATIVE LOAD-BLOCKER (C-Ebene)\n    // =========================================================================\n    // Wenn die Java-VM versucht, den RASP \u00fcber System.load() in den RAM zu ziehen,\n    // sagen wir dlopen(), dass es eine harmlose Logging-Lib laden soll.\n    var validDummyHandle = new NativeFunction(dlopenPtr, 'pointer', ['pointer', 'int'])(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    function blockRaspLoad(ptr) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.libPathStr = args[0].readUtf8String();\n                    if (!this.libPathStr) return;\n\n                    if (this.libPathStr.indexOf(\"libdb0c.so\") !== -1 || this.libPathStr.indexOf(\"dexguard\") !== -1 || this.libPathStr.indexOf(\"promon\") !== -1) {\n                        log(\"[\ud83d\udd25 DLOPEN BLOCKER] RASP Ladeversuch abgefangen: \" + this.libPathStr);\n                        this.shouldBlock = true;\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.shouldBlock) {\n                    retval.replace(validDummyHandle);\n                    log(\"[\ud83d\udd25 DLOPEN BLOCKER] RASP erfolgreich neutralisiert! App l\u00e4dt weiter...\");\n                }\n            }\n        });\n    }\n\n    blockRaspLoad(dlopenPtr);\n    blockRaspLoad(androidDlopenExtPtr);\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (T\u00e4uscht Checksummen & Memory-Scans)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var pathStr = args[pathIdx].readUtf8String();\n                    if (!pathStr) return;\n\n                    // APK Hash umleiten\n                    if (pathStr.indexOf(\"base.apk\") !== -1 && pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        // log(\"[\ud83d\udd25 KERNEL SPOOFER] \" + funcName + \"() umgeleitet auf clean_base.apk\");\n                    }\n                    // Falls der RASP versucht, direkt das Dateisystem nach sich selbst zu scannen\n                    else if (pathStr.indexOf(\"libdb0c.so\") !== -1) {\n                        args[pathIdx] = ghostPathMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(fstatatPtr, 1, \"fstatat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. JNI SPOOFER (Befriedigt Java, falls RASP-Reste aufgerufen werden)\n    // =========================================================================\n    var dlsymPtr = findExport(\"dlsym\");\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"read__\") !== -1) {\n                            log(\"[\ud83d\udd25 JNI SPOOFER] Neutralisiere JNI Call: \" + this.symStr);\n                            retval.replace(fakeJni);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Crypto-Hook aktiv! Setup abgeschlossen. Setze App-Ausf\u00fchrung fort...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "4032c335",
            "name": "0037_GHOST",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[*] INITIALISIERUNG: BARE-METAL GHOST PROTOCOL (V3) gestartet...\");\n\n    // =========================================================================\n    // 0. DAS JAVA AUFFANGNETZ (Verhindert die NullPointerException)\n    // =========================================================================\n    try {\n        Java.perform(function() {\n            var System = Java.use(\"java.lang.System\");\n            var Runtime = Java.use(\"java.lang.Runtime\");\n\n            // F\u00e4ngt System.load(null) oder System.load(\"...libdb0c.so\") ab\n            System.load.overload('java.lang.String').implementation = function(filename) {\n                if (filename === null || filename.indexOf(\"libdb0c.so\") !== -1 || filename.indexOf(\"dexguard\") !== -1) {\n                    log(\"[\ud83d\udd25 JAVA-DEFUSER] System.load(\" + filename + \") blockiert (NPE verhindert)!\");\n                    return; // Stiller Abbruch, VM l\u00e4uft weiter\n                }\n                this.load(filename);\n            };\n\n            // F\u00e4ngt Runtime.load0 ab (die tiefere Java-Ebene)\n            Runtime.load0.overload('java.lang.Class', 'java.lang.String').implementation = function(clazz, filename) {\n                if (filename === null || filename.indexOf(\"libdb0c.so\") !== -1 || filename.indexOf(\"dexguard\") !== -1) {\n                    log(\"[\ud83d\udd25 JAVA-DEFUSER] Runtime.load0(\" + filename + \") blockiert!\");\n                    return; \n                }\n                this.load0(clazz, filename);\n            };\n            \n            // F\u00e4ngt Standard loadLibrary ab\n            System.loadLibrary.overload('java.lang.String').implementation = function(libname) {\n                if (libname === null || libname.indexOf(\"db0c\") !== -1 || libname.indexOf(\"dexguard\") !== -1) {\n                    log(\"[\ud83d\udd25 JAVA-DEFUSER] System.loadLibrary(\" + libname + \") blockiert!\");\n                    return;\n                }\n                this.loadLibrary(libname);\n            };\n            \n            log(\"[+] Java-Defuser (NPE-Schutz) aktiv!\");\n        });\n    } catch (e) {\n        log(\"[-] Warnung: Java-Hooks konnten nicht sofort gesetzt werden: \" + e.message);\n    }\n\n    // =========================================================================\n    // 1. C-LEVEL KONFIGURATION & RESOLVER\n    // =========================================================================\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var fstatatPtr = findExport(\"fstatat\");\n    var accessPtr = findExport(\"access\");\n    var dlopenPtr = findExport(\"dlopen\");\n    var androidDlopenExtPtr = findExport(\"android_dlopen_ext\");\n\n    var ghostPathMem = Memory.allocUtf8String(\"/system/lib64/lib_gibts_nicht.so\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 2. DER ULTIMATIVE LOAD-BLOCKER (dlopen)\n    // =========================================================================\n    var validDummyHandle = new NativeFunction(dlopenPtr, 'pointer', ['pointer', 'int'])(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    function blockRaspLoad(ptr) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.libPathStr = args[0].readUtf8String();\n                    if (!this.libPathStr) return;\n\n                    if (this.libPathStr.indexOf(\"libdb0c.so\") !== -1 || this.libPathStr.indexOf(\"dexguard\") !== -1 || this.libPathStr.indexOf(\"promon\") !== -1) {\n                        log(\"[\ud83d\udd25 C-BLOCKER] RASP Ladeversuch (dlopen) abgefangen: \" + this.libPathStr);\n                        this.shouldBlock = true;\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.shouldBlock) {\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    blockRaspLoad(dlopenPtr);\n    blockRaspLoad(androidDlopenExtPtr);\n\n    // =========================================================================\n    // 3. KERNEL I/O SPOOFER (T\u00e4uscht Checksummen & Memory-Scans)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var pathStr = args[pathIdx].readUtf8String();\n                    if (!pathStr) return;\n\n                    if (pathStr.indexOf(\"base.apk\") !== -1 && pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    }\n                    else if (pathStr.indexOf(\"libdb0c.so\") !== -1) {\n                        args[pathIdx] = ghostPathMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(fstatatPtr, 1, \"fstatat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 4. JNI SPOOFER\n    // =========================================================================\n    var dlsymPtr = findExport(\"dlsym\");\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"read__\") !== -1) {\n                            retval.replace(fakeJni);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Crypto-Hook aktiv! Setup abgeschlossen. Setze App-Ausf\u00fchrung fort...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "eaa3a309",
            "name": "0038_GHOST",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} // Framework Logging Rule[cite: 4]\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V4) gestartet...\");\n\n    // ApiResolver umgeht Module-Shadowing in QuickJS[cite: 4]\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var fstatatPtr = findExport(\"fstatat\");\n    var accessPtr = findExport(\"access\");\n    var dlopenPtr = findExport(\"dlopen\");\n    var androidDlopenExtPtr = findExport(\"android_dlopen_ext\");\n\n    // DER FIX: Wir geben eine EXISTIERENDE, harmlose Datei an, um Java-NPEs zu verhindern!\n    var ghostPathMem = Memory.allocUtf8String(\"/system/lib64/liblog.so\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 2. DER ULTIMATIVE LOAD-BLOCKER (dlopen)\n    // =========================================================================\n    var validDummyHandle = new NativeFunction(dlopenPtr, 'pointer', ['pointer', 'int'])(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    function blockRaspLoad(ptr) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    // Pointer Safeguarding[cite: 4]\n                    this.libPathStr = args[0].readUtf8String();\n                    if (!this.libPathStr) return;\n\n                    if (this.libPathStr.indexOf(\"libdb0c.so\") !== -1 || this.libPathStr.indexOf(\"dexguard\") !== -1 || this.libPathStr.indexOf(\"promon\") !== -1) {\n                        log(\"[\ud83d\udd25 C-BLOCKER] RASP Ladeversuch (dlopen) abgefangen: \" + this.libPathStr);\n                        this.shouldBlock = true;\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.shouldBlock) {\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    log(\"[2] Setze Load-Blocker...\");\n    blockRaspLoad(dlopenPtr);\n    blockRaspLoad(androidDlopenExtPtr);\n\n    // =========================================================================\n    // 3. KERNEL I/O SPOOFER (T\u00e4uscht Checksummen & Memory-Scans)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    }\n                    else if (this.pathStr.indexOf(\"libdb0c.so\") !== -1) {\n                        args[pathIdx] = ghostPathMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[3] Setze Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(fstatatPtr, 1, \"fstatat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 4. JNI SPOOFER\n    // =========================================================================\n    var dlsymPtr = findExport(\"dlsym\");\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[4] Setze JNI Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"read__\") !== -1) {\n                            retval.replace(fakeJni);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. Setze App-Ausf\u00fchrung fort...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    // Diagnostic Try-Catch Rule[cite: 4]\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "35dda928",
            "name": "0039_GHOST",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V5) gestartet...\");\n\n    // =========================================================================\n    // 0. DAS JAVA AUFFANGNETZ\n    // =========================================================================\n    try {\n        Java.perform(function() {\n            var System = Java.use(\"java.lang.System\");\n            var Runtime = Java.use(\"java.lang.Runtime\");\n\n            System.load.overload('java.lang.String').implementation = function(filename) {\n                if (filename === null || filename.indexOf(\"libdb0c.so\") !== -1 || filename.indexOf(\"dexguard\") !== -1) {\n                    return; \n                }\n                this.load(filename);\n            };\n\n            Runtime.load0.overload('java.lang.Class', 'java.lang.String').implementation = function(clazz, filename) {\n                if (filename === null || filename.indexOf(\"libdb0c.so\") !== -1 || filename.indexOf(\"dexguard\") !== -1) {\n                    return; \n                }\n                this.load0(clazz, filename);\n            };\n            \n            System.loadLibrary.overload('java.lang.String').implementation = function(libname) {\n                if (libname === null || libname.indexOf(\"db0c\") !== -1 || libname.indexOf(\"dexguard\") !== -1) {\n                    return;\n                }\n                this.loadLibrary(libname);\n            };\n        });\n    } catch (e) {\n        log(\"[-] Warnung: Java-Hooks konnten nicht gesetzt werden: \" + e.message);\n    }\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var fstatatPtr = findExport(\"fstatat\");\n    var accessPtr = findExport(\"access\");\n    var dlopenPtr = findExport(\"dlopen\");\n    var androidDlopenExtPtr = findExport(\"android_dlopen_ext\");\n\n    var ghostPathMem = Memory.allocUtf8String(\"/system/lib64/lib_gibts_nicht.so\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 2. DER ULTIMATIVE LOAD-BLOCKER\n    // =========================================================================\n    var validDummyHandle = new NativeFunction(dlopenPtr, 'pointer', ['pointer', 'int'])(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    function blockRaspLoad(ptr) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.libPathStr = args[0].readUtf8String();\n                    if (!this.libPathStr) return;\n\n                    if (this.libPathStr.indexOf(\"libdb0c.so\") !== -1 || this.libPathStr.indexOf(\"dexguard\") !== -1 || this.libPathStr.indexOf(\"promon\") !== -1) {\n                        log(\"[\ud83d\udd25 C-BLOCKER] RASP Ladeversuch abgefangen: \" + this.libPathStr);\n                        this.shouldBlock = true;\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.shouldBlock) {\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    blockRaspLoad(dlopenPtr);\n    blockRaspLoad(androidDlopenExtPtr);\n\n    // =========================================================================\n    // 3. PR\u00c4ZISER KERNEL I/O SPOOFER\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    var pathStr = args[pathIdx].readUtf8String();\n                    if (!pathStr) return;\n\n                    // PR\u00c4ZISION: Leite base.apk NUR um, wenn sie zum LibreLinkUp Paket geh\u00f6rt!\n                    if (pathStr.indexOf(\"base.apk\") !== -1 && pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        // log(\"[\ud83d\udd25 KERNEL SPOOFER] \" + funcName + \"() umgeleitet auf clean_base.apk\");\n                    }\n                    else if (pathStr.indexOf(\"libdb0c.so\") !== -1) {\n                        args[pathIdx] = ghostPathMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(fstatatPtr, 1, \"fstatat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 4. JNI SPOOFER\n    // =========================================================================\n    var dlsymPtr = findExport(\"dlsym\");\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"read__\") !== -1) {\n                            retval.replace(fakeJni);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup erfolgreich. Bereit f\u00fcr AES-Extraktion...\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "6efef70b",
            "name": "0040",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} // Framework Rule 1: Immer send()\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V5) - No Java Bridge Mode...\");\n\n    // ApiResolver umgeht das Module-Bug Risiko in QuickJS\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var dlopenPtr = findExport(\"dlopen\");\n    var androidDlopenExtPtr = findExport(\"android_dlopen_ext\");\n    var dlsymPtr = findExport(\"dlsym\");\n\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (Spooft nur Maps & APK, l\u00e4sst libdb0c.so in Ruhe!)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String(); // Pointer Safeguarding\n                    if (!this.pathStr) return;\n\n                    // Spoofing der RAM-Maps gegen Frida-Detection\n                    if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                        log(\"[\ud83d\udd25 KERNEL] \" + funcName + \"() auf /proc/self/maps -> clean_maps.txt\");\n                    }\n                    // Spoofing der APK gegen Hash-Checks (NUR f\u00fcr LibreLinkUp, nicht f\u00fcr WebView)\n                    else if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1) {\n                        args[pathIdx] = cleanApkMem;\n                        log(\"[\ud83d\udd25 KERNEL] \" + funcName + \"() auf base.apk -> clean_base.apk\");\n                    }\n                    // WICHTIG: Wir spoofen hier NICHT libdb0c.so, um die Java NPE zu vermeiden!\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere selektiven I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n\n    // =========================================================================\n    // 3. DER ULTIMATIVE LOAD-BLOCKER (dlopen)\n    // =========================================================================\n    var validDummyHandle = new NativeFunction(dlopenPtr, 'pointer', ['pointer', 'int'])(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    function blockRaspLoad(ptr) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.libPathStr = args[0].readUtf8String();\n                    if (!this.libPathStr) return;\n\n                    if (this.libPathStr.indexOf(\"libdb0c.so\") !== -1 || this.libPathStr.indexOf(\"dexguard\") !== -1 || this.libPathStr.indexOf(\"promon\") !== -1) {\n                        log(\"[\ud83d\udd25 DLOPEN] RASP Ladeversuch blockiert: \" + this.libPathStr);\n                        this.shouldBlock = true;\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.shouldBlock) {\n                    retval.replace(validDummyHandle); // Schiebt liblog.so unter\n                }\n            }\n        });\n    }\n\n    log(\"[3] Aktiviere dlopen Blocker...\");\n    blockRaspLoad(dlopenPtr);\n    blockRaspLoad(androidDlopenExtPtr);\n\n    // =========================================================================\n    // 4. JNI UNIVERSAL SPOOFER (Verhindert den 4GB OOM Crash)\n    // =========================================================================\n    var fakeJni = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[4] Aktiviere JNI Universal Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    this.symStr = args[1].readUtf8String(); \n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    // Wenn Android eine Native-Methode sucht (Java_...) und diese NICHT findet (retval === 0x0)\n                    if (this.symStr && this.symStr.indexOf(\"Java_\") === 0) {\n                        if (retval.isNull()) {\n                            log(\"[\ud83d\udd25 DLSYM] Simuliere fehlende JNI-Methode: \" + this.symStr);\n                            retval.replace(fakeJni); // Beantwortet RASP Methoden mit einem blinden Return\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. Setze App-Ausf\u00fchrung fort...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    // Diagnostic Try-Catch Rule\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "796e2177",
            "name": "0041",
            "code": "// @ts-nocheck\n\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V6 - Tarpit Edition) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlopenPtr = findExport(\"dlopen\");\n    var androidDlopenExtPtr = findExport(\"android_dlopen_ext\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var sleepPtr = findExport(\"sleep\");\n\n    var ghostPathMem = Memory.allocUtf8String(\"/system/lib64/lib_gibts_nicht.so\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 2. DER ULTIMATIVE LOAD-BLOCKER (dlopen)\n    // =========================================================================\n    var validDummyHandle = new NativeFunction(dlopenPtr, 'pointer', ['pointer', 'int'])(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    function blockRaspLoad(ptr) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.libPathStr = args[0].readUtf8String();\n                    if (!this.libPathStr) return;\n\n                    if (this.libPathStr.indexOf(\"libdb0c.so\") !== -1 || this.libPathStr.indexOf(\"dexguard\") !== -1 || this.libPathStr.indexOf(\"promon\") !== -1) {\n                        log(\"[\ud83d\udd25 C-BLOCKER] RASP Ladeversuch abgefangen: \" + this.libPathStr);\n                        this.shouldBlock = true;\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.shouldBlock) {\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    log(\"[2] Setze Load-Blocker...\");\n    blockRaspLoad(dlopenPtr);\n    blockRaspLoad(androidDlopenExtPtr);\n\n    // =========================================================================\n    // 3. PR\u00c4ZISER KERNEL I/O SPOOFER (Fix f\u00fcr WebView SecurityException)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    // PR\u00c4ZISION: Leite base.apk NUR um, wenn sie zum LibreLinkUp Paket geh\u00f6rt!\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        // log(\"[\ud83d\udd25 KERNEL] \" + funcName + \"() umgeleitet auf clean_base.apk\");\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[3] Setze Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 4. JNI TARPIT (F\u00e4ngt den Background-Thread ein und verhindert den Crash)\n    // =========================================================================\n    var sleepFunc = sleepPtr ? new NativeFunction(sleepPtr, 'int', ['int']) : null;\n\n    var fakeJniTarpit = new NativeCallback(function() { \n        log(\"[\ud83e\uddca TARPIT] RASP Wachhund-Thread erfolgreich eingefroren!\");\n        // Die Teergrube: Der Thread verl\u00e4sst diese Schleife niemals.\n        if (sleepFunc) {\n            while (true) { sleepFunc(1000); }\n        } else {\n            while (true) {} // Fallback Busy-Wait\n        }\n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[4] Setze JNI Teergrube...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        // Greift NUR bei RASP-Methoden, l\u00e4sst Rive und WebView in Ruhe!\n                        if (this.symStr.indexOf(\"Java_o_\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1) {\n                            log(\"[\ud83d\udd25 DLSYM] Leite RASP-Methode in die Teergrube: \" + this.symStr);\n                            retval.replace(fakeJniTarpit);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. App ist gesichert und bereit!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "8b2d5586",
            "name": "0042",
            "code": "// @ts-nocheck\n\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V7 - Zombie Edition) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlopenPtr = findExport(\"dlopen\");\n    var androidDlopenExtPtr = findExport(\"android_dlopen_ext\");\n    var dlsymPtr = findExport(\"dlsym\");\n\n    var ghostPathMem = Memory.allocUtf8String(\"/system/lib64/lib_gibts_nicht.so\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 2. DER ULTIMATIVE LOAD-BLOCKER (dlopen)\n    // =========================================================================\n    var validDummyHandle = new NativeFunction(dlopenPtr, 'pointer', ['pointer', 'int'])(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    function blockRaspLoad(ptr) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.libPathStr = args[0].readUtf8String();\n                    if (!this.libPathStr) return;\n\n                    if (this.libPathStr.indexOf(\"libdb0c.so\") !== -1 || this.libPathStr.indexOf(\"dexguard\") !== -1 || this.libPathStr.indexOf(\"promon\") !== -1) {\n                        log(\"[\ud83d\udd25 C-BLOCKER] RASP Ladeversuch abgefangen: \" + this.libPathStr);\n                        this.shouldBlock = true;\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.shouldBlock) {\n                    retval.replace(validDummyHandle);\n                }\n            }\n        });\n    }\n\n    log(\"[2] Setze Load-Blocker...\");\n    blockRaspLoad(dlopenPtr);\n    blockRaspLoad(androidDlopenExtPtr);\n\n    // =========================================================================\n    // 3. PR\u00c4ZISER KERNEL I/O SPOOFER\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[3] Setze Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 4. JNI ZOMBIE SPOOFER (Sofortiger Return ohne Sleep)\n    // =========================================================================\n    // Erstelle einen Dummy-Callback, der JNI_Env und jclass/jobject als Parameter nimmt und SOFORT 0 zur\u00fcckgibt\n    var fakeJniZombie = new NativeCallback(function(env, clazz) { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[4] Setze JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        // Hookt spezifisch die Methoden, die der RASP sucht\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            log(\"[\ud83d\udd25 DLSYM] Zombie-Antwort f\u00fcr RASP-Methode: \" + this.symStr);\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. App ist gesichert und bereit!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "b282eeb9",
            "name": "0043_Trojan",
            "code": "// @ts-nocheck\n\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V8 - Trojan Horse) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (Macht den RASP komplett blind)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    // 1. Frida-Memory-Scans blockieren (Zeigt sauberen RAM)\n                    if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                        log(\"[\ud83d\udd25 KERNEL] \" + funcName + \"() umgeleitet: maps -> clean_maps.txt\");\n                    }\n                    // 2. APK-Integrit\u00e4tspr\u00fcfung f\u00e4lschen (Zeigt unmodifizierte APK)\n                    else if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        log(\"[\ud83d\udd25 KERNEL] \" + funcName + \"() umgeleitet: base.apk -> clean_base.apk\");\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    log(\"[3] RASP (libdb0c.so) darf jetzt laden. Er ist vollst\u00e4ndig blind.\");\n\n    // =========================================================================\n    // 4. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. App ist gesichert und bereit!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "c46b07dd",
            "name": "0044",
            "code": "// @ts-nocheck\n\n// Regel 1: Ausschlie\u00dflich synchrones send() Logging[cite: 3]\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\n// Regel 4: Globaler Diagnostic Try-Catch Block[cite: 3]\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V9 - Ghost Thread Edition) gestartet...\");\n\n    // Regel 6: ApiResolver statt fehleranf\u00e4lligem Module.findExportByName[cite: 3]\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var pthread_createPtr = findExport(\"pthread_create\");\n    var dl_iterate_phdrPtr = findExport(\"dl_iterate_phdr\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (Leitet base.apk Checksummen-Checks sicher um)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    // Regel 3: Pointer-Safeguarding[cite: 3]\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    // Pr\u00e4ziser Spoof nur f\u00fcr LibreLinkUp, verhindert WebView SecurityException\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        log(\"[\ud83d\udd25 I/O SPOOFER] \" + funcName + \"() umgeleitet auf clean_base.apk\");\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. GHOST THREAD INJECTION (T\u00f6tet den RASP Wachhund lautlos)\n    // =========================================================================\n    // Dieser Dummy-Thread macht absolut nichts und beendet sich sofort fehlerfrei.\n    var dummyStartRoutine = new NativeCallback(function(arg) {\n        log(\"[\ud83e\uddca GHOST THREAD] RASP-Wachhund erfolgreich isoliert und vaporisiert!\");\n        return ptr(0);\n    }, 'pointer', ['pointer']);\n\n    if (pthread_createPtr) {\n        log(\"[3] Aktiviere pthread_create Hook...\");\n        Interceptor.attach(pthread_createPtr, {\n            onEnter: function(args) {\n                try {\n                    var caller = Process.findModuleByAddress(this.returnAddress);\n                    if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1 || caller.name.indexOf(\"promon\") !== -1)) {\n                        log(\"[\ud83d\udd25 PTHREAD] RASP versucht Wachhund zu starten. Initiiere Ghost-Thread Swap!\");\n                        // Wir lassen den Thread erstellen, tauschen aber den Schadcode gegen unsere leere Funktion aus!\n                        args[2] = dummyStartRoutine;\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. MEMORY SCAN BLINDER (Verhindert Frida-Erkennung via dl_iterate_phdr)\n    // =========================================================================\n    // Ein leerer Callback, der dem RASP meldet: \"Ich habe null geladene Bibliotheken gefunden.\"\n    var dummyDlCallback = new NativeCallback(function(info, size, data) {\n        return 0; \n    }, 'int', ['pointer', 'size_t', 'pointer']);\n\n    if (dl_iterate_phdrPtr) {\n        log(\"[4] Aktiviere dl_iterate_phdr Blinder...\");\n        Interceptor.attach(dl_iterate_phdrPtr, {\n            onEnter: function(args) {\n                try {\n                    var caller = Process.findModuleByAddress(this.returnAddress);\n                    if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1 || caller.name.indexOf(\"promon\") !== -1)) {\n                        log(\"[\ud83d\udd25 DL_ITERATE] RASP Memory-Scan geblendet!\");\n                        // Wir ersetzen den RASP-Suchalgorithmus durch unsere leere Funktion\n                        args[0] = dummyDlCallback;\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    log(\"[5] Suche BoringSSL Crypto-Exporte...\");\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Crypto-Hook aktiv! Setup abgeschlossen. Setze App-Ausf\u00fchrung fort...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (globalErr) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack);\n}"
        },
        {
            "id": "d1451f5b",
            "name": "0045_SUCCES_APP_CLOAKING",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V10 - App Cloaking) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var mallocPtr = findExport(\"malloc\");\n    var mmapPtr = findExport(\"mmap\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    var cleanStringMem = Memory.allocUtf8String(\"nothing_to_see_here\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        log(\"[\ud83d\udd25 I/O SPOOFER] \" + funcName + \"() umgeleitet auf clean_base.apk\");\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                        log(\"[\ud83d\udd25 I/O SPOOFER] \" + funcName + \"() umgeleitet auf clean_maps.txt\");\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. JNI ANTI-REFLECTION & ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Anti-Reflection Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        // Wir beantworten ALLE RASP-bezogenen Aufrufe als Zombie\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            log(\"[\ud83d\udd25 DLSYM] Zombie-Antwort f\u00fcr RASP: \" + this.symStr);\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. OOM-SLAYER (C-Ebene Allokations-Filter)\n    // =========================================================================\n    // DexGuard verlangt 1.5 GB bis 5.3 GB um die VM zu crashen.\n    // Wir fangen mmap und malloc ab. Alles \u00fcber 1 GB wird auf 1 Byte reduziert!\n    var ONE_GIGABYTE = 1073741824;\n\n    if (mallocPtr) {\n        Interceptor.attach(mallocPtr, {\n            onEnter: function(args) {\n                try {\n                    var size = args[0].toInt32();\n                    if (size > ONE_GIGABYTE) {\n                        log(\"[\ud83d\udd25 OOM-SLAYER] malloc() Bombe entsch\u00e4rft! Geforderte Gr\u00f6\u00dfe: \" + size);\n                        args[0] = ptr(1); // \u00c4ndere Anforderung auf 1 Byte\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    if (mmapPtr) {\n        Interceptor.attach(mmapPtr, {\n            onEnter: function(args) {\n                try {\n                    var size = args[1].toInt32();\n                    if (size > ONE_GIGABYTE) {\n                        log(\"[\ud83d\udd25 OOM-SLAYER] mmap() Bombe entsch\u00e4rft! Geforderte Gr\u00f6\u00dfe: \" + size);\n                        args[1] = ptr(4096); // \u00c4ndere Anforderung auf 1 harmlose Page\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    log(\"[4] OOM-Slayer aktiv (Schwellenwert 1GB).\");\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP (libdb0c.so) l\u00e4dt...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "e148579c",
            "name": "0046",
            "code": "// @ts-nocheck\n\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V11 - JNIEnv Slayer) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. ART JNI-SLAYER (Blockiert RASP Exceptions und OOMs direkt in libart.so)\n    // =========================================================================\n    log(\"[3] Installiere ART JNI-Slayer...\");\n\n    // A) OOM-Slayer: F\u00e4ngt massive Array-Allokationen ab\n    var arrayMatches = resolver.enumerateMatches(\"exports:*libart.so!*New*Array*\");\n    arrayMatches.forEach(function(match) {\n        Interceptor.attach(match.address, {\n            onEnter: function(args) {\n                try {\n                    var size = args[1].toInt32();\n                    // Wenn RASP versucht, ein Array > 50 MB zu allokieren (OOM-Bombe)\n                    if (size > 50000000) { \n                        log(\"[\ud83d\udd25 JNI-SLAYER] OOM-Bombe in \" + match.name + \" entsch\u00e4rft! Geforderte Gr\u00f6\u00dfe: \" + size);\n                        args[1] = ptr(1); // Reduziere die Allokation heimlich auf 1 Byte\n                    }\n                } catch(e) {}\n            }\n        });\n    });\n\n    // B) Exception-Slayer: Blockiert ThrowNew f\u00fcr den RASP\n    var throwNewMatches = resolver.enumerateMatches(\"exports:*libart.so!*ThrowNew*\");\n    throwNewMatches.forEach(function(match) {\n        var origThrowNew = new NativeFunction(match.address, 'int', ['pointer', 'pointer', 'pointer']);\n        Interceptor.replace(match.address, new NativeCallback(function(env, clazz, msgPtr) {\n            var caller = Process.findModuleByAddress(this.returnAddress);\n            if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1 || caller.name.indexOf(\"promon\") !== -1)) {\n                var msg = \"unknown\";\n                try { if (!msgPtr.isNull()) msg = msgPtr.readUtf8String(); } catch(e){}\n                log(\"[\ud83d\udd25 JNI-SLAYER] RASP ThrowNew blockiert! Msg: \" + msg);\n                return 0; // JNI_OK - T\u00e4uscht dem RASP vor, dass die Exception erfolgreich geworfen wurde\n            }\n            return origThrowNew(env, clazz, msgPtr);\n        }, 'int', ['pointer', 'pointer', 'pointer']));\n    });\n\n    // C) FatalError-Slayer: Blockiert harte C-Abst\u00fcrze\n    var fatalMatches = resolver.enumerateMatches(\"exports:*libart.so!*FatalError*\");\n    fatalMatches.forEach(function(match) {\n        Interceptor.replace(match.address, new NativeCallback(function(env, msgPtr) {\n            var msg = \"unknown\";\n            try { if (!msgPtr.isNull()) msg = msgPtr.readUtf8String(); } catch(e){}\n            log(\"[\ud83d\udd25 JNI-SLAYER] FatalError blockiert! Msg: \" + msg);\n        }, 'void', ['pointer', 'pointer']));\n    });\n\n    // =========================================================================\n    // 4. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP l\u00e4dt, aber seine Waffen sind zerst\u00f6rt!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "fcf9ca0a",
            "name": "0047",
            "code": "// @ts-nocheck\n\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V12 - The Matrix) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (Die perfekte Illusion)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    // Spoofing der APK gegen Hash-Checks (NUR f\u00fcr LibreLinkUp)\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        // log(\"[\ud83d\udd25 KERNEL] \" + funcName + \"() umgeleitet auf clean_base.apk\");\n                    } \n                    // Spoofing der RAM-Maps gegen Frida-Detection\n                    else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                        // log(\"[\ud83d\udd25 KERNEL] \" + funcName + \"() umgeleitet auf clean_maps.txt\");\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Matrix I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    log(\"[3] RASP darf ungest\u00f6rt laden. Die Matrix ist aktiv.\");\n\n    // =========================================================================\n    // 4. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. App ist gesichert!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "c9438a7e",
            "name": "0048",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} // Framework Rule 1: Logging via send()[cite: 1]\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V13 - The Immortal) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\"); // Framework Rule 6: ApiResolver nutzen[cite: 1]\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var mallocPtr = findExport(\"malloc\");\n    var mmapPtr = findExport(\"mmap\");\n    \n    // Immortality Exports\n    var abortPtr = findExport(\"abort\");\n    var exitPtr = findExport(\"exit\");\n    var _exitPtr = findExport(\"_exit\");\n    var tgkillPtr = findExport(\"tgkill\");\n    var sleepPtr = findExport(\"sleep\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String(); // Pointer-Safeguarding[cite: 1]\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER (Verdr\u00e4ngt den Crash in den Background)\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. OOM-SLAYER (Gefixed mit 64-Bit Pointer Comparison)\n    // =========================================================================\n    var SIZE_500MB = ptr(\"0x1FFFFFFF\"); \n\n    if (mallocPtr) {\n        Interceptor.attach(mallocPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[0].compare(SIZE_500MB) > 0) { \n                        log(\"[\ud83d\udd25 OOM-SLAYER] malloc(\" + args[0] + \") entsch\u00e4rft!\");\n                        args[0] = ptr(1); \n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    if (mmapPtr) {\n        Interceptor.attach(mmapPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[1].compare(SIZE_500MB) > 0) { \n                        log(\"[\ud83d\udd25 OOM-SLAYER] mmap(\" + args[1] + \") entsch\u00e4rft!\");\n                        args[1] = ptr(4096); \n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. IMMORTALITY HOOKS (Verhindert App-Kill durch Uncaught Exceptions)\n    // =========================================================================\n    var sleepFunc = sleepPtr ? new NativeFunction(sleepPtr, 'int', ['int']) : null;\n\n    function makeImmortal(ptrAddr, name) {\n        if (!ptrAddr || !sleepFunc) return;\n        Interceptor.replace(ptrAddr, new NativeCallback(function() {\n            log(\"[\ud83e\uddca IMMORTAL] \" + name + \"() abgefangen! Friere RASP-Crash-Thread dauerhaft ein...\");\n            while(true) { sleepFunc(1000); }\n        }, 'void', []));\n    }\n\n    log(\"[4] Aktiviere Unsterblichkeits-Protokoll...\");\n    makeImmortal(abortPtr, \"abort\");\n    makeImmortal(exitPtr, \"exit\");\n    makeImmortal(_exitPtr, \"_exit\");\n\n    if (tgkillPtr && sleepFunc) {\n        Interceptor.replace(tgkillPtr, new NativeCallback(function(tgid, tid, sig) {\n            // Signal 6 = SIGABRT (Exception Crash), Signal 9 = SIGKILL\n            if (sig === 6 || sig === 9) { \n                log(\"[\ud83e\uddca IMMORTAL] tgkill(sig=\" + sig + \") blockiert! Friere Thread ein...\");\n                while(true) { sleepFunc(1000); }\n            }\n            return 0;\n        }, 'int', ['int', 'int', 'int']));\n    }\n\n    // =========================================================================\n    // 6. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {} // Diagnostic Try-Catch Rule[cite: 1]\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP ist isoliert, App l\u00e4uft weiter!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "73bd3848",
            "name": "0049",
            "code": "// @ts-nocheck\n\nfunction log(msg) {\n    // Framework Rule 1: Ausschlie\u00dflich synchrones send() Logging\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\n// Framework Rule 4: Globaler Diagnostic Try-Catch Block\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V14 - The ART Surgeon) gestartet...\");\n\n    // Framework Rule 6: ApiResolver umgeht Module-Shadowing in QuickJS\n    var resolver = new ApiResolver(\"module\"); \n\n    function findLibcExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findLibcExport(\"open\");\n    var openatPtr = findLibcExport(\"openat\");\n    var statPtr = findLibcExport(\"stat\");\n    var accessPtr = findLibcExport(\"access\");\n    var dlsymPtr = findLibcExport(\"dlsym\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    // Framework Rule 3: Pointer-Safeguarding\n                    this.pathStr = args[pathIdx].readUtf8String(); \n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER (Verhindert Main-Thread RASP Abst\u00fcrze)\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Anti-Reflection Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. ART SURGEON (Pariert JNI-Exceptions und OOM-Bomben in libart.so)\n    // =========================================================================\n    log(\"[4] Suche JNI-Vektoren in Android Runtime (libart.so)...\");\n\n    // F\u00e4ngt NegativeArraySizeException und OutOfMemoryError ab, bevor sie den Allocator erreichen\n    var matchesByteArray = resolver.enumerateMatches(\"exports:*libart.so!*JNI*NewByteArray*\");\n    matchesByteArray.forEach(function(match) {\n        log(\"    -> OOM-Slayer angebunden an: \" + match.name);\n        Interceptor.attach(match.address, {\n            onEnter: function(args) {\n                try {\n                    var len = args[1].toInt32();\n                    // Blockiert Arrays \u00fcber 50 MB oder negative Array-Gr\u00f6\u00dfen\n                    if (len > 50000000 || len < 0) { \n                        log(\"[\ud83d\udd25 ART SURGEON] OOM/NegativeArray Bombe entsch\u00e4rft! Geforderte Gr\u00f6\u00dfe: \" + len);\n                        args[1] = ptr(1); // Setzt die Gr\u00f6\u00dfe hart auf 1 Byte\n                    }\n                } catch(e) {}\n            }\n        });\n    });\n\n    // F\u00e4ngt unwillk\u00fcrliche RuntimeExceptions des RASP-Wachhunds ab\n    var matchesThrow = resolver.enumerateMatches(\"exports:*libart.so!*JNI*ThrowNew*\");\n    matchesThrow.forEach(function(match) {\n        log(\"    -> Exception-Slayer angebunden an: \" + match.name);\n        Interceptor.attach(match.address, {\n            onEnter: function(args) {\n                try {\n                    var msg = \"unknown\";\n                    if (!args[2].isNull()) msg = args[2].readUtf8String();\n                    \n                    // Filtert RASP-typische Exceptions, die nur aus Zahlen (-1709668551) bestehen\n                    if (msg && (msg.indexOf(\"-\") === 0 || !isNaN(msg))) {\n                        log(\"[\ud83d\udd25 ART SURGEON] Blockierte RASP Exception: \" + msg);\n                        this.shouldBlock = true;\n                    }\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                if (this.shouldBlock) {\n                    retval.replace(0); // JNI_OK - T\u00e4uscht dem RASP vor, der Wurf war erfolgreich\n                }\n            }\n        });\n    });\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP l\u00e4dt, aber seine Waffen sind zerst\u00f6rt!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (globalErr) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack);\n}"
        },
        {
            "id": "8c647127",
            "name": "0050",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V15 - The Safety Net) gestartet...\");\n\n    // =========================================================================\n    // 0. DAS JAVA AUFFANGNETZ (Der Matratzen-Trick)\n    // =========================================================================\n    // Frida reiht diesen Code automatisch ein und f\u00fchrt ihn aus, sobald die Java-VM bootet.\n    try {\n        Java.perform(function() {\n            var Thread = Java.use(\"java.lang.Thread\");\n            \n            var ExceptionHandler = Java.registerClass({\n                name: \"com.frida.ExceptionHandler\",\n                implements: [Java.use(\"java.lang.Thread$UncaughtExceptionHandler\")],\n                methods: {\n                    uncaughtException: function (thread, ex) {\n                        log(\"[\ud83e\uddca JAVA-NET] RASP Suizid erfolgreich neutralisiert!\");\n                        log(\" -> Thread stirbt lautlos: \" + thread.getName());\n                        log(\" -> Abgefangene Waffe: \" + ex.toString());\n                        // Wir schlucken den Fehler. Der Thread endet, die App lebt weiter!\n                    }\n                }\n            });\n            \n            Thread.setDefaultUncaughtExceptionHandler(ExceptionHandler.$new());\n            log(\"[+] Java Auffangnetz (UncaughtExceptionHandler) gespannt! Warte auf RASP-Angriff...\");\n        });\n    } catch(e) {\n        log(\"[-] Java.perform queued (wird aktiviert, sobald JVM hochf\u00e4hrt).\");\n    }\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere selektiven Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER (Triggert den isolierten RASP Suizid-Modus)\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP-Falle gestellt, bereit f\u00fcr Extraktion!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (globalErr) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack);\n}"
        },
        {
            "id": "15ad0280",
            "name": "0051",
            "code": "// @ts-nocheck\n\nfunction log(msg) {\n    // Die asynchrone Gadget-Architektur erfordert konsequentes Logging via send()[cite: 1]\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V16 - The Ninja) gestartet...\");\n\n    // Der native ApiResolver ist in der QuickJS-Umgebung robuster als Module.findExportByName[cite: 1]\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var dlsymPtr = findExport(\"dlsym\");\n\n    // =========================================================================\n    // 2. DLSYM ZOMBIE INJECTION (Der lautlose RASP-Amputateur)\n    // =========================================================================\n    var hookedFunctions = {};\n    \n    // Eine v\u00f6llig leere C-Funktion, die dem System einen erfolgreichen Ablauf (0) meldet\n    var fakeJniZombie = new NativeCallback(function() { \n        log(\"[\ud83e\uddca NINJA] RASP-Strafprozess im Hintergrund lautlos vaporisiert!\");\n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[2] Aktiviere dlsym Ninja-Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if (sym) this.symStr = sym; // Pointer sicher in den this-Kontext \u00fcbertragen[cite: 1]\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr && !retval.isNull()) {\n                        // Wir fangen ausschlie\u00dflich die DexGuard-spezifischen Straffunktionen ab\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            var addrStr = retval.toString();\n                            \n                            // Verhindert mehrfaches \u00dcberschreiben derselben Speicheradresse\n                            if (!hookedFunctions[addrStr]) {\n                                hookedFunctions[addrStr] = true;\n                                log(\"[\ud83d\udd25 DLSYM] Zombifiziere RASP-Methode: \" + this.symStr + \" @ \" + addrStr);\n                                \n                                // Der Trick: Wir lassen dlsym den korrekten Pointer zur\u00fcckgeben, \n                                // aber \u00fcberschreiben die Funktion im Speicher durch den Zombie!\n                                Interceptor.replace(retval, fakeJniZombie);\n                            }\n                        }\n                    }\n                } catch (e) {} \n            }\n        });\n    }\n\n    // =========================================================================\n    // 3. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[3] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. App l\u00e4uft frei von globalen Hooks!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (globalErr) {\n    // Verhindert Silent-Crashes im Gadget durch globales Exception-Tracing[cite: 1]\n    log(\"[!] FATAL SCRIPT ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack);\n}"
        },
        {
            "id": "7e58c7b2",
            "name": "0052",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V10.1 Reforged) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var mallocPtr = findExport(\"malloc\");\n    var mmapPtr = findExport(\"mmap\");\n    \n    // Immortality Exports\n    var killPtr = findExport(\"kill\");\n    var abortPtr = findExport(\"abort\");\n    var sleepPtr = findExport(\"sleep\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                        // log(\"[\ud83d\udd25 I/O SPOOFER] \" + funcName + \"() umgeleitet auf clean_base.apk\");\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                        // log(\"[\ud83d\udd25 I/O SPOOFER] \" + funcName + \"() umgeleitet auf clean_maps.txt\");\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER (Verhindert Main-Thread RASP Abst\u00fcrze)\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            log(\"[\ud83d\udd25 DLSYM] Zombie-Antwort f\u00fcr RASP: \" + this.symStr);\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. OOM-SLAYER (Gefixt: Vorzeichenloser Pointer-Vergleich!)\n    // =========================================================================\n    var ONE_GIGABYTE_PTR = ptr(\"0x3FFFFFFF\"); \n\n    if (mallocPtr) {\n        Interceptor.attach(mallocPtr, {\n            onEnter: function(args) {\n                try {\n                    // Verhindert den Bug mit negativen 32-Bit Integern\n                    if (args[0].compare(ONE_GIGABYTE_PTR) > 0) { \n                        log(\"[\ud83d\udd25 OOM-SLAYER] malloc() Bombe entsch\u00e4rft! Geforderte Gr\u00f6\u00dfe: \" + args[0]);\n                        args[0] = ptr(1); \n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    if (mmapPtr) {\n        Interceptor.attach(mmapPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[1].compare(ONE_GIGABYTE_PTR) > 0) { \n                        log(\"[\ud83d\udd25 OOM-SLAYER] mmap() Bombe entsch\u00e4rft! Geforderte Gr\u00f6\u00dfe: \" + args[1]);\n                        args[1] = ptr(4096); \n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. IMMORTALITY HOOKS (Schild gegen AndroidRuntime Kills)\n    // =========================================================================\n    var sleepFunc = sleepPtr ? new NativeFunction(sleepPtr, 'int', ['int']) : null;\n\n    if (killPtr) {\n        var origKill = new NativeFunction(killPtr, 'int', ['int', 'int']);\n        Interceptor.replace(killPtr, new NativeCallback(function(pid, sig) {\n            if (sig === 9) { // SIGKILL (9) ist der Todessto\u00df der AndroidRuntime\n                log(\"[\ud83e\uddca IMMORTAL] Android versuchte die App zu beenden (kill SIGKILL). Blockiert!\");\n                return 0; // Erfolg vorgaukeln\n            }\n            return origKill(pid, sig);\n        }, 'int', ['int', 'int']));\n    }\n\n    if (abortPtr && sleepFunc) {\n        Interceptor.replace(abortPtr, new NativeCallback(function() {\n            log(\"[\ud83e\uddca IMMORTAL] abort() abgefangen! RASP-Crash-Thread in Teergrube verschoben.\");\n            while(true) { sleepFunc(1000); } // Thread schl\u00e4ft unendlich lange, Main-Thread lebt weiter\n        }, 'void', []));\n    }\n\n    // =========================================================================\n    // 6. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[6] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP l\u00e4dt, aber die App ist unsterblich!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "0d07d120",
            "name": "0053_Success_App_neu_in_fokus_dann_nochmal_feuern",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} // Framework Rule 1: Logging ausschlie\u00dflich via send()[cite: 1]\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry { // Framework Rule 4: Globaler Diagnostic Try-Catch[cite: 1]\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V17 - The Sniper) gestartet...\");\n\n    // Framework Rule 6: ApiResolver ist QuickJS-stabil[cite: 1]\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var mallocPtr = findExport(\"malloc\");\n    var mmapPtr = findExport(\"mmap\");\n    var abortPtr = findExport(\"abort\");\n    var sleepPtr = findExport(\"sleep\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps Illusion)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String(); // Pointer Safeguarding[cite: 1]\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER (Blockt den Main-Thread Suizid)\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            log(\"[\ud83d\udd25 DLSYM] Zombie-Antwort f\u00fcr RASP: \" + this.symStr);\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. PR\u00c4ZISIONS-OOM-SLAYER (Mit Caller-Filter)\n    // =========================================================================\n    var ONE_GIGABYTE_PTR = ptr(\"0x3FFFFFFF\"); \n\n    if (mallocPtr) {\n        Interceptor.attach(mallocPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[0].compare(ONE_GIGABYTE_PTR) > 0) { \n                        var caller = Process.findModuleByAddress(this.returnAddress);\n                        if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                            log(\"[\ud83d\udd25 OOM-SLAYER] RASP malloc-Bombe entsch\u00e4rft! \" + args[0]);\n                            args[0] = ptr(1); \n                        }\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    if (mmapPtr) {\n        Interceptor.attach(mmapPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[1].compare(ONE_GIGABYTE_PTR) > 0) { \n                        var caller = Process.findModuleByAddress(this.returnAddress);\n                        if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                            log(\"[\ud83d\udd25 OOM-SLAYER] RASP mmap-Bombe entsch\u00e4rft! \" + args[1]);\n                            args[1] = ptr(4096); \n                        } else {\n                            log(\"[\u2139\ufe0f SYSTEM] Erlaube legitimes WebView mmap (V8 Cage): \" + args[1]);\n                        }\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. IMMORTALITY HOOK (F\u00e4ngt Exception-Kills ab)\n    // =========================================================================\n    var sleepFunc = sleepPtr ? new NativeFunction(sleepPtr, 'int', ['int']) : null;\n\n    if (abortPtr && sleepFunc) {\n        Interceptor.replace(abortPtr, new NativeCallback(function() {\n            log(\"[\ud83e\uddca IMMORTAL] abort() abgefangen! Friere gecrashten RASP-Thread ein...\");\n            while(true) { sleepFunc(1000); } \n        }, 'void', []));\n    }\n\n    // =========================================================================\n    // 6. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[6] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP l\u00e4dt isoliert, App ist vollst\u00e4ndig gesch\u00fctzt!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (globalErr) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack); //[cite: 1]\n}"
        },
        {
            "id": "47ad6aa1",
            "name": "0054",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V18 - The Immortal V10) gestartet...\");\n\n    // ApiResolver f\u00fcr maximale Stabilit\u00e4t in QuickJS\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    // 1. Spoofer Exports\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n    \n    // 2. Immortality Exports\n    var abortPtr = findExport(\"abort\");\n    var exitPtr = findExport(\"exit\");\n    var _exitPtr = findExport(\"_exit\");\n    var killPtr = findExport(\"kill\");\n    var tgkillPtr = findExport(\"tgkill\");\n    var sleepPtr = findExport(\"sleep\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps Illusion aus V10)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    // Pr\u00e4ziser Spoofer nur f\u00fcr die LibreLinkUp APK (verhindert WebView Crash)\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere selektiven Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER (Die Magie aus V10)\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        // T\u00e4uscht DexGuard erfolgreich vor, dass seine Methoden nicht laden\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. IMMORTALITY PROTOKOLL (Schluckt die RASP-RuntimeException)\n    // =========================================================================\n    var sleepFunc = sleepPtr ? new NativeFunction(sleepPtr, 'int', ['int']) : null;\n\n    function makeImmortal(ptrAddr, name) {\n        if (!ptrAddr || !sleepFunc) return;\n        Interceptor.replace(ptrAddr, new NativeCallback(function() {\n            log(\"[\ud83e\uddca IMMORTAL] \" + name + \"() abgefangen! RASP-Crash-Thread in Teergrube verschoben.\");\n            // Der RASP-Hintergrundthread stirbt nicht, er schl\u00e4ft f\u00fcr immer. Main-Thread l\u00e4uft weiter!\n            while(true) { sleepFunc(1000); }\n        }, 'void', []));\n    }\n\n    log(\"[4] Aktiviere Unsterblichkeit gegen AndroidRuntime...\");\n    makeImmortal(abortPtr, \"abort\");\n    makeImmortal(exitPtr, \"exit\");\n    makeImmortal(_exitPtr, \"_exit\");\n\n    if (killPtr) {\n        var origKill = new NativeFunction(killPtr, 'int', ['int', 'int']);\n        Interceptor.replace(killPtr, new NativeCallback(function(pid, sig) {\n            if (sig === 9 || sig === 6) { // SIGKILL oder SIGABRT\n                log(\"[\ud83e\uddca IMMORTAL] System kill(\" + sig + \") blockiert!\");\n                return 0;\n            }\n            return origKill(pid, sig);\n        }, 'int', ['int', 'int']));\n    }\n\n    if (tgkillPtr && sleepFunc) {\n        var origTgkill = new NativeFunction(tgkillPtr, 'int', ['int', 'int', 'int']);\n        Interceptor.replace(tgkillPtr, new NativeCallback(function(tgid, tid, sig) {\n            if (sig === 9 || sig === 6) { \n                log(\"[\ud83e\uddca IMMORTAL] tgkill(\" + sig + \") blockiert! Friere RASP Thread ein...\");\n                while(true) { sleepFunc(1000); }\n            }\n            return origTgkill(tgid, tid, sig);\n        }, 'int', ['int', 'int', 'int']));\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP-Schattenboxer aktiv, App bleibt am Leben!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "71bd581b",
            "name": "0055",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V19 - The Phantom) gestartet...\");\n\n    // =========================================================================\n    // 0. DAS JAVA PHANTOM-NETZ (Der Schalld\u00e4mpfer f\u00fcr den RASP-Suizid)\n    // =========================================================================\n    try {\n        Java.perform(function() {\n            var ThreadGroup = Java.use(\"java.lang.ThreadGroup\");\n            \n            // F\u00e4ngt alle ungefangenen Exceptions ab, BEVOR Android die App killt\n            ThreadGroup.uncaughtException.overload('java.lang.Thread', 'java.lang.Throwable').implementation = function(thread, throwable) {\n                var errStr = throwable.toString();\n                \n                // Filtere die DexGuard-typischen Zahlen-Exceptions und Array-Bomben\n                if (errStr.indexOf(\"RuntimeException\") !== -1 || errStr.indexOf(\"NegativeArraySize\") !== -1) {\n                    log(\"[\ud83e\uddca PHANTOM] RASP Suizid lautlos geschluckt auf Thread: \" + thread.getName());\n                    log(\" -> Abgefangene Waffe: \" + errStr);\n                    // Wir kehren zur\u00fcck OHNE die Exception an Android weiterzugeben.\n                    // Der b\u00f6sartige Thread endet hier in Frieden, die App lebt weiter.\n                    return; \n                }\n                \n                // Normale App-Crashes lassen wir passieren\n                this.uncaughtException(thread, throwable);\n            };\n            log(\"[+] Java Phantom-Netz erfolgreich gespannt!\");\n        });\n    } catch(e) {\n        log(\"[-] Java.perform queued (wartet auf den JVM-Boot).\");\n    }\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n    spoofFilesystem(accessPtr, 0);\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. App ist bereit f\u00fcr die freie Netzwerkanalyse!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "d6480afb",
            "name": "0056",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V20 - The Perfect Lobotomy) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    \n    var dlopenPtr = findExport(\"dlopen\");\n    var androidDlopenExtPtr = findExport(\"android_dlopen_ext\");\n    var dlsymPtr = findExport(\"dlsym\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps Illusion)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx, funcName) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    // Spoofe nur die LibreLinkUp APK und proc/maps. \n                    // ACHTUNG: libdb0c.so wird ABSICHTLICH NICHT gespooft!\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere I/O Spoofer (L\u00e4sst libdb0c.so Check passieren)...\");\n    spoofFilesystem(openPtr, 0, \"open\");\n    spoofFilesystem(openatPtr, 1, \"openat\");\n    spoofFilesystem(statPtr, 0, \"stat\");\n    spoofFilesystem(accessPtr, 0, \"access\");\n\n    // =========================================================================\n    // 3. DIE ABSOLUTE LOBOTOMIE (dlopen Interceptor)\n    // =========================================================================\n    var validDummyHandle = new NativeFunction(dlopenPtr, 'pointer', ['pointer', 'int'])(Memory.allocUtf8String(\"liblog.so\"), 1);\n\n    function lobotomizeRasp(ptr) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.libPathStr = args[0].readUtf8String();\n                    if (!this.libPathStr) return;\n\n                    // Wenn Android versucht den RASP wirklich in den RAM zu laden...\n                    if (this.libPathStr.indexOf(\"libdb0c.so\") !== -1 || this.libPathStr.indexOf(\"dexguard\") !== -1) {\n                        log(\"[\ud83d\udd25 LOBOTOMIE] RASP wird durch harmlose liblog.so ersetzt: \" + this.libPathStr);\n                        this.shouldBlock = true;\n                    }\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                if (this.shouldBlock) {\n                    retval.replace(validDummyHandle); // ...schieben wir ihm den Dummy unter!\n                }\n            }\n        });\n    }\n\n    log(\"[3] Amputiere DexGuard am Kernel-Eingang...\");\n    lobotomizeRasp(dlopenPtr);\n    lobotomizeRasp(androidDlopenExtPtr);\n\n    // =========================================================================\n    // 4. JNI ZOMBIE SPOOFER (Befriedigt die Java-VM)\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; // T\u00e4uscht der Java-VM eine erfolgreiche RASP-Funktion vor\n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[4] Aktiviere JNI Matrix (T\u00e4usche Native-Funktionen vor)...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        // Alle RASP Java-Aufrufe landen im Nichts\n                        if (this.symStr.indexOf(\"Java_o_\") === 0 || this.symStr.indexOf(\"stackFrames\") !== -1) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks f\u00fcr BoringSSL...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP ist physisch nicht im RAM. App l\u00e4dt sauber durch!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "c1528d75",
            "name": "0057",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V22 - The Pinnacle) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var dlsymPtr = findExport(\"dlsym\");\n    \n    var exitPtr = findExport(\"exit\");\n    var _exitPtr = findExport(\"_exit\");\n    var abortPtr = findExport(\"abort\");\n    var pthread_exit_ptr = findExport(\"pthread_exit\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. DYNAMIC JAVA INJECTION (Umgeht den \"Java is not defined\" Bug)\n    // =========================================================================\n    var javaHooked = false;\n    function tryHookJava() {\n        if (javaHooked) return;\n        try {\n            if (typeof Java !== 'undefined' && Java.available) {\n                javaHooked = true;\n                Java.perform(function() {\n                    try {\n                        var System = Java.use(\"java.lang.System\");\n                        System.exit.overload('int').implementation = function(status) {\n                            log(\"[\ud83d\udd25 JAVA-NET] System.exit(\" + status + \") vom RASP blockiert!\");\n                        };\n                        \n                        var Runtime = Java.use(\"java.lang.Runtime\");\n                        Runtime.exit.overload('int').implementation = function(status) {\n                            log(\"[\ud83d\udd25 JAVA-NET] Runtime.exit(\" + status + \") blockiert!\");\n                        };\n\n                        var Thread = Java.use(\"java.lang.Thread\");\n                        var ExceptionHandler = Java.registerClass({\n                            name: \"com.frida.ExceptionHandler\",\n                            implements: [Java.use(\"java.lang.Thread$UncaughtExceptionHandler\")],\n                            methods: {\n                                uncaughtException: function (thread, ex) {\n                                    log(\"[\ud83e\uddca JAVA-NET] RASP Suizid-Waffe auf Thread '\" + thread.getName() + \"' geschluckt!\");\n                                    log(\" -> Typ: \" + ex.toString());\n                                    // Wir lassen die Exception verschwinden. Der RASP-Thread stirbt, die App lebt.\n                                }\n                            }\n                        });\n                        Thread.setDefaultUncaughtExceptionHandler(ExceptionHandler.$new());\n                        log(\"[+] Java-Schutzschilde (Exit & OOM-Matratze) erfolgreich hochgefahren!\");\n                    } catch(e) {\n                        log(\"[-] Java-Hook Fehler: \" + e.message);\n                    }\n                });\n            }\n        } catch(e) {}\n    }\n\n    // =========================================================================\n    // 3. KERNEL I/O SPOOFER (APK & Maps Illusion)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                // Dynamischer JVM Trigger (L\u00f6st Java.perform aus, sobald die VM hochgefahren ist)\n                tryHookJava(); \n\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer & Dynamic Java Trigger...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n\n    // =========================================================================\n    // 4. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. C-LEVEL THREAD ASSASSIN (Ersetzt die alte Sleep-Teergrube)\n    // =========================================================================\n    var pthread_exit = pthread_exit_ptr ? new NativeFunction(pthread_exit_ptr, 'void', ['pointer']) : null;\n    var replacedFuncs = {}; // Verhindert den \"already replaced this function\" Crash\n\n    function blockFatalC(ptrAddr, name) {\n        if (!ptrAddr) return;\n        var addrStr = ptrAddr.toString();\n        if (replacedFuncs[addrStr]) return; // Schon gehookt? (z.B. wenn exit und _exit identisch sind)\n        \n        replacedFuncs[addrStr] = true;\n        Interceptor.replace(ptrAddr, new NativeCallback(function() {\n            log(\"[\ud83e\uddca IMMORTAL] \" + name + \"() auf C-Ebene abgefangen! Kille nur den Angreifer-Thread...\");\n            if (pthread_exit) {\n                pthread_exit(ptr(0)); // Beendet NUR den aktuellen Hintergrund-Thread sauber\n            }\n        }, 'void', ['int'])); \n    }\n\n    blockFatalC(exitPtr, \"exit\");\n    blockFatalC(_exitPtr, \"_exit\");\n    blockFatalC(abortPtr, \"abort\");\n\n    // =========================================================================\n    // 6. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. Warte auf App-Start...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "bb40f17e",
            "name": "0058",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V21 - Resurrection) gestartet...\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(libName, funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*\" + libName + \"!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    // Libc Exports\n    var openPtr = findExport(\"libc.so\", \"open\");\n    var openatPtr = findExport(\"libc.so\", \"openat\");\n    var statPtr = findExport(\"libc.so\", \"stat\");\n    var accessPtr = findExport(\"libc.so\", \"access\");\n    var dlsymPtr = findExport(\"libc.so\", \"dlsym\");\n    \n    // Libc++ Exports (Der Exception-Flaschenhals)\n    var cxaThrowPtr = findExport(\"libc++.so\", \"__cxa_throw\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (APK & Maps Illusion aus V10)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    // Pr\u00e4ziser Spoofer nur f\u00fcr die LibreLinkUp APK\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere selektiven Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n    spoofFilesystem(accessPtr, 0);\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER (Blockt den Main-Thread Suizid)\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr) {\n                        // T\u00e4uscht DexGuard erfolgreich vor, dass seine Methoden nicht laden\n                        if (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. EXCEPTION ANNIHILATOR (L\u00f6scht Exceptions bevor sie existieren)\n    // =========================================================================\n    if (cxaThrowPtr) {\n        log(\"[4] Aktiviere __cxa_throw Exception Annihilator...\");\n        Interceptor.attach(cxaThrowPtr, {\n            onEnter: function(args) {\n                try {\n                    var caller = Process.findModuleByAddress(this.returnAddress);\n                    // Wenn die Exception aus der RASP-Lib kommt...\n                    if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                        log(\"[\ud83e\uddca RESURRECTION] RASP versuchte __cxa_throw auszuf\u00fchren. L\u00f6sche Exception!\");\n                        // Anstatt die App zu crashen, pausieren wir den werfenden Thread unendlich.\n                        // Das blockiert die Exception-Weitergabe an Android.\n                        while (true) { Thread.sleep(1); } \n                    }\n                } catch(e) {}\n            }\n        });\n    } else {\n        log(\"[-] Konnte __cxa_throw nicht finden. Fallback-Plan inaktiv.\");\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. RASP-Schattenboxer aktiv, App bleibt am Leben!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message + \"\\n\" + err.stack);\n}"
        },
        {
            "id": "99e92558",
            "name": "0059",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} // Framework Rule 1: Synchrones EventBus Logging[cite: 1]\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry { // Framework Rule 4: Globaler Diagnostic Try-Catch[cite: 1]\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V28 - The True Sentinel)\");\n\n    // Framework Rule 6: Niemals Module.xyz nutzen! Immer ApiResolver[cite: 1]\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var accessPtr = findExport(\"access\");\n    var keyFilePath = \"/data/data/org.nativescript.LibreLinkUp/ghost_aes_key.txt\";\n    var keyFilePathMem = Memory.allocUtf8String(keyFilePath);\n    \n    // =========================================================================\n    // STATE MACHINE LOGIK (Pr\u00fcft ob wir in Phase 1 oder 2 sind)\n    // =========================================================================\n    var isPhase2 = false;\n    if (accessPtr) {\n        var accessFunc = new NativeFunction(accessPtr, 'int', ['pointer', 'int']);\n        // access() gibt 0 zur\u00fcck, wenn die Datei existiert (F_OK)\n        if (accessFunc(keyFilePathMem, 0) === 0) {\n            isPhase2 = true;\n        }\n    }\n\n    // Hilfsfunktion gegen doppelte Hooks bei Aliases (wie exit / _exit)\n    var hookedFuncs = {};\n    function safeHook(ptrAddr, callbacks) {\n        if (!ptrAddr) return;\n        var key = ptrAddr.toString();\n        if (hookedFuncs[key]) return;\n        hookedFuncs[key] = true;\n        Interceptor.attach(ptrAddr, callbacks);\n    }\n\n    // =========================================================================\n    // PHASE 2: PASSIV-MODUS (Key ist da, Frida ist komplett aus dem Hook)\n    // =========================================================================\n    if (isPhase2) {\n        log(\"[2] \u2705 AES Key Datei gefunden! (ghost_aes_key.txt existiert)\");\n        log(\"[3] Starte PHASE 2: FRIDA STEALTH-MODUS (0 Hooks).\");\n        log(\"[+] DexGuard Memory-Scan wird eine 100% saubere libc vorfinden.\");\n        log(\"[+] Deine Smali-Patches (GHOST PROTOCOL v21) \u00fcbernehmen jetzt!\");\n        \n        // Das Skript endet hier. Keine Hooks = Keine DexGuard Panik = App l\u00e4uft stabil.\n    } \n    // =========================================================================\n    // PHASE 1: AGGRESSIV-MODUS (Erster Start, Key extrahieren)\n    // =========================================================================\n    else {\n        log(\"[2] \u274c AES Key Datei fehlt. Starte PHASE 1 (EXTRAKTION).\");\n\n        var openPtr = findExport(\"open\");\n        var openatPtr = findExport(\"openat\");\n        var statPtr = findExport(\"stat\");\n        var dlsymPtr = findExport(\"dlsym\");\n\n        var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n        var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n        // --- I/O Spoofer ---\n        function spoofFilesystem(ptr, pathIdx) {\n            safeHook(ptr, {\n                onEnter: function (args) {\n                    try {\n                        this.pathStr = args[pathIdx].readUtf8String(); // Rule 3: Pointer Safeguarding[cite: 1]\n                        if (!this.pathStr) return;\n                        if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                            args[pathIdx] = cleanApkMem;\n                        } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                            args[pathIdx] = cleanMapsMem;\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n\n        log(\"[3] Aktiviere Kernel I/O Spoofer...\");\n        spoofFilesystem(openPtr, 0);\n        spoofFilesystem(openatPtr, 1);\n        spoofFilesystem(statPtr, 0);\n\n        // --- JNI Zombie Spoofer ---\n        var fakeJniZombie = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n        if (dlsymPtr) {\n            log(\"[4] Aktiviere JNI Zombie Spoofer...\");\n            safeHook(dlsymPtr, {\n                onEnter: function(args) {\n                    try { \n                        var sym = args[1].readUtf8String(); \n                        if(sym) this.symStr = sym;\n                    } catch(e) {}\n                },\n                onLeave: function(retval) {\n                    try {\n                        if (this.symStr && (this.symStr.indexOf(\"Java_o_\") === 0 || this.symStr.indexOf(\"stackFrames\") !== -1)) {\n                            retval.replace(fakeJniZombie);\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n\n        // --- C-Level FileWriter ---\n        function saveKeyToFile(hexKey) {\n            try {\n                var fopenPtr = findExport(\"fopen\");\n                var fputsPtr = findExport(\"fputs\");\n                var fclosePtr = findExport(\"fclose\");\n\n                if (fopenPtr && fputsPtr && fclosePtr) {\n                    var fopen = new NativeFunction(fopenPtr, 'pointer', ['pointer', 'pointer']);\n                    var fputs = new NativeFunction(fputsPtr, 'int', ['pointer', 'pointer']);\n                    var fclose = new NativeFunction(fclosePtr, 'int', ['pointer']);\n                    \n                    var modeMem = Memory.allocUtf8String(\"w\");\n                    var file = fopen(keyFilePathMem, modeMem);\n                    if (!file.isNull()) {\n                        var keyStringMem = Memory.allocUtf8String(hexKey + \"\\n\");\n                        fputs(keyStringMem, file);\n                        fclose(file);\n                        log(\"[\ud83d\udcbe BARE-METAL] AES-Key physisch auf Ger\u00e4t gesichert!\");\n                    }\n                }\n            } catch (e) {\n                log(\"[-] Fehler beim File-Write.\");\n            }\n        }\n\n        // --- BoringSSL Crypto Hook ---\n        var cryptoTarget = null;\n        var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n        else {\n            evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n            if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n        }\n\n        if (cryptoTarget) {\n            log(\"[5] Setze Crypto-Hooks f\u00fcr AES-Extraktion...\");\n            safeHook(cryptoTarget, {\n                onEnter: function (args) {\n                    try {\n                        if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                    } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    try {\n                        if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                            var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                            var hexKey = ab2hex(keyBuffer);\n                            \n                            if (hexKey !== \"00000000000000000000000000000000\") {\n                                log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                                log(\"Key (Hex): \" + hexKey);\n                                log(\"=============================================\\n\");\n                                \n                                // Speichere den Key, damit beim n\u00e4chsten Start Phase 2 triggert!\n                                saveKeyToFile(hexKey);\n                            }\n                        }\n                    } catch (e) {}\n                }\n            });\n            log(\"[+] Phase 1 Setup bereit. Warte auf Key-Generierung...\");\n        } else {\n            log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n        }\n    }\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message);\n}"
        },
        {
            "id": "d0951f80",
            "name": "0060",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} // Framework Rule 1: Logging via send()[cite: 1]\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry { // Framework Rule 4: Global Try-Catch[cite: 1]\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V30 - The Immortal Smali-Enabler)\");\n\n    var resolver = new ApiResolver(\"module\"); // Framework Rule 6[cite: 1]\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var mallocPtr = findExport(\"malloc\");\n    var mmapPtr = findExport(\"mmap\");\n    var abortPtr = findExport(\"abort\");\n    var sleepPtr = findExport(\"sleep\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    var hookedFuncs = {};\n    function safeHook(ptrAddr, callbacks) {\n        if (!ptrAddr) return;\n        var key = ptrAddr.toString();\n        if (hookedFuncs[key]) return;\n        hookedFuncs[key] = true;\n        Interceptor.attach(ptrAddr, callbacks);\n    }\n\n    // =========================================================================\n    // 2. KERNEL I/O SPOOFER (Tarnt deine Smali-Patches dauerhaft)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx) {\n        safeHook(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String(); // Rule 3: Pointer Safeguarding[cite: 1]\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer (Smali-Tarnkappe)...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n\n    // =========================================================================\n    // 3. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        safeHook(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr && (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1)) {\n                        retval.replace(fakeJniZombie);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. PRECISION OOM-SLAYER (Gefiltert auf DexGuard, sch\u00fctzt WebView)\n    // =========================================================================\n    var ONE_GIGABYTE_PTR = ptr(\"0x3FFFFFFF\"); \n\n    if (mallocPtr) {\n        safeHook(mallocPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[0].compare(ONE_GIGABYTE_PTR) > 0) { \n                        var caller = Process.findModuleByAddress(this.returnAddress);\n                        if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                            log(\"[\ud83d\udd25 OOM-SLAYER] DexGuard malloc-Bombe entsch\u00e4rft! \" + args[0]);\n                            args[0] = ptr(1); \n                        }\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    if (mmapPtr) {\n        safeHook(mmapPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[1].compare(ONE_GIGABYTE_PTR) > 0) { \n                        var caller = Process.findModuleByAddress(this.returnAddress);\n                        if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                            log(\"[\ud83d\udd25 OOM-SLAYER] DexGuard mmap-Bombe entsch\u00e4rft! \" + args[1]);\n                            args[1] = ptr(4096); \n                        }\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. DIE TEERGRUBE (Friert DexGuard Crash-Thread lautlos ein)\n    // =========================================================================\n    var sleepFunc = sleepPtr ? new NativeFunction(sleepPtr, 'int', ['int']) : null;\n\n    if (abortPtr && sleepFunc) {\n        log(\"[4] Aktiviere Teergrube f\u00fcr RASP-Suizid-Thread...\");\n        Interceptor.replace(abortPtr, new NativeCallback(function() {\n            log(\"[\ud83e\uddca TEERGRUBE] abort() abgefangen! Friere DexGuard f\u00fcr immer ein.\");\n            while(true) { sleepFunc(1000); } \n        }, 'void', []));\n    }\n\n    // =========================================================================\n    // 6. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks f\u00fcr SSL-Extraktion...\");\n        safeHook(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. App ist gesch\u00fctzt und Smali-Patches k\u00f6nnen laden!\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message); // Diagnostic Rule[cite: 1]\n}"
        },
        {
            "id": "d012a140",
            "name": "0061",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} // Framework Rule 1\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry { // Framework Rule 4\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V31 - Pure Passive Extractor)\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var accessPtr = findExport(\"access\");\n    var keyFilePath = \"/data/data/org.nativescript.LibreLinkUp/ghost_aes_key.txt\";\n    var keyFilePathMem = Memory.allocUtf8String(keyFilePath);\n\n    // =========================================================================\n    // STATE MACHINE (Pr\u00fcft Lauf 1 vs Lauf 2)\n    // =========================================================================\n    var isPhase2 = false;\n    if (accessPtr) {\n        var accessFunc = new NativeFunction(accessPtr, 'int', ['pointer', 'int']);\n        // access() gibt 0 zur\u00fcck, wenn die Datei existiert\n        if (accessFunc(keyFilePathMem, 0) === 0) {\n            isPhase2 = true;\n        }\n    }\n\n    // =========================================================================\n    // PHASE 2: PASSIV-MODUS (0 Hooks)\n    // =========================================================================\n    if (isPhase2) {\n        log(\"[2] \u2705 AES Key Datei gefunden! (ghost_aes_key.txt)\");\n        log(\"[3] Starte PHASE 2: FRIDA IST 100% PASSIV.\");\n        log(\"[+] Keine C-Hooks, keine Blocker. Smali-Patches \u00fcbernehmen!\");\n        // Skript terminiert hier. Die App bootet v\u00f6llig frei von Interceptors.\n    } \n    // =========================================================================\n    // PHASE 1: EXTRAKTION (Nur BoringSSL)\n    // =========================================================================\n    else {\n        log(\"[2] \u274c AES Key Datei fehlt. Starte PHASE 1 (EXTRAKTION).\");\n\n        function saveKeyToFile(hexKey) {\n            try {\n                var fopenPtr = findExport(\"fopen\");\n                var fputsPtr = findExport(\"fputs\");\n                var fclosePtr = findExport(\"fclose\");\n\n                if (fopenPtr && fputsPtr && fclosePtr) {\n                    var fopen = new NativeFunction(fopenPtr, 'pointer', ['pointer', 'pointer']);\n                    var fputs = new NativeFunction(fputsPtr, 'int', ['pointer', 'pointer']);\n                    var fclose = new NativeFunction(fclosePtr, 'int', ['pointer']);\n                    \n                    var modeMem = Memory.allocUtf8String(\"w\");\n                    var file = fopen(keyFilePathMem, modeMem);\n                    if (!file.isNull()) {\n                        var keyStringMem = Memory.allocUtf8String(hexKey + \"\\n\");\n                        fputs(keyStringMem, file);\n                        fclose(file);\n                        log(\"[\ud83d\udcbe BARE-METAL] AES-Key physisch auf Ger\u00e4t gesichert!\");\n                    }\n                }\n            } catch (e) {\n                log(\"[-] Fehler beim File-Write: \" + e.message);\n            }\n        }\n\n        var cryptoTarget = null;\n        var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n        else {\n            evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n            if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n        }\n\n        if (cryptoTarget) {\n            log(\"[3] Setze Crypto-Hook f\u00fcr AES-Extraktion...\");\n            Interceptor.attach(cryptoTarget, {\n                onEnter: function (args) {\n                    try {\n                        if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                    } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    try {\n                        if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                            var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                            var hexKey = ab2hex(keyBuffer);\n                            \n                            if (hexKey !== \"00000000000000000000000000000000\") {\n                                log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                                log(\"Key (Hex): \" + hexKey);\n                                log(\"=============================================\\n\");\n                                \n                                // Speichert den Key f\u00fcr den passiven Lauf 2\n                                saveKeyToFile(hexKey);\n                            }\n                        }\n                    } catch (e) {}\n                }\n            });\n            log(\"[+] Phase 1 Setup bereit. Lese Key aus...\");\n        } else {\n            log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n        }\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message);\n}"
        },
        {
            "id": "0141c10d",
            "name": "0062",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} // Framework Rule 1: Logging via send()\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V32 - The Silent Guardian)\");\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var accessPtr = findExport(\"access\");\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    \n    var keyFilePath = \"/data/data/org.nativescript.LibreLinkUp/ghost_aes_key.txt\";\n    var keyFilePathMem = Memory.allocUtf8String(keyFilePath);\n\n    // =========================================================================\n    // STATE MACHINE LOGIK\n    // =========================================================================\n    var isPhase2 = false;\n    if (accessPtr) {\n        var accessFunc = new NativeFunction(accessPtr, 'int', ['pointer', 'int']);\n        // 0 = Datei existiert bereits\n        if (accessFunc(keyFilePathMem, 0) === 0) {\n            isPhase2 = true;\n        }\n    }\n\n    // =========================================================================\n    // KERNEL I/O SPOOFER (IMMER AKTIV - Tarnt deine Smali-Patches!)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String(); // Pointer Safeguarding\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer (Tarnkappe f\u00fcr Smali-Modifikationen)...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n    spoofFilesystem(accessPtr, 0);\n\n    // =========================================================================\n    // PHASE 2: PASSIV-MODUS\n    // =========================================================================\n    if (isPhase2) {\n        log(\"[3] \u2705 AES Key Datei gefunden! (ghost_aes_key.txt)\");\n        log(\"[+] Frida l\u00e4uft als Silent Guardian. Nur I/O-Spoofing ist aktiv.\");\n        log(\"[+] Keine C-Blocker. App bootet fl\u00fcssig in deine Smali-Patches!\");\n        // Skript endet hier. App startet ungehindert durch.\n    } \n    // =========================================================================\n    // PHASE 1: EXTRAKTION (Nur beim ersten Start)\n    // =========================================================================\n    else {\n        log(\"[3] \u274c AES Key Datei fehlt. Starte EXTRAKTION.\");\n\n        function saveKeyToFile(hexKey) {\n            try {\n                var fopenPtr = findExport(\"fopen\");\n                var fputsPtr = findExport(\"fputs\");\n                var fclosePtr = findExport(\"fclose\");\n\n                if (fopenPtr && fputsPtr && fclosePtr) {\n                    var fopen = new NativeFunction(fopenPtr, 'pointer', ['pointer', 'pointer']);\n                    var fputs = new NativeFunction(fputsPtr, 'int', ['pointer', 'pointer']);\n                    var fclose = new NativeFunction(fclosePtr, 'int', ['pointer']);\n                    \n                    var modeMem = Memory.allocUtf8String(\"w\");\n                    var file = fopen(keyFilePathMem, modeMem);\n                    if (!file.isNull()) {\n                        var keyStringMem = Memory.allocUtf8String(hexKey + \"\\n\");\n                        fputs(keyStringMem, file);\n                        fclose(file);\n                        log(\"[\ud83d\udcbe BARE-METAL] AES-Key physisch auf Ger\u00e4t gesichert!\");\n                    }\n                }\n            } catch (e) {}\n        }\n\n        var cryptoTarget = null;\n        var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n        else {\n            evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n            if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n        }\n\n        if (cryptoTarget) {\n            log(\"[4] Setze Crypto-Hook f\u00fcr AES-Extraktion...\");\n            Interceptor.attach(cryptoTarget, {\n                onEnter: function (args) {\n                    try {\n                        if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                    } catch (e) {}\n                },\n                onLeave: function (retval) {\n                    try {\n                        if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                            var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                            var hexKey = ab2hex(keyBuffer);\n                            \n                            if (hexKey !== \"00000000000000000000000000000000\") {\n                                log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                                log(\"Key (Hex): \" + hexKey);\n                                log(\"=============================================\\n\");\n                                saveKeyToFile(hexKey);\n                            }\n                        }\n                    } catch (e) {}\n                }\n            });\n        }\n    }\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message);\n}"
        },
        {
            "id": "8baf1068",
            "name": "0063",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V33 - The Looper Shield)\");\n\n    // =========================================================================\n    // 2. DAS JAVA LOOPER SHIELD (Rettet den Main-Thread vor dem Freeze/Crash)\n    // =========================================================================\n    try {\n        Java.perform(function() {\n            var Handler = Java.use(\"android.os.Handler\");\n            \n            // F\u00e4ngt Events direkt in der Message-Loop des Threads ab\n            Handler.dispatchMessage.implementation = function(msg) {\n                try {\n                    // F\u00fchre die normale Android-Logik aus\n                    this.dispatchMessage(msg);\n                } catch (e) {\n                    var errStr = e.toString();\n                    // Wenn DexGuard seine Straf-Exceptions wirft...\n                    if (errStr.indexOf(\"NegativeArraySizeException\") !== -1 || errStr.indexOf(\"RuntimeException\") !== -1 || errStr.indexOf(\"OutOfMemoryError\") !== -1) {\n                        log(\"[\ud83d\udee1\ufe0f LOOPER SHIELD] RASP-Giftpille auf Main-Thread geschluckt!\");\n                        log(\" -> Neutralisiert: \" + errStr);\n                        // Wir fangen den Fehler ab! Der Looper l\u00e4uft fehlerfrei weiter, kein Freeze!\n                    } else {\n                        throw e; // Normale App-Fehler werfen wir weiter\n                    }\n                }\n            };\n            log(\"[+] Java Looper Shield erfolgreich gespannt!\");\n        });\n    } catch(e) {\n        log(\"[-] Java.perform queued (wartet auf JVM).\");\n    }\n\n    var resolver = new ApiResolver(\"module\");\n\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var accessPtr = findExport(\"access\");\n    var dlsymPtr = findExport(\"dlsym\");\n\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n\n    // =========================================================================\n    // 3. KERNEL I/O SPOOFER (Tarnkappe)\n    // =========================================================================\n    function spoofFilesystem(ptr, pathIdx) {\n        if (!ptr) return;\n        Interceptor.attach(ptr, {\n            onEnter: function (args) {\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[3] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n    spoofFilesystem(accessPtr, 0);\n\n    // =========================================================================\n    // 4. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { \n        return 0; \n    }, 'int64', ['pointer', 'pointer']);\n\n    if (dlsymPtr) {\n        log(\"[4] Aktiviere JNI Zombie Spoofer...\");\n        Interceptor.attach(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr && (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacksMessenger\") !== -1)) {\n                        retval.replace(fakeJniZombie);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR (BoringSSL AES Session Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Setze Crypto-Hooks f\u00fcr AES-Extraktion...\");\n        Interceptor.attach(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Setup abgeschlossen. Warte auf React Native & Smali Patches...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Funktionen im RAM nicht auffindbar.\");\n    }\n\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message);\n}"
        },
        {
            "id": "23648b28",
            "name": "0064",
            "code": "// @ts-nocheck\nfunction log(msg) {\n    try { send(msg); } catch (e) {} \n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V34 - One-Shot Extractor)\");\n\n    var resolver = new ApiResolver(\"module\");\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    var keyFilePathMem = Memory.allocUtf8String(\"/data/data/org.nativescript.LibreLinkUp/ghost_aes_key.txt\");\n\n    // =========================================================================\n    // 2. DAS JAVA LOOPER SHIELD (Rettet den Main-Thread vor dem RASP-Kill)\n    // =========================================================================\n    var javaHooked = false;\n    function tryHookJava() {\n        if (javaHooked) return;\n        try {\n            if (typeof Java !== 'undefined' && Java.available) {\n                javaHooked = true;\n                Java.perform(function() {\n                    try {\n                        var Handler = Java.use(\"android.os.Handler\");\n                        Handler.dispatchMessage.implementation = function(msg) {\n                            try {\n                                this.dispatchMessage(msg);\n                            } catch (e) {\n                                var errStr = e.toString();\n                                // DexGuard wirft irreale Array-Gr\u00f6\u00dfen oder generische Runtimes\n                                if (errStr.indexOf(\"NegativeArraySizeException\") !== -1 || errStr.indexOf(\"RuntimeException\") !== -1) {\n                                    log(\"[\ud83d\udee1\ufe0f LOOPER SHIELD] RASP-Giftpille auf Main-Thread neutralisiert!\");\n                                } else {\n                                    throw e; // Normale App-Fehler weitergeben\n                                }\n                            }\n                        };\n                        log(\"[+] Java Looper Shield aktiv! Main-Thread ist immun.\");\n                    } catch(e) {}\n                });\n            }\n        } catch(e) {}\n    }\n\n    // =========================================================================\n    // 3. KERNEL I/O SPOOFER (Verz\u00f6gert den RASP & Triggert das Shield)\n    // =========================================================================\n    var hookedFuncs = {};\n    function safeHook(ptrAddr, callbacks) {\n        if (!ptrAddr) return;\n        var key = ptrAddr.toString();\n        if (hookedFuncs[key]) return;\n        hookedFuncs[key] = true;\n        Interceptor.attach(ptrAddr, callbacks);\n    }\n\n    function spoofFilesystem(ptr, pathIdx) {\n        safeHook(ptr, {\n            onEnter: function (args) {\n                tryHookJava(); // Triggert das Java-Shield, sobald die JVM l\u00e4uft\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n\n    // =========================================================================\n    // 4. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        safeHook(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr && (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacks\") !== -1)) {\n                        retval.replace(fakeJniZombie);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR & FILE WRITER\n    // =========================================================================\n    function saveKeyToFile(hexKey) {\n        try {\n            var fopenPtr = findExport(\"fopen\");\n            var fputsPtr = findExport(\"fputs\");\n            var fclosePtr = findExport(\"fclose\");\n            \n            if (fopenPtr && fputsPtr && fclosePtr) {\n                var fopen = new NativeFunction(fopenPtr, 'pointer', ['pointer', 'pointer']);\n                var fputs = new NativeFunction(fputsPtr, 'int', ['pointer', 'pointer']);\n                var fclose = new NativeFunction(fclosePtr, 'int', ['pointer']);\n                \n                var modeMem = Memory.allocUtf8String(\"w\");\n                var file = fopen(keyFilePathMem, modeMem);\n                if (!file.isNull()) {\n                    var keyStringMem = Memory.allocUtf8String(hexKey + \"\\n\");\n                    fputs(keyStringMem, file);\n                    fclose(file);\n                    log(\"[\ud83d\udcbe BARE-METAL] AES-Key physisch in ghost_aes_key.txt gesichert!\");\n                }\n            }\n        } catch (e) {\n            log(\"[-] Fehler beim File-Write: \" + e.message);\n        }\n    }\n\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze BoringSSL Crypto-Hooks...\");\n        safeHook(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                            \n                            // Speichere den Key f\u00fcr Phase 2 (Smali-Patch)\n                            saveKeyToFile(hexKey);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Phase 1 Setup bereit. Warte auf Key-Generierung...\");\n    }\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message);\n}"
        },
        {
            "id": "39e58304",
            "name": "0065",
            "code": "// @ts-nocheck\n// =========================================================================\n// 1. ECHTES LOGCAT-LOGGING F\u00dcR AUTONOME GADGETS\n// =========================================================================\nfunction log(msg) {\n    try {\n        if (typeof Java !== 'undefined' && Java.available) {\n            Java.perform(function() {\n                var Log = Java.use(\"android.util.Log\");\n                Log.d(\"GHOST_FRIDA\", msg);\n            });\n        } else {\n            console.log(\"[GHOST_FRIDA] \" + msg);\n        }\n    } catch (e) {\n        console.log(\"[GHOST_FRIDA] \" + msg);\n    }\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V35 - Global Shield)\");\n\n    var resolver = new ApiResolver(\"module\");\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    var keyFilePathMem = Memory.allocUtf8String(\"/data/data/org.nativescript.LibreLinkUp/ghost_aes_key.txt\");\n\n    // =========================================================================\n    // 2. DAS GLOBAL EXCEPTION SHIELD (F\u00e4ngt Thread-5 und OOM ab)\n    // =========================================================================\n    var javaHooked = false;\n    function tryHookJava() {\n        if (javaHooked) return;\n        try {\n            if (typeof Java !== 'undefined' && Java.available) {\n                javaHooked = true;\n                Java.perform(function() {\n                    try {\n                        var Thread = Java.use(\"java.lang.Thread\");\n                        var Throwable = Java.use(\"java.lang.Throwable\");\n                        \n                        // Erstelle einen eigenen Catcher, der JEDE unbehandelte Exception schluckt\n                        var GhostCatcher = Java.registerClass({\n                            name: 'com.ghost.Catcher',\n                            implements: [Java.use('java.lang.Thread$UncaughtExceptionHandler')],\n                            methods: {\n                                uncaughtException: function (thread, ex) {\n                                    var exMsg = Java.cast(ex, Throwable).toString();\n                                    log(\"[\ud83d\udee1\ufe0f GLOBAL SHIELD] Crash auf \" + thread.getName() + \" neutralisiert! Grund: \" + exMsg);\n                                    // Wir werfen NICHTS weiter. Die App bleibt am Leben.\n                                }\n                            }\n                        });\n                        \n                        // Setze unseren Catcher als Standard f\u00fcr alle Threads\n                        Thread.setDefaultUncaughtExceptionHandler(GhostCatcher.$new());\n                        log(\"[+] Global Exception Shield aktiv! Alle Threads sind immun.\");\n                    } catch(e) {\n                        log(\"[-] Fehler beim Shield-Setup: \" + e);\n                    }\n                });\n            }\n        } catch(e) {}\n    }\n\n    // =========================================================================\n    // 3. KERNEL I/O SPOOFER (Verz\u00f6gert den RASP & Triggert das Shield)\n    // =========================================================================\n    var hookedFuncs = {};\n    function safeHook(ptrAddr, callbacks) {\n        if (!ptrAddr) return;\n        var key = ptrAddr.toString();\n        if (hookedFuncs[key]) return;\n        hookedFuncs[key] = true;\n        Interceptor.attach(ptrAddr, callbacks);\n    }\n\n    function spoofFilesystem(ptr, pathIdx) {\n        safeHook(ptr, {\n            onEnter: function (args) {\n                tryHookJava(); // Triggert das Java-Shield, sobald die JVM l\u00e4uft\n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n\n    // =========================================================================\n    // 4. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        safeHook(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr && (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacks\") !== -1)) {\n                        retval.replace(fakeJniZombie);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR & FILE WRITER\n    // =========================================================================\n    function saveKeyToFile(hexKey) {\n        try {\n            var fopenPtr = findExport(\"fopen\");\n            var fputsPtr = findExport(\"fputs\");\n            var fclosePtr = findExport(\"fclose\");\n            \n            if (fopenPtr && fputsPtr && fclosePtr) {\n                var fopen = new NativeFunction(fopenPtr, 'pointer', ['pointer', 'pointer']);\n                var fputs = new NativeFunction(fputsPtr, 'int', ['pointer', 'pointer']);\n                var fclose = new NativeFunction(fclosePtr, 'int', ['pointer']);\n                \n                var modeMem = Memory.allocUtf8String(\"w\");\n                var file = fopen(keyFilePathMem, modeMem);\n                if (!file.isNull()) {\n                    var keyStringMem = Memory.allocUtf8String(hexKey + \"\\n\");\n                    fputs(keyStringMem, file);\n                    fclose(file);\n                    log(\"[\ud83d\udcbe BARE-METAL] AES-Key physisch in ghost_aes_key.txt gesichert!\");\n                }\n            }\n        } catch (e) {\n            log(\"[-] Fehler beim File-Write: \" + e.message);\n        }\n    }\n\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze BoringSSL Crypto-Hooks...\");\n        safeHook(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                            \n                            // Speichere den Key f\u00fcr Phase 2 (Smali-Patch)\n                            saveKeyToFile(hexKey);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[+] Phase 1 Setup bereit. Warte auf Key-Generierung...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Target nicht gefunden!\");\n    }\n} catch (err) {\n    log(\"[!] FATAL SCRIPT ERROR: \" + err.message);\n}"
        },
        {
            "id": "49eb8da2",
            "name": "0066_log",
            "code": "// @ts-nocheck\n// =========================================================================\n// 1. FRAMEWORK-LOGGING VIA EVENTBUS\n// =========================================================================\nfunction log(msg) {\n    try {\n        send(msg);\n    } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V36 - Global Shield & EventBus)\");\n\n    // Nutzt den systemnahen Resolver, der den 'Module'-Namespace umgeht (Namespace-Isolation)\n    var resolver = new ApiResolver(\"module\");\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    var keyFilePathMem = Memory.allocUtf8String(\"/data/data/org.nativescript.LibreLinkUp/ghost_aes_key.txt\");\n\n    // =========================================================================\n    // 2. DAS GLOBAL EXCEPTION SHIELD (F\u00e4ngt Thread-5 und OOM ab)\n    // =========================================================================\n    var javaHooked = false;\n    function tryHookJava() {\n        if (javaHooked) return;\n        try {\n            if (typeof Java !== 'undefined' && Java.available) {\n                javaHooked = true;\n                Java.perform(function() {\n                    try {\n                        var Thread = Java.use(\"java.lang.Thread\");\n                        var Throwable = Java.use(\"java.lang.Throwable\");\n                        \n                        var GhostCatcher = Java.registerClass({\n                            name: 'com.ghost.Catcher',\n                            implements: [Java.use('java.lang.Thread$UncaughtExceptionHandler')],\n                            methods: {\n                                uncaughtException: function (thread, ex) {\n                                    var exMsg = Java.cast(ex, Throwable).toString();\n                                    log(\"[\ud83d\udee1\ufe0f GLOBAL SHIELD] Crash auf \" + thread.getName() + \" neutralisiert! Grund: \" + exMsg);\n                                }\n                            }\n                        });\n                        \n                        Thread.setDefaultUncaughtExceptionHandler(GhostCatcher.$new());\n                        log(\"[+] Global Exception Shield aktiv! Alle Threads sind immun.\");\n                    } catch(e) {\n                        log(\"[-] Fehler beim Shield-Setup: \" + e);\n                    }\n                });\n            }\n        } catch(e) {}\n    }\n\n    // =========================================================================\n    // 3. KERNEL I/O SPOOFER (Verz\u00f6gert den RASP & Triggert das Shield)\n    // =========================================================================\n    var hookedFuncs = {};\n    function safeHook(ptrAddr, callbacks) {\n        if (!ptrAddr) return;\n        var key = ptrAddr.toString();\n        if (hookedFuncs[key]) return;\n        hookedFuncs[key] = true;\n        Interceptor.attach(ptrAddr, callbacks);\n    }\n\n    function spoofFilesystem(ptr, pathIdx) {\n        safeHook(ptr, {\n            onEnter: function (args) {\n                tryHookJava(); \n                try {\n                    // Pointer-Safeguarding f\u00fcr Pfade\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n\n    // =========================================================================\n    // 4. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        safeHook(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr && (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacks\") !== -1)) {\n                        retval.replace(fakeJniZombie);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 5. CRYPTO EXTRACTOR & FILE WRITER\n    // =========================================================================\n    function saveKeyToFile(hexKey) {\n        try {\n            var fopenPtr = findExport(\"fopen\");\n            var fputsPtr = findExport(\"fputs\");\n            var fclosePtr = findExport(\"fclose\");\n            \n            if (fopenPtr && fputsPtr && fclosePtr) {\n                var fopen = new NativeFunction(fopenPtr, 'pointer', ['pointer', 'pointer']);\n                var fputs = new NativeFunction(fputsPtr, 'int', ['pointer', 'pointer']);\n                var fclose = new NativeFunction(fclosePtr, 'int', ['pointer']);\n                \n                var modeMem = Memory.allocUtf8String(\"w\");\n                var file = fopen(keyFilePathMem, modeMem);\n                if (!file.isNull()) {\n                    var keyStringMem = Memory.allocUtf8String(hexKey + \"\\n\");\n                    fputs(keyStringMem, file);\n                    fclose(file);\n                    log(\"[\ud83d\udcbe BARE-METAL] AES-Key physisch in ghost_aes_key.txt gesichert!\");\n                }\n            }\n        } catch (e) {\n            log(\"[-] Fehler beim File-Write: \" + e.message);\n        }\n    }\n\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze BoringSSL Crypto-Hooks...\");\n        safeHook(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    // Pointer-Safeguarding: NativePointer als Strings im this-Kontext sichern\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        // String zur\u00fcck in Pointer wandeln und lesen\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                            \n                            saveKeyToFile(hexKey);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[5] Phase 1 Setup bereit. Warte auf Key-Generierung...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Target nicht gefunden!\");\n    }\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "96d80282",
            "name": "67_Success_Extract_Ghost_Key_and_go_Stealth_again",
            "code": "// @ts-nocheck\n// =========================================================================\n// 1. FRAMEWORK-LOGGING VIA EVENTBUS\n// =========================================================================\nfunction log(msg) {\n    try {\n        send(msg);\n    } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V37 - Global Shield & OOM Slayer)\");\n\n    var resolver = new ApiResolver(\"module\");\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var mallocPtr = findExport(\"malloc\");\n    var mmapPtr = findExport(\"mmap\");\n    \n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    var keyFilePathMem = Memory.allocUtf8String(\"/data/data/org.nativescript.LibreLinkUp/ghost_aes_key.txt\");\n\n    // =========================================================================\n    // 2. DAS GLOBAL EXCEPTION SHIELD (F\u00e4ngt Thread-5 und Java OOM ab)\n    // =========================================================================\n    var javaHooked = false;\n    function tryHookJava() {\n        if (javaHooked) return;\n        try {\n            if (typeof Java !== 'undefined' && Java.available) {\n                javaHooked = true;\n                Java.perform(function() {\n                    try {\n                        var Thread = Java.use(\"java.lang.Thread\");\n                        var Throwable = Java.use(\"java.lang.Throwable\");\n                        \n                        var GhostCatcher = Java.registerClass({\n                            name: 'com.ghost.Catcher',\n                            implements: [Java.use('java.lang.Thread$UncaughtExceptionHandler')],\n                            methods: {\n                                uncaughtException: function (thread, ex) {\n                                    var exMsg = Java.cast(ex, Throwable).toString();\n                                    log(\"[\ud83d\udee1\ufe0f GLOBAL SHIELD] Crash auf \" + thread.getName() + \" neutralisiert! Grund: \" + exMsg);\n                                }\n                            }\n                        });\n                        \n                        Thread.setDefaultUncaughtExceptionHandler(GhostCatcher.$new());\n                        log(\"[+] Global Exception Shield aktiv! Alle Threads sind immun.\");\n                    } catch(e) {\n                        log(\"[-] Fehler beim Shield-Setup: \" + e);\n                    }\n                });\n            }\n        } catch(e) {}\n    }\n\n    // =========================================================================\n    // 3. PR\u00c4ZISIONS-OOM-SLAYER (Zerschneidet die 8.4 GB RAM Bombe)\n    // =========================================================================\n    var ONE_GIGABYTE_PTR = ptr(\"0x3FFFFFFF\"); \n\n    if (mallocPtr) {\n        Interceptor.attach(mallocPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[0].compare(ONE_GIGABYTE_PTR) > 0) { \n                        var caller = Process.findModuleByAddress(this.returnAddress);\n                        if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                            log(\"[\ud83d\udd25 OOM-SLAYER] RASP malloc-Bombe entsch\u00e4rft! \" + args[0]);\n                            args[0] = ptr(1); \n                        }\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    if (mmapPtr) {\n        Interceptor.attach(mmapPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[1].compare(ONE_GIGABYTE_PTR) > 0) { \n                        var caller = Process.findModuleByAddress(this.returnAddress);\n                        if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                            log(\"[\ud83d\udd25 OOM-SLAYER] RASP mmap-Bombe entsch\u00e4rft! \" + args[1]);\n                            args[1] = ptr(4096); \n                        }\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. KERNEL I/O SPOOFER (Triggert auch das Java Shield)\n    // =========================================================================\n    var hookedFuncs = {};\n    function safeHook(ptrAddr, callbacks) {\n        if (!ptrAddr) return;\n        var key = ptrAddr.toString();\n        if (hookedFuncs[key]) return;\n        hookedFuncs[key] = true;\n        Interceptor.attach(ptrAddr, callbacks);\n    }\n\n    function spoofFilesystem(ptr, pathIdx) {\n        safeHook(ptr, {\n            onEnter: function (args) {\n                tryHookJava(); \n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[2] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n\n    // =========================================================================\n    // 5. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n    if (dlsymPtr) {\n        log(\"[3] Aktiviere JNI Zombie Spoofer...\");\n        safeHook(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym;\n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr && (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacks\") !== -1)) {\n                        retval.replace(fakeJniZombie);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 6. CRYPTO EXTRACTOR & FILE WRITER\n    // =========================================================================\n    function saveKeyToFile(hexKey) {\n        try {\n            var fopenPtr = findExport(\"fopen\");\n            var fputsPtr = findExport(\"fputs\");\n            var fclosePtr = findExport(\"fclose\");\n            \n            if (fopenPtr && fputsPtr && fclosePtr) {\n                var fopen = new NativeFunction(fopenPtr, 'pointer', ['pointer', 'pointer']);\n                var fputs = new NativeFunction(fputsPtr, 'int', ['pointer', 'pointer']);\n                var fclose = new NativeFunction(fclosePtr, 'int', ['pointer']);\n                \n                var modeMem = Memory.allocUtf8String(\"w\");\n                var file = fopen(keyFilePathMem, modeMem);\n                if (!file.isNull()) {\n                    var keyStringMem = Memory.allocUtf8String(hexKey + \"\\n\");\n                    fputs(keyStringMem, file);\n                    fclose(file);\n                    log(\"[\ud83d\udcbe BARE-METAL] AES-Key physisch in ghost_aes_key.txt gesichert!\");\n                }\n            }\n        } catch (e) {\n            log(\"[-] Fehler beim File-Write: \" + e.message);\n        }\n    }\n\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[4] Setze BoringSSL Crypto-Hooks...\");\n        safeHook(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString();\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 AES KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"=============================================\\n\");\n                            saveKeyToFile(hexKey);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n        log(\"[5] Setup bereit. App darf jetzt laden...\");\n    } else {\n        log(\"[-] BoringSSL Crypto-Target nicht gefunden!\");\n    }\n} catch (globalErr) {\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message);\n}"
        },
        {
            "id": "556a3ab5",
            "name": "68",
            "code": "// @ts-nocheck\n// =========================================================================\n// 1. FRAMEWORK-LOGGING VIA EVENTBUS (Regel 1)\n// =========================================================================\nfunction log(msg) {\n    try {\n        send(msg);\n    } catch (e) {}\n}\n\nfunction ab2hex(ab) {\n    var view = new Uint8Array(ab);\n    var hex = \"\";\n    for (var i = 0; i < view.length; i++) {\n        var b = view[i].toString(16);\n        hex += (b.length === 1 ? \"0\" + b : b);\n    }\n    return hex;\n}\n\ntry {\n    log(\"\\n=======================================================\");\n    log(\"[1] BARE-METAL GHOST PROTOCOL (V39 - Dual Extractor & JCA Oracle)\");\n\n    // Regel 6: ApiResolver statt Module.findExportByName\n    var resolver = new ApiResolver(\"module\");\n    function findExport(funcName) {\n        var matches = resolver.enumerateMatches(\"exports:*libc.so!\" + funcName);\n        if (matches.length === 0) matches = resolver.enumerateMatches(\"exports:*!\" + funcName);\n        return matches.length > 0 ? matches[0].address : null;\n    }\n\n    var openPtr = findExport(\"open\");\n    var openatPtr = findExport(\"openat\");\n    var statPtr = findExport(\"stat\");\n    var dlsymPtr = findExport(\"dlsym\");\n    var mallocPtr = findExport(\"malloc\");\n    var mmapPtr = findExport(\"mmap\");\n    \n    var cleanApkMem = Memory.allocUtf8String(\"/data/local/tmp/clean_base.apk\");\n    var cleanMapsMem = Memory.allocUtf8String(\"/data/local/tmp/clean_maps.txt\");\n    \n    // ZWEI getrennte Dateien f\u00fcr den alten und neuen Key\n    var staticKeyPathMem = Memory.allocUtf8String(\"/data/data/org.nativescript.LibreLinkUp/ghost_aes_key_static.txt\");\n    var jcaDumpPathMem = Memory.allocUtf8String(\"/data/data/org.nativescript.LibreLinkUp/ghost_jca_dump.txt\");\n\n    // Universelle File-Write Funktion\n    function saveStringToFile(contentStr, pathMemPtr) {\n        try {\n            var fopenPtr = findExport(\"fopen\");\n            var fputsPtr = findExport(\"fputs\");\n            var fclosePtr = findExport(\"fclose\");\n            \n            if (fopenPtr && fputsPtr && fclosePtr) {\n                var fopen = new NativeFunction(fopenPtr, 'pointer', ['pointer', 'pointer']);\n                var fputs = new NativeFunction(fputsPtr, 'int', ['pointer', 'pointer']);\n                var fclose = new NativeFunction(fclosePtr, 'int', ['pointer']);\n                \n                var modeMem = Memory.allocUtf8String(\"w\");\n                var file = fopen(pathMemPtr, modeMem);\n                if (!file.isNull()) {\n                    var contentMem = Memory.allocUtf8String(contentStr + \"\\n\");\n                    fputs(contentMem, file);\n                    fclose(file);\n                    log(\"[\ud83d\udcbe BARE-METAL] Datei gesichert: \" + pathMemPtr.readUtf8String());\n                }\n            }\n        } catch (e) {\n            log(\"[-] Fehler beim File-Write: \" + e.message);\n        }\n    }\n\n    // =========================================================================\n    // 2. DAS GLOBAL EXCEPTION SHIELD\n    // =========================================================================\n    var javaHooked = false;\n    function tryHookJava() {\n        if (javaHooked) return;\n        try {\n            if (typeof Java !== 'undefined' && Java.available) {\n                javaHooked = true;\n                Java.perform(function() {\n                    try {\n                        var Thread = Java.use(\"java.lang.Thread\");\n                        var Throwable = Java.use(\"java.lang.Throwable\");\n                        \n                        var GhostCatcher = Java.registerClass({\n                            name: 'com.ghost.Catcher',\n                            implements: [Java.use('java.lang.Thread$UncaughtExceptionHandler')],\n                            methods: {\n                                uncaughtException: function (thread, ex) {\n                                    var exMsg = Java.cast(ex, Throwable).toString();\n                                    log(\"[\ud83d\udee1\ufe0f GLOBAL SHIELD] Crash neutralisiert! Grund: \" + exMsg);\n                                }\n                            }\n                        });\n                        \n                        Thread.setDefaultUncaughtExceptionHandler(GhostCatcher.$new());\n                        log(\"[2] Global Exception Shield aktiv! Threads sind immun.\");\n                        \n                        // Direkter Aufruf des JCA-Orakels innerhalb des sicheren Java-Kontexts\n                        initJcaOracle();\n                    } catch(e) {\n                        log(\"[-] Fehler beim Shield-Setup: \" + e);\n                    }\n                });\n            }\n        } catch(e) {}\n    }\n\n    // =========================================================================\n    // 3. PR\u00c4ZISIONS-OOM-SLAYER (Zerschneidet die 8.4 GB RAM Bombe)\n    // =========================================================================\n    var ONE_GIGABYTE_PTR = ptr(\"0x3FFFFFFF\"); \n\n    if (mallocPtr) {\n        Interceptor.attach(mallocPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[0].compare(ONE_GIGABYTE_PTR) > 0) { \n                        var caller = Process.findModuleByAddress(this.returnAddress);\n                        if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                            log(\"[\ud83d\udd25 OOM-SLAYER] RASP malloc-Bombe entsch\u00e4rft!\");\n                            args[0] = ptr(1); \n                        }\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    if (mmapPtr) {\n        Interceptor.attach(mmapPtr, {\n            onEnter: function(args) {\n                try {\n                    if (args[1].compare(ONE_GIGABYTE_PTR) > 0) { \n                        var caller = Process.findModuleByAddress(this.returnAddress);\n                        if (caller && (caller.name.indexOf(\"db0c\") !== -1 || caller.name.indexOf(\"dexguard\") !== -1)) {\n                            log(\"[\ud83d\udd25 OOM-SLAYER] RASP mmap-Bombe entsch\u00e4rft!\");\n                            args[1] = ptr(4096); \n                        }\n                    }\n                } catch(e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 4. KERNEL I/O SPOOFER (Triggert das Java Shield)\n    // =========================================================================\n    var hookedFuncs = {};\n    function safeHook(ptrAddr, callbacks) {\n        if (!ptrAddr) return;\n        var key = ptrAddr.toString();\n        if (hookedFuncs[key]) return;\n        hookedFuncs[key] = true;\n        Interceptor.attach(ptrAddr, callbacks);\n    }\n\n    function spoofFilesystem(ptrAddr, pathIdx) {\n        safeHook(ptrAddr, {\n            onEnter: function (args) {\n                tryHookJava(); \n                try {\n                    this.pathStr = args[pathIdx].readUtf8String();\n                    if (!this.pathStr) return;\n                    if (this.pathStr.indexOf(\"base.apk\") !== -1 && this.pathStr.indexOf(\"org.nativescript.LibreLinkUp\") !== -1 && this.pathStr.indexOf(\"clean_base.apk\") === -1) {\n                        args[pathIdx] = cleanApkMem;\n                    } else if (this.pathStr.indexOf(\"/proc/\") !== -1 && this.pathStr.indexOf(\"maps\") !== -1) {\n                        args[pathIdx] = cleanMapsMem;\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    log(\"[3] Aktiviere Kernel I/O Spoofer...\");\n    spoofFilesystem(openPtr, 0);\n    spoofFilesystem(openatPtr, 1);\n    spoofFilesystem(statPtr, 0);\n\n    // =========================================================================\n    // 5. JNI ZOMBIE SPOOFER\n    // =========================================================================\n    var fakeJniZombie = new NativeCallback(function() { return 0; }, 'int64', ['pointer', 'pointer']);\n    if (dlsymPtr) {\n        log(\"[4] Aktiviere JNI Zombie Spoofer...\");\n        safeHook(dlsymPtr, {\n            onEnter: function(args) {\n                try { \n                    var sym = args[1].readUtf8String(); \n                    if(sym) this.symStr = sym; \n                } catch(e) {}\n            },\n            onLeave: function(retval) {\n                try {\n                    if (this.symStr && (this.symStr.indexOf(\"Java_o_MediaBrowserCompat\") !== -1 || this.symStr.indexOf(\"stackFrames\") !== -1 || this.symStr.indexOf(\"Java_o_setCallbacks\") !== -1)) {\n                        retval.replace(fakeJniZombie);\n                    }\n                } catch (e) {}\n            }\n        });\n    }\n\n    // =========================================================================\n    // 6. C-LEVEL CRYPTO EXTRACTOR (\"Alter\" Static Ghost Key)\n    // =========================================================================\n    var cryptoTarget = null;\n    var evpMatches = resolver.enumerateMatches(\"exports:*!EVP_CipherInit_ex\");\n    if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    else {\n        evpMatches = resolver.enumerateMatches(\"exports:*!EVP_DecryptInit_ex\");\n        if (evpMatches.length > 0) cryptoTarget = evpMatches[0].address;\n    }\n\n    if (cryptoTarget) {\n        log(\"[5] Bringe C-Level BoringSSL Hooks an...\");\n        safeHook(cryptoTarget, {\n            onEnter: function (args) {\n                try {\n                    if (args[3].toString() !== \"0x0\") this.keyPtrStr = args[3].toString(); // Pointer-Safeguarding\n                } catch (e) {}\n            },\n            onLeave: function (retval) {\n                try {\n                    if (this.keyPtrStr && this.keyPtrStr !== \"0x0\") {\n                        var keyBuffer = ptr(this.keyPtrStr).readByteArray(16); \n                        var hexKey = ab2hex(keyBuffer);\n                        \n                        if (hexKey !== \"00000000000000000000000000000000\") {\n                            log(\"\\n========== [\ud83d\udd11 STATIC C-KEY ABGEFANGEN] ==========\");\n                            log(\"Key (Hex): \" + hexKey);\n                            log(\"==================================================\\n\");\n                            saveStringToFile(hexKey, staticKeyPathMem);\n                        }\n                    }\n                } catch (e) {}\n            }\n        });\n    } else {\n        log(\"[-] C-Level Crypto-Target nicht gefunden!\");\n    }\n\n    // =========================================================================\n    // 7. DAS JCA KEYSTORE ORAKEL (\"Neuer\" Java-Level Key)\n    // =========================================================================\n    function initJcaOracle() {\n        try {\n            var Cipher = Java.use(\"javax.crypto.Cipher\");\n            var StringClass = Java.use(\"java.lang.String\");\n\n            log(\"[6] Bringe JCA Cipher.doFinal() Hook an...\");\n\n            Cipher.doFinal.overload('[B').implementation = function (input) {\n                var result = null;\n                try {\n                    result = this.doFinal(input);\n                } catch (cryptoErr) {\n                    throw cryptoErr; \n                }\n\n                if (result !== null) {\n                    try {\n                        var decryptedStr = StringClass.$new(result).toString();\n                        \n                        // Filtern nach Abbott ALE-Tresor oder purem 128-Bit Hex-Key\n                        if (decryptedStr.indexOf(\"sessionKey\") !== -1 || decryptedStr.indexOf(\"devicePublicKey\") !== -1 || /^[0-9a-fA-F]{32}$/.test(decryptedStr)) {\n                            log(\"\\n=================================================================\");\n                            log(\" \ud83c\udfaf JACKPOT! JCA-Orakel hat Hardware-Keystore Daten abgefangen!\");\n                            log(\"=================================================================\");\n                            log(decryptedStr);\n                            log(\"=================================================================\\n\");\n                            \n                            // Schreibe kompletten Output (JSON oder Hex) in JCA-Dump\n                            saveStringToFile(decryptedStr, jcaDumpPathMem);\n                        }\n                    } catch (e) {}\n                }\n                return result;\n            };\n            log(\"[7] JCA Oracle aktiv! Warte auf Entschl\u00fcsselung...\");\n        } catch (setupErr) {\n            log(\"[-] Fehler beim Setup des JCA-Oracles: \" + setupErr);\n        }\n    }\n\n    log(\"[8] Setup vollst\u00e4ndig. App l\u00e4uft!\");\n\n} catch (globalErr) {\n    // Regel 4: The Stacktrace Rule\n    log(\"[!] GLOBAL FATAL ERROR: \" + globalErr.message + \"\\n\" + globalErr.stack);\n}"
        }
    ],
    "collections": [
        {
            "id": "f1af4f05",
            "name": "TEst",
            "script_ids": [
                "ec5079fc",
                "f49153a2",
                "d3a39db9"
            ]
        }
    ]
}